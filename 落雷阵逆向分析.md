# 落雷阵（Spell1018 / ThunderAura）逆向分析

> 范围：仅玩家主动法术「落雷阵」（`abilityType = 1018`，配置 ID `10181–10183`）。Boss6 二阶段对 1018 的免伤特判见 6.4，不展开怪物侧。
> 方法：`SpellConfig.json` + 反编译 `Assembly-CSharp` 交叉验证。未标注「推测」「未确认」的结论均有代码/数据直接证据。
> 主路径：玩家施法走 **ECS**（`Spell1018ThunderAuraSystem` 内的两个 Job）。Mono `Spell1018ThunderAura` 为残留轨，第 10 节单独对照。
> 配套：总览见《魔法工艺法术系统逆向报告.md》，原始数值见《法术数值总表.md》。离线可开见 `落雷阵逆向分析.html`。
> **本文是 `isDPS = true` 法术的第一篇文档**，第 2.3 节把「持续伤害模型」这套通用管线单独立了一节，后续 DPS 类法术（高压水流、龙息、激光水晶等）应直接引用。
> 第 7 节对已逆向的 21 张增强符文做了逐一校验，第 8 节额外覆盖 5 张尚无文档但对落雷阵影响极大的符文，第 13 节列出需要回写既有文档的位置。

---

## 1. 身份与定位

| 项 | 值 |
| --- | --- |
| 中文名 | 落雷阵 |
| 英文名 | Thunderstorm |
| `abilityType` | `1018` / `SpellAbilityType.ThunderAura` |
| 配置 ID | `10181`（Lv1）/ `10182`（Lv2）/ `10183`（Lv3） |
| 文案 ID | 名 `701018x` / 机制 `711018x` / 风味 `7210181` / 短评 `7310181`（后两条**只有一级**） |
| 稀有度 | 普通（`dropType=1`） |
| `useType` | `0`（Missile 主动飞弹）——但 `speed = 0`，实际是**原地光环** |
| 槽位 / 蓝耗 | `slotCost=1`；`mpCost` = 33 / 43 / 56（一次性，**不按 tick 扣**） |
| 伤害 | `damage` = 22 / 44 / 88，**`isDPS = true`**、`DPSDamageInterval = 0.25` |
| 作用半径 | `radius` = 2.8 / 3.0 / 3.2 |
| 存活 | `duration = 5`，`speed = 0`，`knockback = 0`，`angle = 0`，`shootCount = 1` |
| 关键字段 | `float1 = 100/130/160`（单目标加伤 %）、`float2 = 0.6`（间隔抖动比）、`int1 = 3/5/7`（**单次结算命中上限**） |
| 预制体 | 三级共用 `prefab="10181"`，图标 `1018` |
| 合成 | Lv1→Lv2→Lv3（`canCompound`：Lv1/2=`true`，Lv3=`false`） |
| 售价（金币） | 9 / 27 / 81 |
| 复杂度 | `CalculateSpellComplexity` 记 3 分（与滚球、蝴蝶、激光、悬浮火炬同档） |

**机制一句话**：在枪口位置放下一个**完全静止**的雷雨区，每约 0.25 秒结算一次，在半径内挑最多 `int1` 个异阵营目标同时劈；半径内目标不足 `int1` 时，从 **1.5 倍半径的环带**里随机补人（这就是面板说的「传导」）；每次结算的伤害是 `damage × 0.25`，所以面板 `damage` 字段就是**每目标每秒伤害**；只劈到一个目标时整发伤害乘 `float1%`。

```mermaid
flowchart TD
    S["施放：在枪口生成实体，speed=0 原地不动"] --> I["首帧 Spell1018ThunderAuraInitializeTag<br/>非坠落 → SpawnChild 1018_OnGround 地面贴花"]
    I --> T["Spell1018Job（每渲染帧）<br/>DamageTimer += dt"]
    T --> C{"DamageTimer ≥ 0.25 ?"}
    C -->|否| T
    C -->|是| R["OverlapSphere(radius) 捞可攻击实体<br/>RemoveCannotAttackSpell + 去同阵营/NotAttack/Brittleness"]
    R --> E{"数量 < int1 ?"}
    E -->|是| L["GetLightningLineAttackTargets<br/>在 radius×1.5 内随机补人，最多补到 int1"]
    E -->|否| N
    L --> N["num = min(int1, 数量)"]
    N --> D["逐个 TryAttackEntity<br/>伤害 = damage×0.25×(num==1 ? float1/100 : 1)<br/>+ 1018_Chain 视觉链 + 命中粒子 + 震屏"]
    D --> J["DamageTimer -= 0.25 + Random(−0.15, +0.15)"]
    J --> T
    T -.SpellSystem.UpdateDuration.-> O{"DurationTimer ≥ 5 ?"}
    O -->|是| H["Speed=0 → 累加 HoverTimer<br/>（悬停期光环仍在结算！见 7.4）"]
    H --> X["SpellDestroyTag → 分裂 / 结束触发 / 雷霆晶石落雷"]
```

文案（`TextConfig_Spell`）：

- 名称 `7010181–7010183`：落雷阵 / Thunderstorm（三级同名）
- 机制 `7110181`：> 落雷可传导int1次
- 机制 `7110182` / `7110183`：> 落雷可传导int1次\仅命中一名敌方单位时法术伤害×float1%
- 风味 `7210181`：> 划定一片区域，落雷会攻击区域内的目标，区域外的敌人也可能会被牵连。
- 短评 `7310181`：> 定期连锁攻击敌人的范围法术

三条文案各对应一条真机制，但**「传导」这个词有歧义**（见 3.4）。Lv1 的机制文案不写 `float1` 那一句，因为 Lv1 的 `float1 = 100`，乘上去等于不变——这是配置和文案联动的一个正确设计。

---

## 2. 数值结构

### 2.1 配置表原始值（`assets_export/SpellConfig.json`）

| ID | Lv | damage | isDPS | DPSDamageInterval | mpCost | duration | radius | speed | knockback | float1 | float2 | int1 | 售价 | 可合成 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10181 | 1 | **22** | true | 0.25 | 33 | 5 | **2.8** | 0 | 0 | **100** | 0.6 | **3** | 9 | true |
| 10182 | 2 | **44** | true | 0.25 | 43 | 5 | **3.0** | 0 | 0 | **130** | 0.6 | **5** | 27 | true |
| 10183 | 3 | **88** | true | 0.25 | 56 | 5 | **3.2** | 0 | 0 | **160** | 0.6 | **7** | 81 | false |

规律：升级同时抬 `damage`（×2 每级）、`int1`（+2 每级）、`float1`（+30 每级），`radius` 只微涨（2.8→3.0→3.2，共 +14%）。`duration` / `speed` / `knockback` / `angle` / `float2` / `DPSDamageInterval` 三级完全不变，`float3`、`int2/3`、`gravity`、`upSpeed`、`summonID` 全 0。`isKeepCasting=false`、`canCancelCasting=false`、`isParentTypeSpell=false`、`isSplitSpell=false`、`needActivate=false`、`criticalChance=0`。

`haveEffecforMissileSpell` / `haveEffecforSummonSpell` 都是 **false**——它们只对 `useType=2` 的强化符文有意义，主动法术这两位不被任何逻辑读取。

### 2.2 字段语义

| 字段 | 语义 | 置信度 | 证据 |
| --- | --- | --- | --- |
| **`isDPS = true`** | 把 `DPSDamageInterval` 写进 `SpellConfigComponentData.DamageInterval`，触发 2.3 的持续伤害模型 | 高 | `ShootSpellUtils.cs` L286；`SpellSingleton.cs` L86 |
| **`DPSDamageInterval = 0.25`** | 结算周期基数，同时是**单次伤害的比例系数** | 高 | `Spell1018ThunderAuraSystem.cs` L160–162；`TakeDamageInfo_Dots.cs` L153 |
| **`damage = 22/44/88`** | 因为上一条，面板数字实际是**每目标每秒伤害**，面板加「(秒伤)」后缀 | 高 | `SlotData.cs` L385–388；`SpellConfig.cs` L283–287 |
| **`radius = 2.8/3.0/3.2`** | 每次结算的 `OverlapSphere` 半径；也是传导环带的底数（×1.5）和坠落态每跳的步长 | 高 | 系统 L167–168、L274、L847 |
| **`float1 = 100/130/160`** | **仅当本次结算命中数恰好为 1 时**，伤害 `× float1/100`。Lv1 的 100 等于无效果 | 高 | 系统 L191–194（光环）、L773–776（坠落） |
| **`float2 = 0.6`** | 结算间隔抖动比。抖动量 = `DamageInterval × float2 = 0.15`，扣减量落在 `[0.10, 0.40]` | 高 | 系统 L252–256、L227 |
| **`int1 = 3/5/7`** | **单次结算的命中数上限**（不是「额外传导次数」，见 3.4） | 高 | 系统 L171–175（`math.min(Int1, entities.Length)`） |
| `speed = 0` | 光环不移动。但这是 `AttributeValue.Base`，**能被加速器 / 公转 / 寻踪 抬成非 0**，见 7.6 / 8.1 / 8.2 | 高 | `ShootSpellUtils.cs` L198–205；`AttributeValue.cs` L24–27 |
| `knockback = 0` | 无击退。全表最低档 | 高 | 配置 |
| `angle = 0`、`shootCount = 1` | 无散射、单发。散射类符文对它只改一个不被读取的 `Scatter` 字段 | 高 | 配置 + `ShootSpellUtils.cs` L282 |
| `float3` / `int2` / `int3` / `gravity` / `upSpeed` | 恒 0，无引用 | 高 | 配置 + 全工程检索 |

「1.5 倍传导环带」的系数**不写在配置里**，是 `Spell1018Job.GetLightningLineAttackTargets` 里的硬编码字面量 `1.5f`（系统 L274）。`GameConst.ThunderAuraOutRangeEnemyDetectRangeRatio = 1.5f`（`GameConst.cs` L649）存在但 ECS 侧没有引用它；`Spell1018ThunderAuraAuthoring.LightningChainMaxConductRangeRatio`（Authoring L21）是**声明后 Baker 完全不读的死字段**。三处同一个 1.5，只有字面量那处生效。

### 2.3 通用管线：`isDPS` 的持续伤害模型

这是本文第一次展开，后续 DPS 类法术文档应直接引用本节。

**（1）配置 → 组件**。`isDPS` 决定 `DamageInterval` 是否非 0：

```286:286:decompiled/Assembly-CSharp/ShootSpellUtils.cs
			DamageInterval = (configCopy.isDPS ? configCopy.DPSDamageInterval : 0f),
```

**（2）组件 → 单次伤害**。`TakeDamageInfo_Dots.NewInfo` 用 `DamageInterval` 是否大于 0 做分支：

```153:153:decompiled/Assembly-CSharp/TakeDamageInfo_Dots.cs
		info.damage = ((config.DamageInterval > 0f) ? (config.Damage.Calculate() * config.DamageInterval) : config.Damage.Calculate());
```

所以对落雷阵而言，**每次 `MakeDamageInfo` 拿到的伤害已经是 `damage × 0.25`**。落雷阵 Lv3 一次结算对一个目标打 `88 × 0.25 = 22`，一秒四次 = 88。面板的 `damage` 字段名义上是「单次伤害」，实际语义被 `isDPS` 改成了「每秒伤害」。

**（3）面板文案**。`isDPS` 唯一的 UI 作用是在伤害数字后面挂一个链接文本 `14000302`（即「秒伤」）：

```384:388:decompiled/Assembly-CSharp/SlotData.cs
			stringBuilder.AppendStringField(14000301, spellDamage_FinalPlayerValue.Result.ToStringDamage(), SpellInfoExtend.ColorByValue_BigGood(configIgnoreMimic.damage, spellDamage_FinalPlayerValue.Result), newLine: true, newLineStartAs);
			if (configIgnoreMimic.isDPS)
			{
				stringBuilder.AppendLink(" (", 14000302.GetText(), ")");
			}
```

**（4）蓝耗与 `isDPS` 无关**。`GetSpellManaCost` 完全不看 `isDPS`（`SpellShootData.cs` L614–644），`mpCost = 33` 是**施放时一次性扣掉**的。这意味着落雷阵的蓝耗效率完全由「它在 5 秒里能打到多少目标 × 多少次」决定，是全表方差最大的一个法术。

**（5）ECS 不做取整**。Mono 侧 `MakeDamage` 对 DPS 伤害做了 `Mathf.Ceil`（`Spell1018ThunderAura.cs` L214），ECS 侧 `NewInfo` 是纯浮点乘法，没有 Ceil。低伤害档位下两条轨会有几点差异。

**（6）坠落态是例外**。`Spell1018FallJob.TryAttack` 显式覆写 `damage.damage = config.Damage.Calculate()`（系统 L772），**绕过了 `×0.25`**。这是坠落落雷阵爆发极高的一半原因，另一半见 5.3。

### 2.4 每级性价比

以下按「5 秒共约 20 次结算」计算（首次结算在 0.25 s，施放瞬间不造成伤害；结算间隔均值恰为 0.25 s，见 3.2）。

| Lv | 每 tick 每目标 | 命中上限 | 单目标 5s 总伤 | 满命中 5s 总伤 | 蓝耗 | 单目标伤害/蓝 | 满命中伤害/蓝 | 半径 / 面积 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 22×0.25 = **5.5** | 3 | 20×5.5×1.00 = **110** | 20×3×5.5 = **330** | 33 | 3.33 | 10.0 | 2.8 m / 24.6 m² |
| 2 | 44×0.25 = **11** | 5 | 20×11×1.30 = **286** | 20×5×11 = **1100** | 43 | 6.65 | 25.6 | 3.0 m / 28.3 m² |
| 3 | 88×0.25 = **22** | 7 | 20×22×1.60 = **704** | 20×7×22 = **3080** | 56 | 12.57 | 55.0 | 3.2 m / 32.2 m² |

两个要点：

1. **升级同时抬「单发伤害」「命中上限」「单目标加伤」三个乘数**，所以满命中总伤 Lv1→Lv3 涨 **9.3 倍**，而蓝耗只涨 1.7 倍。这是三级主动法术里升级收益最陡的一档，比蝴蝶（5.67 倍）还高。
2. **单目标与满命中的差距是 3~4.4 倍**，而 `float1` 的单目标补偿只有 +0%/+30%/+60%，远不足以补平。落雷阵是一个**强烈偏向多目标**的法术，打 Boss 时它的伤害/蓝比只有 3.33~12.57，在全表属于下游。

---

## 3. 核心机制：光环结算循环

### 3.1 一次结算的完整流程

整个光环逻辑只有一个函数：

```157:237:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
		private void Execute(SpellAspect spell, [ChunkIndexInQuery] int chunkIndex, ref Spell1018ThunderAuraData spellData)
		{
			ref SpellConfigComponentData valueRW = ref spell.Config.ValueRW;
			valueRW.DamageTimer += DeltaTime;
			if (!(valueRW.DamageTimer >= valueRW.DamageInterval))
			{
				return;
			}
			NativeList<Entity> entities = new NativeList<Entity>(Allocator.Temp);
			ref readonly float3 position = ref spell.Transform.ValueRO.Position;
			float radius = spell.Config.ValueRO.Radius.Calculate();
			SpellTools.GetAttackableEntitiesInRange(in position, in radius, in spell.Config.ValueRO.ShooterType, containsBrittleness: false, in UnitPropertyLookup, in SpellConfigLookup, in PhysicsWorld, ref entities);
			SpellTools.RemoveCannotAttackSpell(ref entities, spell.Config.ValueRO.ShooterType, in SpellConfigLookup);
			RemoveSameCampUnitAndNotAttackUnit(spell.Config.ValueRO.ShooterType, ref entities);
			if (entities.Length < valueRW.Int1)
			{
				GetLightningLineAttackTargets(ref entities, spell);
			}
			int num = math.min(valueRW.Int1, entities.Length);
```

五个要点：

1. **查询无任何生命周期门槛**。Job 的查询只有 `Spell1018ThunderAuraData` + `SpellAspect` + `WithNone<SpellFallTag>`（系统 L25、L57–61），**不看 `DurationTimer`、不看 `HoverTimer`**。这是 7.4「悬停给落雷阵白送伤害」的根因。
2. **`DamageTimer` 存在 `SpellConfigComponentData` 上**，不是 1018 私有组件。1018 自己的组件 `Spell1018ThunderAuraData` 只有三个字段，全部服务于坠落态：

```1:11:decompiled/Assembly-CSharp/Spell1018ThunderAuraData.cs
using Unity.Entities;
using Unity.Mathematics;

public struct Spell1018ThunderAuraData : IComponentData, IQueryTypeParameter
{
	public float FallDelayTimer;

	public float3 FallPosition;

	public float OriginalSpeed;
}
```

3. **每帧只结算一次**。`DamageTimer` 每帧减掉一个间隔，不做 while 循环。所以当 `dt > 0.25`（低于 4 FPS）时会漏结算——Mono 侧对此有 `timeScale` 补偿（10 节），ECS 侧**没有**。
4. **`Radius.Calculate()` 每 tick 现算**，所以任何在运行期改半径的东西都会立刻生效。
5. **候选列表每 tick 重建**，没有「已命中」记忆。所以同一个敌人只要一直站在圈里就会一直被打，也没有穿透消耗（`MakeDamageInfo(costPenetrate: false)`，系统 L189）——**穿透符文对落雷阵完全无效**。

### 3.2 抖动间隔：均值不变，节奏随机

```252:256:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
		private float GetRandomDamageInterval(SpellAspect spell)
		{
			float num = spell.Config.ValueRO.DamageInterval * spell.Config.ValueRO.Float2;
			return Random.random.NextFloat(0f - num, num);
		}
```

```227:227:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
			valueRW.DamageTimer -= valueRW.DamageInterval + GetRandomDamageInterval(spell);
```

抖动量 `= 0.25 × 0.6 = 0.15`，每次扣减 `0.25 + U(−0.15, +0.15)`，即 **`[0.10, 0.40]` 均匀分布**。

关键性质：**`E[扣减] = 0.25` 恰好等于触发阈值，所以长期平均结算频率精确是 4 次/秒，抖动只改节奏不改 DPS。** 5 秒窗口内的结算次数是随机变量，均值 20、标准差约 `√20 × (0.0866/0.25) ≈ 1.55`（`U(−0.15,0.15)` 的标准差 0.0866）。也就是说单次施放的实际总伤有大约 ±8% 的一西格玛波动。

`float2` 存在的意义是表现层：多个落雷阵同时在场时打散雷击的节拍，避免整齐划一。它**不是伤害系数**。

### 3.3 目标获取与阵营过滤

三道过滤串行执行：

1. `SpellTools.GetAttackableEntitiesInRange`（`SpellTools.cs` L2862–2888）：先 `DTool.GetEnemyEntityInRange` 按碰撞层捞异阵营单位，再额外 `OverlapSphere` 一遍把**范围内所有带 `SpellConfigComponentData` 的法术实体**也塞进候选。`containsBrittleness: false`。
2. `SpellTools.RemoveCannotAttackSpell`（`SpellTools.cs` L2967–2981）：法术实体里只保留**异阵营的滚球和蝴蝶**，其余法术实体全部剔除。
3. `RemoveSameCampUnitAndNotAttackUnit`（系统 L295–309）：再剔一遍 `NotAttack` / `Brittleness` / 同阵营。

```295:309:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
		private void RemoveSameCampUnitAndNotAttackUnit(UnitType selfCamp, ref NativeList<Entity> entities)
		{
			for (int i = 0; i < entities.Length; i++)
			{
				if (UnitPropertyLookup.TryGetComponent(entities[i], out var componentData))
				{
					UnitType unitType = componentData.unitCfg.unitType;
					if (unitType == UnitType.NotAttack || unitType == UnitType.Brittleness || DTool.IsSameCamp(componentData.unitCfg.unitType, selfCamp))
					{
						entities.RemoveAt(i);
						i--;
					}
				}
			}
		}
```

净结果：

- **落雷阵会打碎敌方的蝴蝶、扣敌方滚球的血**（这也是《蝴蝶逆向分析.md》5.2 节点名的那条 Mono 守卫在 ECS 侧的对应实现）。
- **落雷阵不碰己方任何东西**：己方单位、己方蝴蝶、己方滚球都过不了阵营门。
- **可破坏物（`Brittleness`）被排除两次**，落雷阵**不能打碎木桶/罐子**。这是它和大多数 AoE 法术的一个明显区别，容易被误认为是 bug。
- 第 3 道过滤只对**有 `UnitProperty_Dots` 的实体**生效（`TryGetComponent` 失败就跳过），所以第 2 道漏下来的异阵营蝴蝶/滚球能安全留在列表里。

### 3.4 「传导」的真相：它是补位，不是链式

面板机制文案写的是「落雷可传导 int1 次」，但代码里没有任何链式行为：

```258:293:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
		private void GetLightningLineAttackTargets(ref NativeList<Entity> startTargets, SpellAspect spell)
		{
			if (startTargets.Length == 0)
			{
				return;
			}
			int num = 0;
			while (startTargets.Length < spell.Config.ValueRO.Int1)
			{
				num++;
				if (num >= spell.Config.ValueRO.Int1)
				{
					break;
				}
				NativeList<Entity> entities = new NativeList<Entity>(Allocator.Temp);
				ref readonly float3 position = ref spell.Transform.ValueRO.Position;
				float radius = spell.Config.ValueRO.Radius.Calculate() * 1.5f;
				SpellTools.GetAttackableEntitiesInRange(in position, in radius, in spell.Config.ValueRO.ShooterType, containsBrittleness: false, in UnitPropertyLookup, in SpellConfigLookup, in PhysicsWorld, ref entities);
```

四条结论：

1. **搜索中心是光环自己**（`spell.Transform.ValueRO.Position`），不是上一个目标。Mono 侧才是真链式（从 `list[list.Count-1].transform.position` 出发，`Spell1018ThunderAura.cs` L167–169）。**ECS 重写时把链式改成了同心圆扩大**，这是两条轨最大的行为差异。
2. **只在人数不够时才触发**。`if (entities.Length < Int1)`——半径内已经有 `int1` 个敌人时，一次传导都不会发生，1.5 倍环带里的敌人**完全打不到**。
3. **需要种子**。`startTargets.Length == 0` 直接 return——半径内一个敌人都没有时，哪怕 1.5 倍环带里站满了人也不会打。「区域外的敌人也可能会被牵连」这句风味文案是准确的：**只是「可能」，且必须先有人踩进圈里**。
4. **补位是随机挑一个**（`entities[Random.random.NextInt(0, entities.Length)]`，系统 L287），不是挑最近的。而且**每轮都重新做一次 `OverlapSphere`**，最坏情况一次结算要做 `int1` 次物理查询（Lv3 = 7 次/tick × 4 tick/s = 28 次/秒/光环）。

**`int1` 的真实语义是「单次结算的命中数上限」。** 文案「传导 3 次」按中文直觉是「1 个主目标 + 3 次传导 = 4 个目标」，但代码里 `num = math.min(Int1, entities.Length)`，Lv1 最多只打 **3** 个。这是一处文案与实现的口径差（差 1）。

由此得出一条反直觉的构筑结论：**当场上敌人数量已经 ≥ `int1` 时，扩大半径的收益是 0**（见 7.12）。半径的价值只在两处：凑满 `int1` 的概率，和敌人走出圈外的难度。

### 3.5 单目标加伤

```191:194:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
					if (num == 1)
					{
						damage.damage *= valueRW.Float1 / 100f;
					}
```

判定的是**本次结算的最终命中数 `num`**，不是「场上敌人数」。三个衍生结论：

- 补位算进 `num`。半径内 1 个 + 环带补 1 个 = `num = 2`，**加伤消失**。所以在 Boss 战里如果有小怪飘进 1.5 倍环带，你的单体伤害会突然掉 23%（Lv3：从 ×1.6 掉到 ×1.0，即 −37.5%）。
- 加伤作用在**已经乘过 0.25 的值**上（`MakeDamageInfo` 在前，L189），所以是 `damage × 0.25 × float1/100`，不是 `damage × float1/100 × 0.25`——数值上等价，但顺序决定了它和 `Damage.MulRatio` 的关系：`float1` 是最后一道乘算，**不进 `AttributeValue`，任何符文都改不了它**。
- Lv1 的 `float1 = 100` 让这条机制在一级完全不存在。**Lv1→Lv2 的实际单目标提升是 `44×1.3 / 22×1.0 = 2.6 倍`**，比 damage 字段显示的 2 倍更高。

### 3.6 System 执行顺序

| System / Job | 所属组 | 频率 |
| --- | --- | --- |
| `Spell1018ThunderAuraSystem.OnUpdate` 前半（初始化、地面贴花） | `SpacialSpellSystemGroup` ⊂ `SpellSimulationSystemGroup` | 每渲染帧 1 次 |
| `Spell1018Job`（光环结算） | 同上，`ScheduleParallel` 后立即 `Complete()` | 每渲染帧 1 次 |
| `Spell1018FallJob`（坠落打击） | 同上，紧随其后 | 每渲染帧 1 次 |
| `SpellSystem`（`UpdateDuration` + 触发伤害） | `SpellSimulationSystemGroup` | 每渲染帧 1 次 |
| `SpellMoveJob` | `SpellPhysicsSystemGroup` ⊂ `FixedStepSimulationSystemGroup` | 固定步长 |
| `SpellDestroyEventJob`（雷霆晶石落雷） | `UnitTakeDamageGroup` | 每渲染帧 1 次 |

`SpellSimulationSystemGroup` 标了 `UpdateAfter(VariableRateSimulationSystemGroup)` + `UpdateBefore(UnitTakeDamageGroup)`。两个 Job 都用 `state.WorldUnmanaged.Time.DeltaTime`（可变帧时长），并且系统里对两个 Job 都紧跟 `state.Dependency.Complete()`（系统 L1416、L1437）——**这是一个同步点，落雷阵的并行度实际被打断了**。配合 3.4 的多次 `OverlapSphere`，场上叠很多落雷阵时它是性能热点。

### 3.7 表现层

- **地面贴花**：非坠落的落雷阵首帧通过 `SpellTools.SpawnChild(1018, "OnGround", 颜色)` 挂一个子实体，写进 `SpellComponentData.TrailEffectEntity`（系统 L1392–1400）。
- **雷链**：每次命中都实例化一个 `1018_Chain_{颜色}` 实体，`Spell1018ChainData` 记两端坐标，`duration = 0.3`。`Spell1018ChainSystem` 只做拉伸/旋转/倒计时销毁（`Spell1018ChainSystem.cs` L134–158），**完全不造成伤害**。第一个目标的链画成从上方 15 米垂直劈下（`+ new float3(0f, 0f, -15f)`，系统 L211），后续目标的链画成从上一个目标连过来——**视觉上像链式，实际每一下都是光环直接结算**。
- **命中粒子**：`1018_ChainHit_{颜色}` 全局粒子，每个目标一发（系统 L240–250）。
- **音效与震屏**：一次结算只放一次 `1018_Hit` 音效（系统 L176–182）和一次 `ScreenShakeData { Radius=0.025, Speed=1, Time=0.08 }`（系统 L228–236），**与命中数无关**。

---

## 4. 生命周期与移动分派

### 4.1 落雷阵不在任何 `SpellMoveJob` 分支里

全工程检索 `SpellAbilityType.ThunderAura` 只有 7 处引用，`SpellMoveJob.cs` **一处都没有**。落雷阵走 `switch (movement.Type)` 的 `Normal` → `Normal_Common`，即 `velocity.Linear = Direction × Speed`。`Speed = 0` 时速度矢量为零，光环钉在枪口。

生成位置来自通用管线，`Normal` 类型直接取 `SpawnPosition`：

```407:412:decompiled/Assembly-CSharp/SpellShootSystem.cs
		case SpellSpecialMovementType.Normal:
		case SpellSpecialMovementType.ChaseEnemy:
		case SpellSpecialMovementType.ChaseMouse:
		case SpellSpecialMovementType.ChaseOwner:
			valueRW2.Position = spawnParams.SpawnPosition;
			break;
```

**所以「划定一片区域」这句风味文案有点言过其实**：玩家不能选点，落雷阵只能落在法杖枪口。要让它落到别处，只能靠 8 节那几张改移动类型的符文。

### 4.2 `speed = 0` 是 `Base`，不是硬约束

```24:27:decompiled/Assembly-CSharp/AttributeValue.cs
	public readonly float Calculate()
	{
		return (Base + AddBase) * (1f + AddRatio) * MulRatio + Extra;
	}
```

`Speed.Base = 0`，但 `AddBase = (公转 extraSpeed) + sip.bounsSpeed`（`ShootSpellUtils.cs` L201），而 `bounsSpeed` 收的是 `GetSpellFlySpeed().CurrentAddBase`，即**加速器和寻踪的 `float1`**（`SpellShootData.cs` L86–95、`SpellInitialParameter.cs` L307）。

**结论：加速器把落雷阵的速度从 0 直接抬成 `float1`（3 / 6 / 12 m/s），光环会以那个速度沿施法方向匀速飘走。** 这不是加成，是把「区域控制」改成了「移动的雷云」，见 7.6。

### 4.3 存活与销毁

落雷阵不在 `UpdateDuration` 的例外名单里（例外只有 `Laser` / `RuneHammer` / `LaserBeam` / `BiAnLethalBlade` / `Umbrella` / `HighPressureWasher` / `DaveHarpoons`），走完整的通用路径：

```275:307:decompiled/Assembly-CSharp/SpellSystem.cs
			if (!spell.Movement.ValueRO.IsFallSpell)
			{
				SpellAbilityType abilityType = spell.Config.ValueRO.AbilityType;
				if (abilityType != SpellAbilityType.Laser && abilityType != SpellAbilityType.RuneHammer && abilityType != SpellAbilityType.LaserBeam && abilityType != SpellAbilityType.BiAnLethalBlade && abilityType != SpellAbilityType.Umbrella && abilityType != SpellAbilityType.HighPressureWasher && abilityType != SpellAbilityType.DaveHarpoons)
				{
					if (valueRW.DurationTimer < valueRW.Duration.Calculate())
					{
						valueRW.DurationTimer += DeltaTime;
						return;
					}
					spell.Movement.ValueRW.Speed = 0f;
					if (valueRW.HoverTimer < valueRW.HoverDuration)
					{
						valueRW.HoverTimer += DeltaTime;
					}
```

三个衍生结论：

1. **`duration` 用尽后先把 `Speed` 压 0**——所以「加速器让光环飘走」这件事在 5 秒末会自动停住，而不是继续飘。
2. **悬停期实体还活着，而 `Spell1018Job` 不看悬停状态**，光环照常每 0.25 秒结算。见 7.4。
3. **坠落态完全绕开这一段**（`!IsFallSpell` 的 else 分支只是 `DurationTimer += DeltaTime`，L310），**坠落落雷阵没有寿命上限**，只靠 `ReboundCount` 耗尽销毁。见 5.4。

销毁走标准 `SpellDestroyTag` 路径，因此**分裂、结束触发器、雷霆晶石落雷全部照常触发**。`SpellDestroySystem` 对 1018 有一条专属修正，只是把分裂子弹的重力清零：

```568:571:decompiled/Assembly-CSharp/SpellDestroySystem.cs
			case SpellAbilityType.ThunderAura:
				elem.MovementComponentData.Gravity = 0f;
				elem.MovementComponentData.CurrentFallSpeed = 0f;
				break;
```

### 4.4 接触伤害

`SpellSystem.Execute` 的碰撞触发伤害分支要求实体**没有** `SpellDisableTriggerDamageTag`（`SpellSystem.cs` L188）。这个标记由预制体上的 `SpellAuthoring.DisableTriggerDamage` 勾选项决定（`SpellAuthoring.cs` L51–53），**反编译代码里读不到预制体数据**。

Mono 侧 `Spell1018ThunderAura` 把 `TriggerIn` 覆写成空函数（L407–409），明确关掉了接触伤害。据此**推测** ECS 预制体也勾了 `DisableTriggerDamage`，但本文不下定论，列入第 14 节待决。

### 4.5 AI 攻击距离

落雷阵是 `SpellGroupAttackDistanceCalculator` 里少数几个用 `max` 的分支：

```283:285:decompiled/Assembly-CSharp/SpellGroupAttackDistanceCalculator.cs
				case SpellAbilityType.ThunderAura:
					num2 = Mathf.Max(spellEffectRadius_FinalPlayerValue.Result, spellMoveSpeed_FinalPlayerValue.Result * spellDuration_FinalPlayerValue.Result);
					break;
```

默认 `max(2.8, 0 × 5) = 2.8`。插加速器后 `max(2.8, 3×5=15) = 15`——这个式子已经预料到了 4.2 那条「速度可以非 0」的情况，说明**加速器让落雷阵移动是设计内行为，不是漏网**。

---

## 5. 坠落态：换了一个法术

插上「坠落」符文后，`sip.spellIsFall = fallExplosionRadius > 0`（`SpellInitialParameter.cs` L640）为真，实体被加上 `SpellFallTag`（`SpellShootSystem.cs` L330–333），于是**整个第 3 节的光环逻辑被 `[WithNone(SpellFallTag)]` 完全排除**，改走 `Spell1018FallJob`。

### 5.1 它不再是光环，是一道会跳的雷

```638:691:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
		private void Execute(SpellAspect spell, [ChunkIndexInQuery] int chunkIndex, ref Spell1018ThunderAuraData data)
		{
			ref SpellConfigComponentData valueRW = ref spell.Config.ValueRW;
			if (spell.Movement.ValueRO.ReboundCount >= 0)
			{
				if (data.FallDelayTimer > 0.1f)
				{
```

行为：

- 首帧把 `FallPosition` 设成 `movement.FallTargetPosition`（即准星瞄点，系统 L1389），并把 `Speed` / `CurrentFallSpeed` / `OriginalSpellHorizontalSpeed` 全部清零（系统 L1376–1378）。
- 每积满 **0.1 秒**打一次（`FallDelayTimer > 0.1f`）。
- 每次打击：在 `FallPosition` 处 `OverlapSphere(Radius.Calculate())`，同三道阵营过滤，命中上限仍是 `int1`。
- 打完把实体传送到打击点，算出下一个落点，`ReboundCount--`。
- `ReboundCount < 0` 时置 `SpellDestroyTag`。

**没有 5 秒 duration，没有 0.25 秒 tick，没有 1.5 倍传导环带**——`GetLightningLineAttackTargets` 在坠落 Job 里根本没有对应实现。坠落落雷阵和光环落雷阵除了共用 `int1` / `float1` / `radius` 之外几乎没有共同点。

### 5.2 `TryAttack` 的 N² 双循环

这是本文最重要的一条发现：

```764:780:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
		private void TryAttack(SpellAspect spell, int chunkIndex, int totalCount, NativeList<Entity> hitList, SpellConfigComponentData config)
		{
			for (int i = 0; i < totalCount; i++)
			{
				for (int j = 0; j < totalCount; j++)
				{
					Entity target = hitList[j];
					TakeDamageInfo_Dots damage = spell.MakeDamageInfo(costPenetrate: false);
					damage.damage = config.Damage.Calculate();
					if (totalCount == 1)
					{
						damage.damage *= config.Float1 / 100f;
					}
					damage.spell.HitPosition = TransformLookup[target].Position;
					CMD.TryAttackEntity(chunkIndex, in target, in damage, in UnitPropertiesLookup, in SpellConfigLookup);
```

外层 `i` **只被用在 `if (j != i)` 那个「是否画成首段雷链」的表现判断上**（系统 L785），伤害循环完全不需要它。净效果：

- 一次打击总共发出 `totalCount²` 次 `TryAttackEntity`
- **每个目标各挨 `totalCount` 下**

`TryAttackEntity` 只是往目标的 `DynamicBuffer<TakeDamageInfo_Dots>` 里 append（`SpellTools.cs` L2743、L2756），`TakeDamageInfoPreProcessSystem` 逐条遍历整个 buffer（L171），**没有任何同帧去重或命中冷却**。所以这 `totalCount²` 下全部结算。

对照 Mono 版 `OnFallingLightingOnGround`（L258–271）：它对每个目标各调一次 `LightningAttack`，是标准的 O(N)。**ECS 重写引入的这个平方项几乎可以确定是 bug**，本文按「实测行为」记录，不假设它会被修。

### 5.3 坠落打的是满额伤害

`damage.damage = config.Damage.Calculate()` 覆盖掉了 `MakeDamageInfo` 算好的 `×0.25`。合起来：

**坠落态单目标伤害 = `damage × totalCount`**（`totalCount == 1` 时再 `× float1/100`）

| Lv3 落雷阵 + 坠落 | 场上敌人数 | totalCount | 总结算次数 | 每目标伤害 | 单次打击总伤 |
| --- | --- | --- | --- | --- | --- |
| — | 1 | 1 | 1 | 88 × 1.6 = **140.8** | 140.8 |
| — | 2 | 2 | 4 | 88 × 2 = **176** | 352 |
| — | 4 | 4 | 16 | 88 × 4 = **352** | 1408 |
| — | ≥7 | 7 | 49 | 88 × 7 = **616** | **4312** |

作为对照，光环形态 Lv3 满命中打满 5 秒才 3080。**一次坠落打击在 7 人场上的爆发就超过了整个光环形态的全部输出，而且是瞬发的。**

代价：失去 5 秒的持续压制、失去传导环带、失去时长强化和悬停的加成（都不生效，见 7.3 / 7.4 / 7.8）。

### 5.4 反弹与折射：坠落态的续航

`ReboundCount` 来自反弹符文（`SpellShootData.cs` L579–594，`int1` 求和 = 2/4/8）。不插反弹时 `ReboundCount = 0`，`0 >= 0` 成立打一次，然后减到 −1，下一帧销毁——**基础坠落落雷阵只打 1 下**。

下一个落点由 `CalcFallTargetPosition` 按移动类型算（系统 L841–888），`Normal` 时是 `当前位置 + Direction × Radius.Calculate()`，即**每跳前进一个半径**。

折射走另一条路，**不消耗 `ReboundCount`**：

```665:670:decompiled/Assembly-CSharp/Spell1018ThunderAuraSystem.cs
					if (!TryGetRefractionPos(ref entities, fallPosition2, spell.Entity, spell.Config.ValueRO.ShooterType, out var refractionPos))
					{
						CalcFallTargetPosition(ref spell.Movement.ValueRW, spell.Config.ValueRO, spell.Data.ValueRO, data.FallPosition, data.OriginalSpeed, out refractionPos, Random);
						spell.Movement.ValueRW.ReboundCount--;
					}
```

折射成功时下一跳直接落到 `FindReflectionTarget` 找到的敌人身上，而且 `ReboundCount` 不减。**折射符文在坠落落雷阵上是纯增量的自动索敌跳跃**（代价是全局 −10%~−20% 伤害，见 8.4）。

打击间隔恒定 0.1 秒，所以坠落落雷阵的总时长 ≈ `(反弹次数 + 折射次数 + 1) × 0.1 秒`。反弹 Lv3 = 9 跳 = 0.9 秒。

### 5.5 半径也变了

`Radius.Calculate() = (Base + FallRadius) × (1 + AddRatio) × MulRatio + Extra`（`RadiusAttributeValue.cs` L24–27），而 `FallRadius = sip.fallExplosionRadius` 就是坠落符文 `float1` 的求和（`SpellShootData.cs` L596–602，值为 1.0 / 1.5 / 2.3）。

| 落雷阵 Lv3（base 3.2） | 坠落 Lv1 | 坠落 Lv2 | 坠落 Lv3 |
| --- | --- | --- | --- |
| 打击半径 | 4.2 m | 4.7 m | **5.5 m** |
| 每跳步长（= 半径） | 4.2 m | 4.7 m | 5.5 m |

坠落符文对落雷阵是**半径 + 步长 + 触发形态**三合一的改动，是全表对它影响最大的一张符文。

---

## 6. 阵营、可攻击性与特例

### 6.1 落雷阵不会伤害己方

三道过滤（3.3 节）里有两道显式查 `IsSameCamp`。玩家的落雷阵不会打玩家、队友、己方召唤物、己方蝴蝶、己方滚球。友伤（`undifferDamage`）路径也不经过这里。

### 6.2 落雷阵可以被当成攻击目标吗

`GetAttackableEntitiesInRange` 会把范围内所有带 `SpellConfigComponentData` 的实体塞进候选，但 `RemoveCannotAttackSpell` 只保留 `Rollball` 和 `Butterfly`（`SpellTools.cs` L2967–2981）。落雷阵的 `abilityType` 不在名单里，所以：

**敌方的任何范围法术都不会「打掉」你的落雷阵。** 它没有生命值，也不进任何脆弱物判定，5 秒内不可摧毁。

### 6.3 可破坏物打不到

`Brittleness` 在 `GetAttackableEntitiesInRange` 里被 `containsBrittleness: false` 挡一次，又在 `RemoveSameCampUnitAndNotAttackUnit` 里被显式剔一次（系统 L302）。**落雷阵不能开箱、不能砸罐子。**

Mono 侧则相反：`GetAroundTargets` 用的是 tag 集合 `"Monster", "RollBall", "Butterfly"`（L140），也不含可破坏物 tag，两轨在这一点上一致。

### 6.4 Boss6 二阶段对 1018 有专属免伤冷却

```2063:2068:decompiled/Assembly-CSharp/Boss6_Stage2.cs
			case SpellAbilityType.DisintegrationRay:
			case SpellAbilityType.ThunderAura:
			case SpellAbilityType.DragonBreath:
				hitList.Add(entity);
				hitTime.Add(normalSpellClearTime);
				break;
```

被落雷阵命中一次后，该法术实体进入 `hitList`，在 `normalSpellClearTime` 内的后续命中一律 `info.immuneDamage = true`（L2109）。**5.2 的 N² 多段和 3.1 的 4 次/秒频率在这个 Boss 身上都会被节流。** `normalSpellClearTime` 是预制体序列化字段，反编译读不到具体值，列入第 14 节待决。

---

## 7. 增强符文 × 落雷阵 逐一校验矩阵

对已逆向的 21 张增强符文逐条核对「原文档结论」与「落雷阵实际走到的代码分支」。

| # | 符文 | abilityType | 三级关键值 | 对落雷阵的实际效果 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 1 | 坠落 | 3119 Fall | `float1` 1.0/1.5/2.3 | **换掉整个法术**：光环→单次跳跃打击、满额伤害、N² 多段、半径 +float1 | **需回写**（7.1） |
| 2 | 范围增强 | 3107 EnhanceRadiusRatio | `float1` 30/60/120 | 半径 `AddRatio`，面积平方增长；但收益在 `int1` 上限处截断 | **需回写**（7.12） |
| 3 | 加速器 | 3106 EnhanceSpeedValue | `float1` 3/6/12 | **把静止光环变成移动雷云**（`Speed.Base=0 + AddBase`） | **需回写**（7.6） |
| 4 | 悬停 | 3008 SpellHover | `float1` 2/3/5 | **白送 2/3/5 秒满速结算**（结算 Job 不看悬停状态） | **需回写**（7.4） |
| 5 | 节能模式 | 3104 PowerSavingMode | dmg×0.75/0.75/0.80，dur×0.75/0.75/0.80，mp×0.6/0.5/0.4 | **双重打折**：伤害和时长同时被砍，总伤 ×0.5625 | **需回写**（7.9） |
| 6 | 反弹 | 3012 Rebound | `int1` 2/4/8，`float1` 1.0 | 光环态**完全无效**（静止不撞墙，`ReboundAddTime` 也吃不到）；坠落态 = 跳跃次数 | **需回写**（7.7） |
| 7 | 时长强化 | 3103 EnhanceDurationValue | `float1` 2/3.5/6 | 光环态线性加伤（+40%/+70%/+120%）；**坠落态完全无效** | **需回写**（7.3） |
| 8 | 分裂 | 3013 SpellSplit | `int1` 3/5/8，DamageRatio 0.33 | 销毁时在半径处环形铺 N 个 80% 半径的子光环，各跑满 5 秒 | **需回写**（7.5） |
| 9 | 自动导航 | 3011 FollowTarget | `float1` 12/22/38 | 只把 `Type` 改成 `ChaseEnemy`；`Speed=0` 时 `Normal_ChaseTarget` 的 `maxDelta=0`，**光环仍不动** | **需回写**（7.10） |
| 10 | 闪电链 | 3007 LightningChain | `int1` 12/24/48 | 链挂在**法术实体之间**；落雷阵静止 5 秒 → **一条固定的持续伤害线段** | **需回写**（7.11） |
| 11 | 寒霜晶石 | 3014 Frozen | `float1` 1.5/3/6 | 每 tick 都尝试上冻，但 `FronzenState != Normal` + 冻结后 2 秒免疫 → 5 秒内只能生效 1~2 次 | 一致（7.13） |
| 12 | 毒液晶石 | 3005 VenomCrystal | `int1` 80/180/380，`int2` 1/2/4 | 每 tick 每目标各叠一次，5 秒 × 4 次/秒 × `int2` = **单目标 80/160/320 层** | **需回写**（7.14） |
| 13 | 雷霆晶石 | 3101 ThunderCrystal | `float1` 2.2/2.4/2.8（半径）、`float2` 200/270/320（伤害%）、`float3` **15/20/30（染色概率%）** | 销毁时落 1 次，且需先过颜色骰；落雷打**满额**伤害（不乘 0.25）且**能打可破坏物** | **需回写**（7.15） |
| 14 | 伤害强化 | 3102 EnhanceAttackRatio | `float1` 25/50/100 | `Damage.AddRatio`，乘在每 tick 上，光环/坠落通吃 | 一致 |
| 15 | 精准施法 | 3105 EnhanceCriticalChance | `angle` −30/−60/−100，`criticalChance` 10/20/40 | `angle` 部分**完全无效**（落雷阵 `angle=0`、`shootCount=1`，`Scatter` 不被读取）；只剩暴击率 | **需回写**（7.16） |
| 16 | 齐射 | 3001 Volley | `int1` 2/3/4，`int2` 10/15/20，`float1` 78/73/68 | 容量与蓝耗折扣有效；`int2` 的散射恶化**对落雷阵无效**（同上） | **需回写**（7.16） |
| 17 | 粘液晶石 | 3004 MucusCrystal | `float1` 3/4/5，`float2` 60/45/30，`float3` 70/50/30 | 每 tick 刷新减速；4 次/秒的刷新率让 debuff 几乎永不掉 | 一致 |
| 18 | 奥术屏障 | 4012 Umbrella | `int1` 3，`int2` 400 | 独立法术，与落雷阵无交互 | 一致 |
| 19 | 蓄力模式 | 4004 ChargeMode | `int1` 6/12/24，mp×0.9/0.8/0.7 | `IsChargingSpell()` 只认 1026/1027，落雷阵不攒星 | 一致 |
| 20 | 强制冷却 | 4006 ForceCoolDown | `coolDownRatio` 0.6/0.3/0.15 | 法杖层，不进发射包 | 一致 |
| 21 | 储魔之匣 | 4002 EmptyContainer | `float1` 40/80/160 | 法杖层，不进发射包 | 一致 |

以下展开非平凡项。

### 7.1 坠落（3119）：把光环换成瞬发爆发

第 5 节已完整展开。要点重述：`spellIsFall` 由坠落符文独家决定（`SpellInitialParameter.cs` L640），`SpellFallTag` 让 `Spell1018Job` 的 `[WithNone]` 直接把实体排除出光环查询。得到的是一个**没有 duration、每 0.1 秒跳一次、满额伤害、每目标挨 `totalCount` 下**的东西。

《坠落逆向分析.md》如果有「按 abilityType 的坠落行为表」，1018 应当单独成行，且不能只写「半径 +float1」。

### 7.2 半径的双重身份

`radius` 在落雷阵身上同时是四样东西：**结算半径**、**传导环带底数（×1.5）**、**坠落每跳步长**、**分裂子弹的生成环半径**（`SpellDestroySystem.cs` L533–535）。任何改半径的东西都会四路同时生效，这在全表是独一份。

### 7.3 时长强化（3103）：光环态线性，坠落态归零

`GetSpellDuration` 对 `EnhanceDurationValue` 做 `AddBase(float1)`（`SpellShootData.cs` L135–137）→ `sip.extraDuration` → `Duration.AddBase`。

| 槽位 | duration | 结算次数 | 满命中总伤（Lv3） | 相对默认 |
| --- | --- | --- | --- | --- |
| `[落雷阵Lv3]` | 5.0 | 20 | 3080 | — |
| `[时Lv1(+2)] [落雷阵Lv3]` | 7.0 | 28 | 4312 | **+40%** |
| `[时Lv2(+3.5)] [落雷阵Lv3]` | 8.5 | 34 | 5236 | **+70%** |
| `[时Lv3(+6)] [落雷阵Lv3]` | 11.0 | 44 | 6776 | **+120%** |

因为落雷阵没有「减速段」「飞行段」这类固定开销，**时长的收益是严格线性的**——这和蝴蝶（有 0.38 秒固定减速段，边际收益偏高）以及一次性飞弹（时长只延长命中窗口）都不同。落雷阵是全表时长强化收益最干净的载体。

**但坠落态完全吃不到**：`UpdateDuration` 的 `!IsFallSpell` 守卫（`SpellSystem.cs` L275）让坠落落雷阵根本不跑 duration，寿命由 `ReboundCount` 决定。`[坠落] [时长强化] [落雷阵]` 里那张时长强化是一个空槽位。

### 7.4 悬停（3008）：白送的第二份时长

这是本文第二重要的发现。

悬停把 `sip.SpellHoverDuration` 写成各张符文 `float1` 之和（`SpellShootData.cs` L398–411），进 `SpellConfigComponentData.HoverDuration`。`UpdateDuration` 在 `duration` 用尽后先把 `Speed` 压 0，再累加 `HoverTimer` 直到 `HoverDuration`，期间**不置 `SpellDestroyTag`**。

而 `Spell1018Job` 的查询里**没有任何生命周期条件**（系统 L57–61），`Execute` 里也**没有读 `DurationTimer` 或 `HoverTimer`**。所以：

**悬停期的落雷阵以完全相同的频率、完全相同的伤害继续结算。**

| 槽位 | 有效结算时长 | 结算次数 | 满命中总伤（Lv3） |
| --- | --- | --- | --- |
| `[落雷阵Lv3]` | 5.0 | 20 | 3080 |
| `[悬Lv1(+2)] [落雷阵Lv3]` | 7.0 | 28 | **4312** |
| `[悬Lv2(+3)] [落雷阵Lv3]` | 8.0 | 32 | **4928** |
| `[悬Lv3(+5)] [落雷阵Lv3]` | 10.0 | 40 | **6160** |
| `[时Lv3] [悬Lv3] [落雷阵Lv3]` | 16.0 | 64 | **9856** |

对绝大多数法术，悬停的效果是「寿命末原地不动」——对飞弹是纯亏（停止推进），对滚球是小赚。**对落雷阵，悬停和时长强化是同一种收益，且两者可以叠加。** 悬停 Lv3（+5 s）几乎等价于时长强化 Lv3（+6 s），而两张一起插直接把有效时长翻到 3.2 倍。

《悬停逆向分析.md》的机制族表如果按「悬停期做什么」分类，落雷阵应该单独一族：**「悬停期继续全速输出」**。

### 7.5 分裂（3013）：把一个光环变成一圈光环

`SplitComponentData.Count = GetSplitCount()`（`int1` 求和 = 3/5/8），`DamageRatio` 是**硬编码 0.33**，不读配置（`ShootSpellUtils.cs` L314–318）。

`ToSplit` 对子弹的改动：

```44:57:decompiled/Assembly-CSharp/SpellSpawnParamsStorage.cs
		ssp.ConfigComponentData.Damage.MulRatio *= splitData.DamageRatio;
		ssp.ConfigComponentData.Radius.Base *= 0.8f;
		ssp.ConfigComponentData.Radius.Extra *= 0.8f;
		ssp.ConfigComponentData.Knockback *= 0.5f;
		if (ssp.MovementComponentData.IsFallSpell)
		{
			ssp.MovementComponentData.FallingReboundForceRatio *= 0.7f;
			ssp.MovementComponentData.IsFallRebounded = true;
			ssp.MovementComponentData.ReboundFallSpeed();
		}
		else
		{
			ssp.MovementComponentData.Speed *= 0.5f;
		}
```

`Speed *= 0.5` 对 `Speed = 0` 无意义。生成位置来自 `SpellDestroySystem.Split`：角度 `随机基角 + 360°×(i/N)`，位置 `母体位置 + Direction × Radius.Calculate()`（L522–539）。

**所以分裂落雷阵 = 母光环消失的瞬间，在它半径圈的边缘均匀铺开 N 个子光环**，每个子光环：

- 半径 `3.2 × 0.8 = 2.56`
- 伤害 `88 × 0.33 = 29.04` → 每 tick 每目标 `7.26`
- **`duration` 满额重开 5 秒，`int1` 不打折仍是 7**
- 蓝耗整体 ×1.5（`mpCostMulDivCorrection = 150`）

Lv3 落雷阵 + 分裂 Lv3（8 个）满命中理论总伤：`8 × 20 × 7 × 7.26 = 8131`，是母体 3080 的 2.6 倍，而且覆盖面积从一个 32 m² 的圆变成半径 3.2 圈上的 8 个 2.56 m 圆（总覆盖直径约 11.5 m）。

**分裂是落雷阵最强的面积扩张手段，而且时长强化/悬停对母体的延长会让分裂延后触发，两者不冲突。**

### 7.6 加速器（3106）：静止光环变移动雷云

`GetSpellFlySpeed` 对 `EnhanceSpeedValue` 做 `AddBase(float1)`：

```86:95:decompiled/Assembly-CSharp/SpellShootData.cs
			switch (finalConfig.abilityType)
			{
			case SpellAbilityType.AroundMouse:
			case SpellAbilityType.EnhanceSpeedValue:
				ratioValue.AddBase(finalConfig.float1);
				break;
			case SpellAbilityType.SpeedToDuration:
				ratioValue.MulRatio(finalConfig.float1 / 100f);
				break;
			}
```

→ `sip.bounsSpeed`（`SpellInitialParameter.cs` L307）→ `Speed.AddBase`（`ShootSpellUtils.cs` L201）→ `Calculate() = (0 + float1) × ... = float1`。

| 槽位 | 光环速度 | 5 秒漂移距离 | 结论 |
| --- | --- | --- | --- |
| `[落雷阵]` | 0 | 0 m | 钉在枪口 |
| `[加Lv1(+3)] [落雷阵]` | 3 m/s | 15 m | 横穿大半个房间 |
| `[加Lv2(+6)] [落雷阵]` | 6 m/s | 30 m | 一般在撞墙前就飞出交战区 |
| `[加Lv3(+12)] [落雷阵]` | 12 m/s | 60 m | 基本等于把光环丢掉 |

**这是降级还是升级取决于打法**：想「把雷云推到怪堆里」时 Lv1 的 3 m/s 勉强可用；但落雷阵的核心价值是区域封锁，让它匀速直线飘走会让它在敌人身上停留的时间大幅缩短。**默认判定：加速器对落雷阵是负收益**，且等级越高越差——这和它对几乎所有其他法术的定位相反。

一个佐证：`SpellGroupAttackDistanceCalculator` 的 1018 分支专门写了 `Mathf.Max(半径, 速度×时长)`（4.5 节），说明策划知道这个组合存在并给 AI 做了适配。

另外 4.3 提到 `duration` 用尽后 `Speed` 被压 0，所以插了悬停时，光环会在飘到终点后**停下来继续输出**——`[加速器] [悬停] [落雷阵]` 是一个「推进到位再驻扎」的组合，比单插加速器合理。

### 7.7 反弹（3012）：光环态是死槽位

`GetReboundTime_FinalPlayerValue` 给出 `(count = int1 求和, reboundAddTime = max(float1))`（`SpellShootData.cs` L579–594）。

- **光环态**：`ReboundCount` 只在 `SpellMoveJob` 的墙体碰撞事件里消耗。落雷阵 `Speed = 0`、位置不变，生成后不会再产生新的碰撞事件，所以 `ReboundCount` 永远不减，`ReboundAddTime` 那 1 秒也永远加不到 `Duration.Extra` 上。**反弹在光环形态下是一张完全无效的符文。**
- **坠落态**：`ReboundCount` 被 `Spell1018FallJob` 挪用成「还能再跳几次」（5.4 节）。反弹 Lv3 把 1 跳变成 9 跳，是坠落落雷阵伤害的主要乘数。

一张符文在同一个法术的两个形态下，一边完全无效、一边是核心，这在全表也很少见。

### 7.8 精准施法（3105）与齐射（3001）：散射对落雷阵没有消费方

落雷阵 `angle = 0`、`shootCount = 1`。但**免疫散射的原因不是「锥角算不出来」**——这一点容易搞错，值得写清楚。

开火锥角走的是 `CalculateRandomAngle`（`ShootSpellUtils.cs` L631–636），`cone = (主动法术.angle + extraScatter) × mul`。齐射的 `int2 × (N−1)` 进的是 `extraScatter`，所以**齐射 Lv1、N=3 时落雷阵确实拿到了 20° 锥角并随机出了一个偏角**。精准施法的 `angle = −30/−60/−100` 加进去为负，才被钳成 0（`ShootSpellUtils.cs` L282 的快照与 L632–635 的锥角各钳一次，落雷阵两处都是 0 进 0 出）。

真正的原因在下游：**偏角只写进 `shootDirection` → `MovementComponentData.Direction`，而落雷阵 `Direction × Speed = 0`，光环结算只读 `Position` 和 `Radius`（系统 L167–168）。方向这个量在光环形态下没有任何消费方。**

（顺带纠正一个常见误解：`SipToSSP` 写的那份 `Config.Scatter` 快照是另一码事，只有激光 `1004`、分解射线 `1011`、破法者 `1021`、龙息 `1025`、彼岸刃 `4019`、鱼叉 `4024` 等自己的 Job 会读它，`Spell1018ThunderAuraSystem` 不读。)

净结果：

- **精准施法在落雷阵身上只剩暴击率**（`criticalChance` 10/20/40 → `CriticalChance += 0.1/0.2/0.4`）。`TakeDamageInfoPreProcessSystem` 对 buffer 里每条 `TakeDamageInfo` 独立掷骰（已标 `isDamageCritical` 的不再掷，见《精准施法逆向分析.md》4.2），而落雷阵每 tick 每目标各写一条 → Lv3 满命中 5 秒 = **140 次掷骰**，方差极小，**期望值利用率是全表最高的一档**。暴击的 ×2 乘在已经 ×0.25 的单 tick 伤害上，总量等价于整段乘 `1 + 暴击率`。
- **齐射对落雷阵是纯正收益**：`int1` 加组容量、`float1` 的 `Pow(float1/100, N−1)` 蓝耗折扣照拿，唯一的副作用（散射恶化）因为没有消费方而落空。这和它在蝴蝶（`angle = 90`）身上「容量正收益 / 散布大幅恶化」的两面性形成直接对照。

**但这条免疫只对光环形态成立。** 插坠落后 `Direction` 变成第一跳的方向（系统 L847），而 `ApplyScatterAngleToSpellSIP` 对 `spellIsFall` 且有瞄准点的那一发走另一条分支——不转方向，改抖瞄准点：`Target += 随机单位向量 × (锥角 × 0.06)`（`ShootSpellUtils.cs` L708–714）。齐射 20° 锥对应最多 **1.2 个单位**的落点抖动。精准施法在这条分支上仍是空转（锥角本来就是 0，负角减不出东西）。

### 7.9 节能模式（3104）：DPS 法术的双重打折

节能模式在三处生效，落雷阵**三处全中**：

| 作用点 | 字段 | 代码 | Lv1 | Lv2 | Lv3 |
| --- | --- | --- | --- | --- | --- |
| 伤害 | `Damage.MulRatio` ← `float3/100` | `CanShootSpellUtils.cs` L24–26 | ×0.75 | ×0.75 | ×0.80 |
| 时长 | `Duration.MulRatio` ← `float2/100` | `SpellShootData.cs` L138–140 | ×0.75 | ×0.75 | ×0.80 |
| 蓝耗 | `mpCostMulDivCorrection/100` | `SpellShootData.cs` L635–640 | ×0.60 | ×0.50 | ×0.40 |

对一次性飞弹，节能模式只砍伤害，效率比 = `0.75 / 0.60 = 1.25`（划算）。
对落雷阵这种「总伤 = 伤害 × 时长」的 DPS 法术，**伤害和时长同时被砍，是乘法叠加**：

| 槽位 | 伤害系数 | 时长系数 | **总伤系数** | 蓝耗系数 | 效率比 |
| --- | --- | --- | --- | --- | --- |
| `[节Lv1] [落雷阵]` | 0.75 | 0.75 | **0.5625** | 0.60 | **0.94** |
| `[节Lv2] [落雷阵]` | 0.75 | 0.75 | **0.5625** | 0.50 | **1.13** |
| `[节Lv3] [落雷阵]` | 0.80 | 0.80 | **0.64** | 0.40 | **1.60** |

**节能模式 Lv1 插在落雷阵上是负收益**（效率比 < 1），Lv2 才勉强打平，Lv3 才明显划算。

这是一条**通用结论**，但判据不是 `isDPS` 这个旗标本身，而是「**总伤是否与 `Duration` 成正比**」。`SpellSystem.UpdateDuration` 的例外名单（`SpellSystem.cs` L278）里已经包含高压水流 `HighPressureWasher` 和激光晶石 `LaserBeam`，它们根本不读 `Duration`，所以吃不到时长那一层折扣，效率比回到飞弹的 1.25 / 1.50 / 2.00。龙息 `1025` 不在名单里，但它的耗蓝底数本身含 `GetSpellDuration().Result`，时长因子在分子分母上抵消，效率比同样回到 1.25 档。**目前确证适用「双重打折」的只有落雷阵一个**。

### 7.10 自动导航（3011）：对 `Speed = 0` 的法术是空转

自动导航把 `movement.Type` 写成 `ChaseEnemy`（`SpellShootDataExtend.cs` L110–111），落雷阵没有专属分支，落到通用的 `Normal_ChaseTarget`：

```541:545:decompiled/Assembly-CSharp/SpellMoveJob.cs
			float num = (movement.IsFallSpell ? movement.OriginalSpellHorizontalSpeed : movement.Speed);
			float3 source = movement.Direction;
			float3 target = DTool.IgnoreZDir(in targetPosition, in transform.Position);
			movement.Direction = DTool.DirMoveTowardsIgnoreZ(in source, in target, num * movement.ChaseRotateSpeed * DeltaTime);
			velocity.Linear = movement.Direction * movement.Speed;
```

`num = Speed = 0` → `maxDelta = 0`，`Direction` 不转；`velocity.Linear = Direction × 0 = 0`，**光环仍然完全不动**。自动导航单独插在落雷阵上是一张废符文。

**但和加速器同插时它会活过来**：`Speed` 非 0 后 `maxDelta = Speed × ChaseRotateSpeed × dt`，光环变成会追人的雷云。`[加Lv1] [导Lv3] [落雷阵]` 的 `maxDelta = 3 × 38 = 114 °/s`，转向足够灵活。这是加速器负收益的唯一救济方案，但要占两个槽位。

坠落态下自动导航另有作用：`CalcFallTargetPosition` 的 `ChaseEnemy` 分支（系统 L849–864）会用 `DirMoveTowardsIgnoreZ(..., ChaseRotateSpeed * 3f)` 把下一跳的方向拧向最小夹角敌人。**坠落 + 反弹 + 自动导航 = 会自动追着敌人跳的连锁落雷**，而且这里用的是 `ChaseRotateSpeed × 3`（不乘速度），所以 `Speed = 0` 不影响它。

### 7.11 闪电链（3007）：静止锚点造出一条固定伤害线

闪电链把 `sip.lightningChainDamage`（`int1` 经完整伤害管线计算，`CanShootSpellUtils.cs` L69–77）写进 `SpellConfigComponentData.LightningChainDamage`。消费点在发射时：

```606:613:decompiled/Assembly-CSharp/SpellShootSystem.cs
		if (spawnParams.ConfigComponentData.LightningChainDamage > 0f && !spawnParams.IgnoreSpawnLightningChain && spawnParams.ConfigComponentData.AbilityType != SpellAbilityType.BiAnLethalBlade && spawnParams.ConfigComponentData.AbilityType != SpellAbilityType.Umbrella && spawnParams.ConfigComponentData.AbilityType != SpellAbilityType.RuneHammer && spawnParams.ConfigComponentData.AbilityType != SpellAbilityType.LaserBeam)
		{
			TrySpawnLightningChain(ref state, spawnParams, spellSingleton, entity);
			if (DTool.IsSameCamp(spawnParams.ConfigComponentData.ShooterType, UnitType.Player))
			{
				_lastSpellEntity = entity;
			}
		}
```

链建在**相邻两次生成的法术实体之间**（`SourceEntity = 本次`，`TargetEntity = _lastSpellEntity`），`Spell3007Job` 每 0.06 秒沿两端连线做一次 `SphereCast(width = 0.4)` 结算（`Spell3007Job.cs` L190–210）。

链的两端存的是**实体引用**不是坐标，`Spell3007Job` 每帧重取两端的 `LocalTransform`（`Spell3007Job.cs` L179–189），所以链的形状完全由两端法术自己的运动决定。

落雷阵 `shootCount = 1`，需要**多重施法、齐射同帧的另一个法术，或者法杖里的下一发**来提供另一端。一旦成立，就得到全表最干净的「两端都不动」的链。

**但它不是一堵能一直打的墙。** `Spell3007Job` L233–262 每完成一次伤害结算就 `PenetrateCount--`，归零立刻 `DestroyEntity`，而 `PenetrateCount = 1 + BonusPenetrate`——**没插穿透时基础只有 1，线段第一次打到人就消失**，整条链一共只结算一次（Lv3 = 48 点）。同一目标虽有 0.1 s 冷却后可再打，但每次同样扣预算。

所以静止锚点真正改变的**不是伤害量，而是这份预算不会被浪费**：飞弹的链要靠「此刻正好扫过敌人」才兑现，落雷阵的链守在原地等人走进来，5 秒内必然兑现。想把它当墙用，**必须叠穿透**去抬 `PenetrateCount`。

`[多重施法Lv1] [闪电链Lv3] [落雷阵Lv3]` = 两个光环 + 一条守在两者之间、迟早会兑现 48 点的线段。完整 DPS 需要把 `Spell3007DamageCoolDownBuffer` 的每目标冷却一起展开，本文只给机制判定。

### 7.12 范围增强（3107）：面积平方增长，但收益会被 `int1` 截断

`GetSpellEffectRadius` 对 `EnhanceRadiusRatio` 做 `AddRatio(float1/100)`（`SpellShootData.cs` L253–255）→ `sip.extraSizeRatio` → `RadiusAttributeValue.AddRatio`。

| 槽位（落雷阵 Lv3，base 3.2） | 半径 | 面积 | 传导环外径 | 环面积 |
| --- | --- | --- | --- | --- |
| `[落雷阵Lv3]` | 3.20 m | 32.2 m² | 4.80 m | 72.4 m² |
| `[范Lv1(+30%)] [落雷阵Lv3]` | 4.16 m | 54.4 m²（×1.69） | 6.24 m | 122.3 m² |
| `[范Lv2(+60%)] [落雷阵Lv3]` | 5.12 m | 82.4 m²（×2.56） | 7.68 m | 185.3 m² |
| `[范Lv3(+120%)] [落雷阵Lv3]` | 7.04 m | 155.7 m²（×4.84） | 10.56 m | 350.3 m² |
| `[范Lv3]×2 [落雷阵Lv3]` | 10.88 m | 371.9 m²（×11.6） | 16.32 m | 836.8 m² |

注意 `AddRatio` 是加算池（多张相加再一次乘），不是连乘。

**但 `int1` 不动。** 半径扩到 7 米以后，圈里可能站着 15 个敌人，落雷阵一 tick 仍然只打 7 个（且是物理查询返回顺序里的前 7 个，不是最近的 7 个）。所以范围增强的真实收益是：

1. 把「凑满 `int1` 个目标」的概率推到接近 1——这对 Lv1（`int1 = 3`）价值最高，对 Lv3（`int1 = 7`）在小房间里边际收益迅速衰减；
2. 让敌人难以走出光环，等价于变相延长有效时长；
3. 顺带放大坠落态的每跳步长和分裂子弹的铺开半径（7.2 节）。

**它不能提高单目标 DPS，也不能突破 7 目标上限。** 这条限定应该回写进《范围增强逆向分析.md》。

### 7.13 寒霜晶石（3014）：高频结算被冻结免疫吃掉

寒霜写 `sip.FrozenDuration`（1.5/3/6 秒），每次 `TryAttackEntity` 都会带上。但 `SetFrozen` 有 `FronzenState == Normal` 的门，冻结结束后还有一段 `FrozenImmune`。

落雷阵每 0.25 秒对同一目标结算一次，5 秒内对同一目标发起 **20 次**上冻尝试。其中：

- 第 1 次成功，目标进入 `Frozening`
- 冻结持续 1.5/3/6 秒 → 期间的 6/12/24 次尝试全部被门挡掉
- 冻结结束后 2 秒免疫 → 又挡掉 8 次
- Lv1 寒霜（1.5 s 冻 + 2 s 免疫 = 3.5 s 周期）在 5 秒窗口内**最多生效 2 次**；Lv3 寒霜（6 s）只能生效 1 次

**「高频命中 × 寒霜」的浪费率极高**，落雷阵把这个通用不对称放大到 20 倍。相比之下毒液是完全相反的结论（7.14）。

### 7.14 毒液晶石（3005）：层数按结算次数线性堆叠

毒液的 `VenomApplyCount` 没有任何「已中毒则跳过」的门，每次命中各叠 `int2` 层。落雷阵对单个目标 5 秒 20 次结算：

| 槽位 | 每次叠层 | 5 秒对单目标总层数 |
| --- | --- | --- |
| `[毒Lv1] [落雷阵]` | 1 | **20** |
| `[毒Lv2] [落雷阵]` | 2 | **40** |
| `[毒Lv3] [落雷阵]` | 4 | **80** |

配合时长强化 Lv3（44 次结算）和悬停 Lv3，单目标可以堆到 **256 层**。**落雷阵是全表最强的毒液叠层载体**，远超蝴蝶（Lv3 一发 20 层）。

注意 `SpellTools.GetSpellElementDataWithTimeScale`（系统 L183，`SpellTools.cs` L3250–3258）在低帧时会把 `VenomApplyCount` 乘上 `LastFrameTimeScale` 补偿——这是**唯一**做低帧补偿的元素字段，伤害本身不补偿。

### 7.15 雷霆晶石（3101）：只在销毁那一刻落一次，而且还要先过颜色骰

`SpellDestroyEventJob` 对每个带 `SpellDestroyTag` 的法术实体调一次 `CheckFallThunderDamage`（`SpellDestroyEventJob.cs` L127–131），1018 不在排除名单（排除的只有 `DaveHarpoons` / `BiAnLethalBlade` / `LaserBeam`）。但这条链上有**两道闸门**：

**第一道：颜色骰。** `float3`（15/20/30）不是伤害或半径，而是**把法术染成雷霆色的概率**，且多张只取 `Max` 不叠加：

```1643:1650:decompiled/Assembly-CSharp/SpellTools.cs
			float num = (from e in shoot.EnhanceList
				select e.GetFinalConfig() into e
				where e.abilityType == SpellAbilityType.ThunderCrystal
				select e).MaxOrDefault((SpellConfig e) => e.float3, 0f) / 100f;
			if (UnityEngine.Random.Range(0f, 1f) < num)
			{
				return SpellColorType.Thunder;
			}
```

**第二道：`CheckFallThunderDamage` 首行直接按颜色 return**（`SpellTools.cs` L2256–2259）。所以**每次施放只有 15%/20%/30% 的概率会落雷**，其余 70%~85% 的施放这张符文一点伤害都不出。

过了闸门之后的落雷（`SpellTools.cs` L2261–2273）：

- 半径 = `ThunderHitRadius` = `float1` = 2.2 / 2.4 / 2.8
- 伤害 = `spellConfig.Damage.Calculate() × ThunderHitDamageRatio`（`float2` = 200%/270%/320%）。注意这行**直接覆写 `NewInfo` 写好的值，不乘 `DamageInterval`** —— 落雷阵 Lv3 的落雷是 `88 × 3.2 = 281.6` **满额**伤害，不是 `22 × 3.2`
- `containsBrittleness: true` —— **落雷能打碎木桶罐子，而光环本体不能**（6.3 节）。同一套符文组合下这两个行为不一致，容易看着像 bug

落雷阵是**一个实体活 5 秒**，不是 20 个实体，所以触发次数极少：

| 组合（Lv3） | 落雷次数 | 单次伤害 | 触发概率 | 期望增伤 |
| --- | --- | --- | --- | --- |
| 裸落雷阵 + 雷霆Lv3 | 1 | 281.6 | 30% | **84.5** |
| 分裂Lv3 + 雷霆Lv3 | 母 1 + 子 8 = 9 | 母 281.6 / 子 92.9 | 30%（**全有或全无**） | 约 307 |

分裂子体的 `ColorType` 来自 `ToSplit` 复制的快照，**不重新掷骰**，所以是「30% 概率九发全响，70% 概率一发不响」，方差极大。子体伤害同样吃 `Damage.MulRatio × 0.33`（`88 × 0.33 × 3.2 = 92.9`）。

对照单目标 5 秒 704 的基础输出，裸装配雷霆晶石的期望增伤只有约 **+12%**，且波动极大。**这和「多发弹每只销毁各落一次雷」的直觉相反：落雷阵的雷霆晶石收益低，除非配分裂。** 落雷不伤己方（`GetEnemyEntityInRange` 带阵营参数）。

### 7.16 已核验的「散射类无交互」

精准施法与齐射的散射部分对落雷阵完全无效，理由见 7.8。两篇文档如果有「散射底数表」，1018 应当以 `angle = 0` 单独列出，说明这是散射恶化免疫的极端案例。

---

## 8. 尚无逆向文档但对落雷阵影响极大的 5 张符文

这几张改的是**移动类型**或**发射次数**，对一个「静止 5 秒的光环」是质变级别的。本节只给机制判定，不做完整逆向，留待各自文档铺开时回补。

### 8.1 公转（3009 AroundOwner）：让光环黏在玩家身上

`GetSpellMovementType` 命中 `AroundOwner` → `Type = Rotation`（`SpellShootDataExtend.cs` L105–107）。`GetSpellRotationInfo` 取 `extraSpeed = float2`、`extraDuration = float1`、`rotationRadiu = float3`（`SpellShootData.cs` L276–280）。

| 公转 | float1（+时长） | float2（+速度） | float3（公转半径） |
| --- | --- | --- | --- |
| Lv1 | +4 s | 0 | 3 m |
| Lv2 | +7 s | 5 | 4 m |
| Lv3 | +11 s | 10 | 5 m |

`Normal_Rotation`（`SpellMoveJob.cs` L706–720）是**按位置驱动**的，每帧从 `AroundCenter` 重算位置，`velocity.Linear` 直接置零：

```708:718:decompiled/Assembly-CSharp/SpellMoveJob.cs
		float num = (movement.IsFallSpell ? movement.OriginalSpellHorizontalSpeed : movement.Speed);
		float num2 = 360f / (MathF.PI * 2f * movement.AroundRadius / num) * DeltaTime;
		...
		movement.AroundAngle += num2;
		movement.Direction = Tool2D.GetDir(movement.AroundAngle + 90f);
		float3 position = movement.UpdateAroundFollowAndGetAroundPositionWhenAround(TransformLookup);
		position.z = transform.Position.z;
		transform.Position = position;
```

所以：

- **公转 Lv1**：`extraSpeed = 0` → `num2 = 0`，角度不变，但位置每帧跟着玩家重算。**光环变成一个挂在玩家身侧 3 米、跟着玩家跑的固定卫星**，时长 5+4 = **9 秒**。
- **公转 Lv3**：`extraSpeed = 10` → 以 `360/(2π×5/10) ≈ 114 °/s` 绕玩家旋转，半径 5 米，时长 5+11 = **16 秒**。

两级都极强。公转在这里同时提供了「跟随」「大幅加时长」两项，而且 `Type = Rotation` 还会让碰撞过滤忽略墙体（`SpellShootSystem.cs` L419–422），光环不会被地形卡住。**公转很可能是落雷阵的最优单张符文**，值得单独实测。

### 8.2 寻踪（3010 AroundMouse）：光环跟着鼠标走

`AroundMouse` 同时进三个池子：

- `GetSpellFlySpeed` → `AddBase(float1)` = +1/+2/+4 速度（`SpellShootData.cs` L88–91）
- `GetSpellDuration` → `AddBase(float2)` = +1/+2/+4 秒（`SpellShootData.cs` L144–146）
- `GetChaseMouseLerpSpeed` → `float3` = 0.2（`SpellShootData.cs` L566–572）
- `GetSpellMovementType` → `Type = ChaseMouse`

`Normal_Mouse` 的插值系数含 `movement.Speed` 因子，`Speed` 从 0 变成 1/2/4 后光环开始跟手。**寻踪把落雷阵变成一个可以用鼠标推着走的雷云**，同时白送 1/2/4 秒时长。速度比加速器温和得多（Lv1 只有 1 m/s），而且是玩家可控的，**在「让光环去到该去的地方」这个需求上明显优于加速器**。

### 8.3 多重施法（3002 Multishot）：直接乘法术数量

`int1` = 1/2/4 张额外发射，蓝耗 `×1.5`。对落雷阵就是**同时放 2/3/5 个光环**。因为落雷阵的 `int1` 是**每个实体各自的**命中上限，两个光环重叠时同一个敌人会被两个光环各打一次——**多重施法是落雷阵单目标 DPS 的唯一线性乘数**，也解开了 7.11 闪电链需要第二个实体的前提。

### 8.4 折射（3121 Refraction）：光环态是纯负收益

折射的 `float2`（80/85/90）进 `GetDamage` 的 `MulRatio`：

```21:26:decompiled/Assembly-CSharp/CanShootSpellUtils.cs
				case SpellAbilityType.Refraction:
					ratioValue.MulRatio(enhance.float2 / 100f);
					break;
				case SpellAbilityType.PowerSavingMode:
					ratioValue.MulRatio(enhance.float3 / 100f);
					break;
```

即**全局 −20% / −15% / −10% 伤害**，换 `int1`（2/3/5）次折射。

- **光环态**：折射由 `SpellTakeDamageResultSystem` 处理，且有 `!spell.Movement.ValueRO.IsFallSpell` 守卫（L323），所以非坠落的落雷阵**会**走折射逻辑——`TryRefract` 成功时改的是 `movement.Direction`，而落雷阵 `Speed = 0` 不动。**折射次数被白白消耗，只留下 −20% 伤害的代价。折射是光环态的纯负收益符文。**
- **坠落态**：由 `Spell1018FallJob.TryGetRefractionPos` 独立处理（系统 L709–762），成功时**不消耗 `ReboundCount`**（5.4 节），是纯赚的额外跳跃。

同一张符文在两个形态下一边纯亏一边纯赚，和反弹（7.7）正好构成一对镜像。

### 8.5 穿透（3006 Penetrate）：完全无效

`int1` = 1/2/4。落雷阵的两个 Job 都用 `MakeDamageInfo(costPenetrate: false)`（系统 L189、L771），穿透值从头到尾不被读取也不被消耗。**穿透对落雷阵是零效果**。

---

## 9. 数值算例

基准：落雷阵 Lv3（`damage = 88`、`int1 = 7`、`radius = 3.2`、`float1 = 160`、`mpCost = 56`）。忽略法杖修正。

### 9.1 伤害类符文作用在 `Damage.Calculate()` 上

`Damage.Calculate() = (88 + 0) × (1 + extraDamageRatio) × finalDamageRatio + finalDamageExtra`

| 槽位 | `Damage.Calculate()` | 每 tick 每目标 | 单目标 5s（×1.6） | 满命中 5s |
| --- | --- | --- | --- | --- |
| `[落雷阵Lv3]` | 88 | 22 | 704 | 3080 |
| `[伤Lv1(+25%)] [落雷阵Lv3]` | 110 | 27.5 | 880 | 3850 |
| `[伤Lv3(+100%)] [落雷阵Lv3]` | 176 | 44 | 1408 | 6160 |
| `[伤Lv3]×2 [落雷阵Lv3]` | 264 | 66 | 2112 | 9240 |
| `[节Lv3] [落雷阵Lv3]` | 70.4 | 17.6 | 704 × 0.64 = 451 | 1971（蓝耗 22.4） |
| `[折Lv1] [落雷阵Lv3]` | 70.4 | 17.6 | 563 | 2464（折射白扔） |

`AddRatio` 是加算池：两张伤害强化 Lv3 是 `(1 + 1.0 + 1.0) = ×3`，不是 `×4`。

### 9.2 时长类三件套可以叠满

| 槽位 | 有效结算时长 | 结算次数 | 满命中总伤 | 蓝耗 |
| --- | --- | --- | --- | --- |
| `[落雷阵Lv3]` | 5.0 s | 20 | 3080 | 56 |
| `[时Lv3] [落雷阵Lv3]` | 11.0 s | 44 | 6776 | 56 |
| `[悬Lv3] [落雷阵Lv3]` | 10.0 s | 40 | 6160 | 56 |
| `[时Lv3] [悬Lv3] [落雷阵Lv3]` | 16.0 s | 64 | **9856** | 56 |
| `[公转Lv3] [时Lv3] [悬Lv3] [落雷阵Lv3]` | 27.0 s | 108 | **16632** | 56 |

蓝耗一分不涨。**落雷阵的时长堆叠是全表蓝耗效率上限最高的路线之一**，代价是需要 3~4 个槽位、伤害爆发慢、且必须敌人愿意站在圈里。

### 9.3 坠落线的爆发对照

| 槽位 | 形态 | 打击次数 | 7 人场总伤 | 单目标总伤 |
| --- | --- | --- | --- | --- |
| `[落雷阵Lv3]` | 光环 5 s | 20 tick | 3080 | 704 |
| `[坠Lv1] [落雷阵Lv3]` | 1 跳 | 1 | **4312** | 140.8 |
| `[坠Lv1] [反Lv3] [落雷阵Lv3]` | 9 跳 | 9 | **38808** | 1267 |
| `[坠Lv3] [反Lv3] [折Lv3] [落雷阵Lv3]` | 9 跳 + 最多 5 次折射 | ≤14 | ≈ 54 000（伤害 ×0.9） | ≈ 1774 |

坠落线在多目标下的数字大到离谱，主要来自 5.2 的 N² 双循环。**如果这个循环被修复，坠落线的多目标伤害会直接除以 `totalCount`**（7 人场从 38808 掉到 5544），因此本文对该线的所有数字标注为「基于当前实现，随时可能失效」。

### 9.4 元素层的两极分化

| 槽位 | 5 秒对单目标的效果 |
| --- | --- |
| `[毒Lv3] [落雷阵Lv3]` | **80 层毒**（每 tick 4 层 × 20 tick） |
| `[毒Lv3] [时Lv3] [悬Lv3] [落雷阵Lv3]` | **256 层毒** |
| `[寒Lv3] [落雷阵Lv3]` | **1 次冻结**（6 s 冻 + 2 s 免疫覆盖整个窗口，另外 19 次尝试全废） |
| `[粘Lv3] [落雷阵Lv3]` | 减速 debuff 每 0.25 s 刷新一次，圈内几乎恒定生效 |

---

## 10. Mono 残留轨对照

`Spell1018ThunderAura : SpellBase` 仍在程序集里。玩家施法主路径是 ECS，这条轨对玩家落雷阵不生效，但它是理解设计意图的参考，而且**两轨在「传导」这个核心机制上的实现完全不同**。

### 10.1 传导：Mono 是真链式，ECS 是同心圆

```151:177:decompiled/Assembly-CSharp/Spell1018ThunderAura.cs
	private List<Collider> GetLightningLineAttackTargets(List<Collider> startTargets)
	{
		if (startTargets.Count == 0)
		{
			return new List<Collider>(0);
		}
		List<Collider> result = startTargets.ToList();
		int num = 0;
		while (result.Count < base.spellCfg.int1)
		{
			num++;
			if (num >= 100)
			{
				break;
			}
			List<Collider> list = result;
			Collider[] array = (from t in GetAroundTargets(list[list.Count - 1].transform.position, base.spellCfg.radius * longRangeDetectSizeRatio)
				where !result.Contains(t)
				select t).ToArray();
```

| 项 | ECS（玩家主路径） | Mono 残留轨 |
| --- | --- | --- |
| 补位搜索中心 | **光环自己**（`spell.Transform.Position`） | **上一个目标**（`list[^1].transform.position`）——真正的链式扩散 |
| 补位半径系数 | 硬编码 `1.5f` | `longRangeDetectSizeRatio`（预制体序列化字段） |
| 循环上限 | `num >= Int1` | `num >= 100` |
| 起手半径 | `Radius.Calculate()`（含范围增强） | `spellCfg.radius`（**不含**运行期修正，见 `GetAroundTargetsAndLightningLineAttackTargets` L145–149） |

**Mono 版的链能沿着敌人一路串出很远，ECS 版只能在 1.5 倍同心圆里补人。** 面板文案「传导」和风味文案「区域外的敌人也可能会被牵连」都是照 Mono 版写的，ECS 重写后文案没跟着改。

### 10.2 结算间隔：Mono 随机阈值，ECS 随机扣减

```24:36:decompiled/Assembly-CSharp/Spell1018ThunderAura.cs
	private float CurRandomDamageInterval
	{
		get
		{
			float? interval = _interval;
			if (!interval.HasValue)
			{
				float num = base.spellCfg.DPSDamageInterval * base.spellCfg.float2;
				_interval = UnityEngine.Random.Range(base.spellCfg.DPSDamageInterval - num, base.spellCfg.DPSDamageInterval + num);
			}
			return _interval.Value;
		}
	}
```

Mono 每次随机一个 `[0.10, 0.40]` 的**阈值**并缓存，触发后 `ReRandomDamageInterval()` 重掷；ECS 是固定阈值 0.25、随机**扣减量** `0.25 ± 0.15`。两者长期均值都是 0.25，但 ECS 版的 `DamageTimer` 会带残差累积，短期节奏略有不同。

Mono 的首帧初值是 `attackIntervalTimer = -0.05f`（L88），首次结算比 ECS 晚 0.05 秒。

### 10.3 低帧补偿：Mono 有，ECS 没有

```105:105:decompiled/Assembly-CSharp/Spell1018ThunderAura.cs
		timeScale = ((!GeneralTool.IsLowFpsOptimizeActive(30f)) ? 1 : Mathf.CeilToInt(10f * (1f - GameMgr.Inst.GetFps() / 30f)));
```

Mono 在 30 FPS 以下把 `DurationTimer` 和**伤害本身**都乘上 `timeScale`（L110、L211），上限由 `GameConst.ThunderAuraLowFPSMaxTimeScale = 10f` 约束（`GameConst.cs` L867–869）。

ECS 侧这两个常量**没有任何引用**，`Spell1018Job` 只对 `VenomApplyCount` 做时间缩放（系统 L183）。所以 ECS 版在极低帧下会真实丢结算（`dt > 0.25` 时每帧只补一次），Mono 版会用伤害倍率补回来。实践中要低于 4 FPS 才会触发，影响有限，但**两个 `GameConst` 常量已成死代码**这一点值得记录。

### 10.4 取整与其它差异

- **取整**：Mono `MakeDamage` 对非坠落伤害做 `Mathf.Ceil(damage × DPSDamageInterval)`（L214、L248），ECS 是纯浮点。
- **坠落态**：Mono `OnFallingLightingOnGround`（L258–271）对每个目标各调一次 `LightningAttack`，**并且只对 tag 为 `"Monster"` 的目标做链式扩散**——是 O(N) 的。ECS 的 `TryAttack` 是 O(N²)（5.2 节）。**这是 ECS 独有的行为，几乎可以确定是重写时的失误。**
- **接触伤害**：Mono `TriggerIn` 覆写为空（L407–409），明确关闭。
- **表现**：Mono 非坠落时创建 `"Spell"` 和 `"Rain"` 两个特效（L81–82），并把 `tsf_Layer` / `tsf_Ground` 的 localScale 设成 `spellCfg.radius`（L91–92）；ECS 对应的是 `SpawnChild(1018, "OnGround")`。
- **震屏**：Mono 用序列化的 `shockParam`，ECS 用硬编码 `{ Radius=0.025, Speed=1, Time=0.08 }`。

---

## 11. 与构筑相关的行为摘要

| 行为 | 结果 |
| --- | --- |
| 裸放 | 枪口处 5 秒静止光环，每 0.25 s 劈最多 `int1` 个目标，每次 `damage × 0.25` |
| 面板 `damage` 的含义 | **每目标每秒伤害**（`isDPS` 让实际单次伤害 = `damage × 0.25`） |
| 面板「传导 int1 次」的含义 | 实为**单次结算命中上限 = int1 个目标**（不是 1+int1），且只在圈内人不够时才向 1.5 倍环带补人 |
| 只打到 1 个目标 | 伤害 ×`float1%`（Lv1 无效 / Lv2 +30% / Lv3 +60%），补位算进命中数，会让加伤消失 |
| 插伤害强化 | 每 tick 线性放大，光环/坠落通吃，最干净的收益 |
| 插时长强化 | **严格线性**加伤（+40%/+70%/+120%），蓝耗不变；坠落态完全无效 |
| 插悬停 | **白送 2/3/5 秒满速结算**，与时长强化叠加；这是落雷阵独有的行为 |
| 插范围增强 | 面积平方增长，但**不能突破 int1 上限**，也不提高单目标 DPS |
| 插分裂 | 销毁瞬间在半径圈上环形铺 N 个 80% 半径的子光环，各跑满 5 秒，是最强的面积扩张 |
| 插加速器 | **负收益**：把静止光环变成匀速直线飘走的雷云；配悬停可缓解 |
| 插自动导航 | 单插**完全无效**（`Speed=0` → `maxDelta=0`）；配加速器才活；坠落态用于自动追踪下一跳 |
| 插反弹 | 光环态**完全无效**；坠落态是跳跃次数，核心乘数 |
| 插折射 | 光环态**纯负收益**（−10~20% 伤害，折射次数白扔）；坠落态是免费额外跳跃 |
| 插穿透 | 完全无效（两个 Job 都 `costPenetrate: false`） |
| 插精准施法 | `angle` 部分空转，只剩暴击率；但每 tick 每目标独立掷骰（Lv3 满命中 140 次），期望利用率全表最高 |
| 插齐射 | **纯正收益**：容量与蓝耗折扣照拿，散射虽然算得出锥角但方向无消费方；**仅光环态免疫**，坠落态会抖落点 |
| 插节能模式 | **双重打折**（伤害 × 时长），Lv1 效率比 0.94 是**负收益**，Lv3 才划算 |
| 插毒液晶石 | **全表最强叠层载体**：单目标 5 秒 20/40/80 层，堆时长可到 256 层 |
| 插寒霜晶石 | 20 次上冻尝试只有 1~2 次生效，浪费率极高 |
| 插粘液晶石 | 每 0.25 s 刷新，圈内 debuff 几乎恒定 |
| 插雷霆晶石 | 只在销毁时落 1 次雷，且需先过 15/20/30% 的**染色骰**；落雷打满额伤害（Lv3 = 281.6）并能打可破坏物。裸装期望增伤仅约 +12% |
| 插闪电链 | 需要第二个法术实体；两端都不动 → 预算必然兑现，但 `PenetrateCount` 基础只有 1，**打中一次就断**，想当墙必须叠穿透 |
| 插坠落 | **换成另一个法术**：瞬发跳跃打击、满额伤害、每目标挨 `totalCount` 下、无 duration |
| 插公转（未逆向） | 光环跟随玩家 + 时长 +4/+7/+11 秒，很可能是最优单张 |
| 插寻踪（未逆向） | 光环跟随鼠标 + 时长 +1/+2/+4 秒，比加速器温和且可控 |
| 插多重施法（未逆向） | 同时放 2/3/5 个光环，单目标 DPS 的唯一线性乘数 |
| 可破坏物 | **打不到**（`Brittleness` 被过滤两次） |
| 己方单位 / 己方蝴蝶 / 己方滚球 | 无事发生（阵营守卫） |
| 敌方蝴蝶 / 敌方滚球 | **会被打**（`RemoveCannotAttackSpell` 白名单包含二者） |
| 落雷阵本体被攻击 | 不可能——它不在 `RemoveCannotAttackSpell` 白名单里，没有生命值 |
| 蓄力模式 / 强制冷却 / 储魔之匣 / 奥术屏障 | 法杖层或独立法术，不改光环属性 |

---

## 12. 证据索引

| 结论 | 文件 / 位置 |
| --- | --- |
| 配置数值 22/44/88、int1 3/5/7、float1 100/130/160、float2 0.6、radius 2.8/3.0/3.2 | `assets_export/SpellConfig.json`（10181–10183） |
| 文案（机制/风味/短评） | `assets_export/TextConfig_Spell.json`（701018x / 711018x / 7210181 / 7310181） |
| 光环结算主循环 | `Spell1018ThunderAuraSystem.cs` L157–237（`Spell1018Job.Execute`） |
| 结算 Job 查询**不含生命周期条件** | `Spell1018ThunderAuraSystem.cs` L25、L57–61 |
| 抖动间隔 | `Spell1018ThunderAuraSystem.cs` L252–256、L227 |
| 命中上限 `min(Int1, count)` | `Spell1018ThunderAuraSystem.cs` L171–175 |
| 单目标加伤（光环 / 坠落） | `Spell1018ThunderAuraSystem.cs` L191–194 / L773–776 |
| 「传导」= 同心圆补位，中心是光环自己，硬编码 1.5 | `Spell1018ThunderAuraSystem.cs` L258–293（尤其 L273–274） |
| 阵营 / NotAttack / Brittleness 过滤 | `Spell1018ThunderAuraSystem.cs` L295–309；`SpellTools.cs` L2862–2888、L2967–2981 |
| 坠落态主循环、0.1 s 一跳、ReboundCount 耗尽销毁 | `Spell1018ThunderAuraSystem.cs` L638–691 |
| **坠落 N² 双循环** | `Spell1018ThunderAuraSystem.cs` L764–770 |
| 坠落满额伤害（绕过 ×0.25） | `Spell1018ThunderAuraSystem.cs` L772 |
| 坠落折射不消耗 ReboundCount | `Spell1018ThunderAuraSystem.cs` L665–670、L709–762 |
| 坠落下一跳落点计算（含 ChaseEnemy 分支） | `Spell1018ThunderAuraSystem.cs` L841–888 |
| 首帧初始化 / 地面贴花 / 坠落起点 | `Spell1018ThunderAuraSystem.cs` L1355–1400 |
| 两个 Job 各自 `Complete()`（同步点） | `Spell1018ThunderAuraSystem.cs` L1416、L1437 |
| 组件三字段（全为坠落服务） | `Spell1018ThunderAuraData.cs` |
| 烘焙 / `LightningChainMaxConductRangeRatio` 死字段 | `Spell1018ThunderAuraAuthoring.cs` L8–21 |
| 雷链纯表现，0.3 s 后销毁 | `Spell1018ChainSystem.cs` L134–158 |
| 坠落闪电与爆炸表现 | `Spell1018FallEffectSystem.cs` L39–75 |
| `isDPS` → `DamageInterval` | `ShootSpellUtils.cs` L286；`SpellSingleton.cs` L86 |
| `DamageInterval` → 单次伤害 ×0.25 | `TakeDamageInfo_Dots.cs` L153 |
| 面板「(秒伤)」后缀 | `SlotData.cs` L384–388；`SpellConfig.cs` L281–287 |
| 蓝耗与 isDPS 无关 | `SpellShootData.cs` L614–644 |
| duration / 悬停 / 销毁（1018 不在例外名单） | `SpellSystem.cs` L266–311 |
| 坠落态无 duration | `SpellSystem.cs` L275、L310 |
| `SpellFallTag` 由 `IsFallSpell` 添加 | `SpellShootSystem.cs` L330–333 |
| `spellIsFall = fallExplosionRadius > 0` | `SpellInitialParameter.cs` L640 |
| 生成位置 = SpawnPosition（Normal） | `SpellShootSystem.cs` L407–412 |
| `Speed.Base = 0` 可被 AddBase 抬起 | `AttributeValue.cs` L24–27；`ShootSpellUtils.cs` L198–205；`SpellInitialParameter.cs` L304–308 |
| 加速器 / 寻踪 → `Speed.AddBase` | `SpellShootData.cs` L79–98 |
| 半径公式（含 FallRadius） | `RadiusAttributeValue.cs` L24–27；`ShootSpellUtils.cs` L255–262 |
| 范围增强 → `Radius.AddRatio` | `SpellShootData.cs` L244–262 |
| 时长强化 / 节能模式 / 寻踪 → `Duration` | `SpellShootData.cs` L128–150 |
| 伤害强化 / 节能模式 / 折射 → `Damage` | `CanShootSpellUtils.cs` L7–33 |
| 蓝耗乘算（多重施法 / 分裂 / 节能 / 超载） | `SpellShootData.cs` L629–642 |
| 悬停时长 | `SpellShootData.cs` L398–411 |
| 反弹次数与加时 | `SpellShootData.cs` L579–594 |
| 坠落半径 `float1` 求和 | `SpellShootData.cs` L596–602 |
| 折射次数与伤害惩罚 | `SpellShootData.cs` L531–543；`CanShootSpellUtils.cs` L21–23 |
| 移动类型选择 | `SpellShootDataExtend.cs` L99–117 |
| 公转参数（extraSpeed/extraDuration/radius） | `SpellShootData.cs` L264–296 |
| `Normal_Rotation` 按位置驱动 | `SpellMoveJob.cs` L706–720 |
| `Normal_ChaseTarget` 转向速率含 Speed 因子 | `SpellMoveJob.cs` L861–876（关键在 L541–545 段的 `num`） |
| 分裂：数量、0.33 硬编码、80% 半径、环形铺开 | `ShootSpellUtils.cs` L314–318；`SpellSpawnParamsStorage.cs` L36–59；`SpellDestroySystem.cs` L485–540 |
| 分裂子弹重力清零（1018 专属） | `SpellDestroySystem.cs` L568–571 |
| 雷霆晶石在销毁时触发 | `SpellDestroyEventJob.cs` L124–132 |
| 雷霆晶石颜色骰（`float3` 取 Max，不叠加） | `SpellTools.cs` L1639–1651 |
| 落雷按颜色 return / 满额伤害 / `containsBrittleness: true` | `SpellTools.cs` L2254–2274（尤其 L2256、L2261、L2269） |
| 闪电链挂在相邻两个法术实体之间；两端存实体引用、每帧重取坐标 | `SpellShootSystem.cs` L606–656；`Spell3007Job.cs` L164–221、L179–189 |
| 闪电链 `PenetrateCount = 1 + BonusPenetrate`，结算一次即销毁 | `Spell3007Job.cs` L233–262 |
| 开火锥角 `cone = (angle + extraScatter) × mul`，负值钳零 | `ShootSpellUtils.cs` L631–636、L282 |
| 坠落发的散射改抖瞄准点而非转方向 | `ShootSpellUtils.cs` L708–714 |
| 暴击按 buffer 逐条独立掷骰 | `TakeDamageInfoPreProcessSystem.cs`（详见《精准施法逆向分析.md》4.2） |
| 非坠落折射走 TakeDamageResult（对静止法术白扔） | `SpellTakeDamageResultSystem.cs` L322–341 |
| 毒液低帧补偿（唯一被缩放的元素字段） | `SpellTools.cs` L3250–3258 |
| 伤害缓冲区无去重 | `SpellTools.cs` L2733–2760；`TakeDamageInfoPreProcessSystem.cs` L164–186 |
| AI 攻击距离 `max(半径, 速度×时长)` | `SpellGroupAttackDistanceCalculator.cs` L283–285 |
| 复杂度 3 分 | `SpellTools.cs` L3022–3031 |
| 死常量 1.5 / 30 / 10 | `GameConst.cs` L649、L867、L869 |
| System 组归属与顺序 | `SpacialSpellSystemGroup.cs`、`SpellSimulationSystemGroup.cs`、`SpellPhysicsSystemGroup.cs`、`UnitTakeDamageGroup.cs` |
| Boss6 二阶段对 1018 的免伤冷却 | `Boss6_Stage2.cs` L2063–2068、L2101–2109 |
| Mono 真链式传导 | `Spell1018ThunderAura.cs` L151–177 |
| Mono 随机阈值间隔 | `Spell1018ThunderAura.cs` L24–41 |
| Mono 低帧伤害补偿 | `Spell1018ThunderAura.cs` L105、L110、L211 |
| Mono 取整 | `Spell1018ThunderAura.cs` L214、L248 |
| Mono 坠落 O(N) | `Spell1018ThunderAura.cs` L258–271 |
| Mono 关闭接触伤害 | `Spell1018ThunderAura.cs` L407–409 |
| 枚举 / 常量 ID | `SpellAbilityType.cs` L21（`ThunderAura = 1018`）；`IDMgr.cs` L343 |

---

## 13. 需要回写既有文档的位置

> **状态：17 处已全部回写完毕。** 回写过程中反查出 3 处对本文原稿的修正，已合并进 7.8 / 7.9 / 7.11；另在既有文档里改正了 3 处笔误（分裂与自动导航文档把 1018 写成「雷霆光环」「雷光环」，雷霆晶石 HTML 把排除项 `LaserBeam` 误记为 1004 而非 4014）。

| # | 文档 | 位置 | 需要补什么 |
| --- | --- | --- | --- |
| 1 | 坠落逆向分析.md | 按 abilityType 的坠落行为表 | 1018 单独成行：不只是「半径 + float1」，而是**整个法术形态被替换**（无 duration、0.1 s 一跳、满额伤害、N² 多段）；并注明反弹/折射在坠落态才生效 |
| 2 | 悬停逆向分析.md | 机制族表 | 1018 单独成族「悬停期继续全速输出」——这是目前已知唯一一个悬停等价于时长强化的法术 |
| 3 | 时长强化逆向分析.md | 收益模型章节 | 补一条「`isDPS` 法术的时长收益严格线性」，并注明坠落形态下完全无效 |
| 4 | 范围增强逆向分析.md | 通用结论 | 补限定：对有「单次命中上限」字段的法术（1018 的 `int1`），扩圈收益在上限处截断，不提高单目标 DPS |
| 5 | 节能模式逆向分析.md | 效率比章节 | 补一条通用结论：对 `isDPS` 且靠 duration 计时的法术，伤害与时长**双重打折**，Lv1 效率比 0.94 是负收益 |
| 6 | 反弹逆向分析.md | 适用性表 | 补：对 `speed = 0` 的静止法术，光环形态下反弹完全无效（不产生新碰撞事件）；坠落形态下被 1018 挪用成跳跃次数 |
| 7 | 加速器逆向分析.md | 通用结论 | 补：`Speed.Base = 0` 的法术（1018）会被加速器从「静止」变成「移动」，属于形态改变而非数值加成，通常是负收益 |
| 8 | 自动导航逆向分析.md | 例外表 | 补：`Speed = 0` 时 `Normal_ChaseTarget` 的 `maxDelta = Speed × ChaseRotateSpeed × dt = 0`，符文完全空转，必须配加速器 |
| 9 | 齐射逆向分析.md | 散射副作用章节 | 补：1018 会算出锥角但方向无消费方，齐射对它是纯正收益，与蝴蝶形成对照；并注明坠落态例外（改抖落点） |
| 10 | 精准施法逆向分析.md | 2.6 节散射钳零 | 补：`angle = 0` + `Speed = 0` 时偏角写进 `Direction` 后无人消费，符文只剩暴击率；但 140 次掷骰让期望利用率全表最高 |
| 11 | 毒液晶石逆向分析.md | 叠层载体对比 | 补 1018：单目标 5 秒 20/40/80 层，堆时长可达 256 层，是全表上限 |
| 12 | 寒霜晶石逆向分析.md | 高频命中浪费率 | 补 1018：20 次上冻尝试只生效 1~2 次，是「高频 × 寒霜」不对称的极端案例 |
| 13 | 雷霆晶石逆向分析.md | 触发次数章节 | 补 1018：单实体只落 1 次雷，与多发弹的「每只各落一次」形成反差；配分裂才有 N+1 次 |
| 14 | 闪电链逆向分析.md | 锚点运动章节 | 补 1018：静止锚点让链变成固定线段，是全表唯一的稳定形态 |
| 15 | 分裂逆向分析.md | 子弹继承表 | 补 1018：子光环 `Radius.Base ×0.8`、`Damage.MulRatio ×0.33`、`int1` 不打折、duration 满额重开 |
| 16 | 法术数值总表.md | 1018 行 | 补 `isDPS`、`DPSDamageInterval` 两列的语义说明，并注明 `damage` 对 1018 是「每秒伤害」 |
| 17 | 魔法工艺法术系统逆向报告.md | 伤害模型章节 | 新增「`isDPS` 持续伤害模型」小节，引用本文 2.3 |

---

## 14. 待决 / 局限

1. **坠落态的 N² 双循环是否为有意设计未确认。** 5.2 节的结论基于代码直读与「伤害缓冲区无去重」的验证，但没有游戏内实测。外层 `i` 只服务于表现分支这一点强烈提示这是 bug。若被修复，9.3 表里所有坠落多目标数字都要除以 `totalCount`。**建议实测优先级最高。**
2. **`SpellDisableTriggerDamageTag` 是否挂在 1018 预制体上未确认。** 4.4 节。反编译读不到预制体，Mono 侧空 `TriggerIn` 是间接证据。若没挂，落雷阵还会有一次接触伤害。
3. **`Boss6_Stage2.normalSpellClearTime` 的具体值未知。** 6.4 节。它决定落雷阵对该 Boss 的实际 DPS 折损比例，是序列化字段。
4. **`Spell1018ThunderAuraAuthoring.LightningChainMaxConductRangeRatio` 与 Mono 的 `longRangeDetectSizeRatio` 的预制体取值未知。** 前者在 ECS 里是死字段，后者决定 Mono 轨的链式扩散半径。只影响 10.1 的定量对照。
5. **`GetAttackableEntitiesInRange` 的返回顺序未确认。** 当圈内敌人多于 `int1` 时，`entities[0..int1-1]` 是哪几个完全取决于 `PhysicsWorld.OverlapSphere` 的遍历顺序（broadphase 树序），既不是最近也不是随机。这直接影响「半径扩大后打谁」的实际手感，需要实测确认是否稳定。
6. **5 秒内的实际结算次数未实测。** 3.2 节按连续域给出均值 20、标准差 1.55，但 `DamageTimer` 的残差累积与渲染帧长的交互没有做离散仿真。本文所有算例用均值 20。
7. **公转（3009）、寻踪（3010）、多重施法（3002）、折射（3121）、穿透（3006）尚无逆向文档。** 第 8 节只做了机制判定，没有做完整的数值校验。公转对落雷阵的实际强度（尤其 Lv1 的「零角速度跟随」是否真的按本文描述表现）**需要实测确认**。
8. **闪电链 + 落雷阵的实际 DPS 未计算。** 7.11 节只给了机制。`Spell3007Job` 的 `SphereCast(width=0.4)` 每 0.06 秒一次，且有 `Spell3007DamageCoolDownBuffer` 的每目标冷却与 `PenetrateCount` 预算，需要连穿透一起展开才能给数字。
9. **`float2 = 0.6` 三级不变，未验证是否有符文能改它。** 全表检索没有发现写 `Float2` 的强化路径，但 `SpellConfigComponentData.Float2` 是可写字段，不排除某些未逆向的遗物/诅咒会动它。
10. **坠落态为什么吃不到通用 `SpellFallDamageSystem` 未坐实。** 推测原因是首帧把 `CurrentFallSpeed` 清零（系统 L1376–1378）导致 `Normal_Fall` 的落地判定恒假，而不是靠 `DisableDefaultFallDamage`。这条是代码直读推出来的，没有实机验证。
