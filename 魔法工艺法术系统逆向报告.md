# 《魔法工艺》(Magicraft) 法术系统逆向分析报告

> 分析对象：Steam 游戏《魔法工艺》(Magicraft)，开发商 bolang。
> 分析范围：法术（Spell/Rune）设计体系、法杖（Wand）运作机制、核心数值结构与公式。
> 分析方法：静态反编译 Mono 程序集 + 提取本地 Unity 资源中的 JSON 配置表，交叉比对代码与数据得出结论。本报告所有结论均标注来源文件，未标注"推测"的均有代码或数据直接证据支撑。
> 本文档为长期分析产物，配套文件见文末"证据与产物清单"。

---

## 0. 与《微信小游戏逆向标准与经验总结.md》的关系说明

用户提供的参考文档总结的是**微信小游戏**（Cocos/Unity WebGL + IL2CPP + AssetBundle CDN 分发）的逆向 SOP，核心难点是"IL2CPP 方法体为空"和"资源要靠 CDN hash 映射下载"。

《魔法工艺》是完全不同的技术栈，因此本报告**没有照搬**该文档的流程，而是采用了更适合本项目的方法：

| 维度 | 参考文档中的微信小游戏 | 《魔法工艺》实际情况 |
| --- | --- | --- |
| 脚本后端 | IL2CPP（方法体反编译为空） | **Mono**（`Assembly-CSharp.dll` 可完整反编译出可读 C# 源码，无需 IL2CPP dump 工具） |
| 引擎架构 | 传统 GameObject/MonoBehaviour | **Unity DOTS/ECS**（`Unity.Entities`），法术用 Authoring/Baker 模式烘焙 |
| 资源分发 | AssetBundle + CDN + hash 轮转 | 本地 `ContentArchives`（新版 Scriptable Build Pipeline）+ 标准 `resources.assets`，**全部在本地安装包内，不涉及抓包/CDN** |
| 数值配置存储 | 加密配置表 / TypeTree 混淆 | **明文 JSON**，作为 `TextAsset` 存放在 `Resources/Configs/` 下，可直接反序列化 |
| 热更新 | 无对应机制 | 集成了网易 **IFix** 热更新框架，但**实测当前未激活**（见第 10 节） |

结论：本项目不需要 IL2CPP 逆向工具、不需要处理纹理压缩格式、不需要抓包/CDN 分析，核心工作是"反编译 + 配置表提取 + 交叉引用"，比参考文档描述的场景简单得多。

---

## 1. 技术架构总览

- 引擎：Unity（较新版本，模块列表含 `GraphicsStateCollectionSerializerModule`、`HotReloadModule` 等 2023+ 特性）。
- 脚本后端：**Mono**（非 IL2CPP）。证据：`Magicraft_Data/Managed/Assembly-CSharp.dll`（9.7MB）是标准 .NET 程序集，用 `ilspycmd` 反编译后得到 3403 个完整、非空的 `.cs` 源文件。
- 游戏逻辑框架：**Unity DOTS / ECS**（`Unity.Entities`, `Unity.Transforms`, `Unity.Physics`），法术效果大量以 `XxxAuthoring`（GameObject 端烘焙组件）+ `XxxSystem`（ECS System，纯逻辑）的模式实现。仅法术相关的 `.cs` 文件就有 **334 个 Spell 前缀的类文件**，每个数值 ID 对应的具体运行时行为（弹道形状、粒子、AI）都在独立的 `SpellXXXXAuthoring.cs` / `SpellXXXXSystem.cs` 中实现。
- 数值配置：**JSON 驱动**。所有可平衡的数值（伤害、冷却、消耗等）并不写死在代码里，而是从 `Resources/Configs/*.json`（作为 Unity `TextAsset`）反序列化进 `List<T>` / `Dictionary<int,T>` 静态字典。代码只负责"这个数值意味着什么效果"，具体数字全部数据驱动。
- 动画：Spine（`spine-unity.dll`）+ Rukhanka（GPU 动画）。
- 缓动：DOTween。
- 热更新：IFix（网易开源的 C# 热更框架），见第 10 节。
- 平台 SDK：Steamworks.NET，另有 bilibili 小游戏 SDK 残留（`bilibili.OpenBLive.dll`），推测游戏也发行于 B 站小游戏平台或做过相关接入测试。

---

## 2. 数据管线：数值配置从哪里来（证据链）

代码层面的调用链（均为反编译源码原文）：

```1:44:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\DataMgr.cs
public abstract class ConfigInitializer<T> : IConfigInitializer
{
    ...
    public virtual void ReadJson()
    {
        _json = GetConfigText<T>();
    }
    public virtual void Parse()
    {
        rawData = ParseJsonList<T>(_json);
        ApplyResult(rawData);
        ...
    }
}
```

`GetConfigText` 的实现：

```264:267:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\DataMgr.cs
public static string GetConfigText(string configName)
{
    return ABResources.LoadAsset<TextAsset>("Configs/" + configName).text;
}
```

而 `ABResources.LoadAsset` 底层就是标准 `Resources.Load`（并非加密 AssetBundle）：

```16:19:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\ABResources.cs
public static T LoadAsset<T>(string name) where T : UnityEngine.Object
{
    return Resources.Load<T>(name);
}
```

即每个形如 `SpellConfig`、`WandConfig`、`RelicConfig` 的类都对应一个同名的 `Resources/Configs/xxx.json` 明文 TextAsset。这些 TextAsset 被 Unity 打包进了游戏根目录的 `Magicraft_Data/resources.assets`（260MB）里。

**提取方法**：用 Python + [UnityPy](https://github.com/K0lb3/UnityPy) 直接加载 `resources.assets`（Mono 项目，TextAsset 是引擎内置类型，无需程序集 TypeTree 即可正确解析），逐个读出 `m_Name` 和 `m_Script` 字段导出为 `.json` 文件。全程无需 AssetRipper 这种重量级工具，脚本见 `_reverse_engineering/tools/extract_configs.py`。

已确认并提取的配置文件（均为纯 JSON，人类可读）：

| 配置类 | 记录数 | 内容 |
| --- | --- | --- |
| `SpellConfig` | 334 | **法术数值主表**（本报告核心） |
| `TextConfig_Spell` | — | 法术多语言名称/描述文本 |
| `WandConfig` | 136 | 法杖基础属性 + 出厂符文槽位布局 |
| `TextConfig_Wand` | — | 法杖名称/描述 |
| `RelicConfig` | 96 | 遗物（被动道具） |
| `CurseConfig` | 61 | 诅咒（Roguelite 负面/双刃效果） |
| `SetConfig` | 11 | 遗物套装 |
| `RelicGroupConfig` | 5 | 遗物分组 |
| `PotionConfig` | 34 | 药水 |
| `ResearchConfig` | 26 | 研究/科技树 |
| `UnitConfig` | — | 单位（含召唤物属性，如召唤物血量） |

---

## 3. 法术编号与能力类型（abilityType）体系

`SpellConfig` 每条记录的 `id` **不是随意编号**，而是遵循固定公式（来自 `SlotData.GetFinalLevelId`）：

```179:184:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\SlotData.cs
public int GetFinalLevelId(int id)
{
    int num = (int)SpellConfig.dic[id].abilityType * 10;
    int num2 = Mathf.Min(GetSlotSpellMaxLevel(id), id % 10 + slotSpellExtraLevel);
    return num + num2;
}
```

即：**`法术ID = abilityType(能力类型) × 10 + 等级(1/2/3...)`**。例如 `abilityType=1001`（Bullet 魔法弹）对应 `10011`(Lv1)/`10012`(Lv2)/`10013`(Lv3) 三条记录，是同一个法术的三个强化等级，而不是三个不同法术。

`SpellAbilityType` 枚举（`SpellAbilityType.cs`）把 139 种能力类型按数值区间分成明显的语义分组，这是理解整个法术系统的关键索引：

| ID 区间 | 分类 | 说明 | 对应 `useType` |
| --- | --- | --- | --- |
| 1000–1031 | **主动核心法术** | 魔法弹、滚球、激光、黑洞、龙息、超新星…每个都是玩家可主动释放的独立弹道/技能 | Missile |
| 2001–2009 | **召唤类法术** | 召唤物（法典书、融合头等），召唤后由独立 AI 行动 | Summon |
| 3001–3015 | **弹道修饰符** | 齐射/多重射击/穿透/闪电链/反弹/分裂/冰冻/寄生虫——插在主动法术前面，改变其弹道形态 | Enhance |
| 3101–3130 | **数值强化符文** | 攻击力/暴击/速度/范围强化、生命线、火焰水晶、灵魂伴侣等——插在主动法术前面，改变其数值 | Enhance/Passive |
| 3201–3205 | **触发器（扳机）** | 命中触发、移动触发、结束触发等——不直接产生效果，而是"条件满足后释放挂载的法术" | Enhance |
| 4001–4027 | **法杖核心机制符文** | 空的容器(+MP上限)、魔力精华(+回蓝)、法杖之魂、符文之锤、激光束、红/绿/蓝符文等——影响法杖整体运作方式 | 多为 Passive/Enhance |
| 9001–9065 | **通用弹道行为组件** | 抛物线、跳弹、长拖尾、正弦弹道等，多用于怪物弹幕或作为可复用的运动模块，不直接对应玩家可获得的法术条目 | — |

`SpellType`（`useType` 字段）只有 4 个大类：

```1:7:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\SpellType.cs
public enum SpellType
{
    Missile,
    Summon,
    Enhance,
    Passive
}
```

即游戏里所有"符文"最终只分四种插槽语义：主动弹幕、召唤、强化（修饰后面的主动/召唤法术）、被动（全局生效，不需要排列位置）。

---

## 4. `SpellConfig` 核心字段详解

完整字段列表（共 43 个字段，来自 `SpellConfig.cs`）及含义：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `id` | int | 法术ID，见第3节公式 |
| `priceCoin` / `priceHP` | int | 商店购买价格（金币/血量） |
| `icon` / `iconH` | string | 图标资源名（`iconH` 为节日/联动皮肤图标） |
| `level` | int | 强化等级，1/2/3，UI 上显示为 "+" "++" |
| `canCompound` | bool | 是否可通过合成升级（3个同名同级道具合成为下一级，见第8节） |
| `dropType` | enum `ItemDropType` | 稀有度：None(初始)/Common(普通)/Rare(稀有)/Epic(史诗)/Special(特殊) |
| `needActivate` | bool | 是否需要先激活（图鉴/研究解锁类） |
| `slotCost` | int | 占用法杖格子数（多数为1，部分大型法术占多格，见第5节） |
| `slotNumModifyValue` | int | 该法术使法杖总格数增减多少 |
| `useType` | enum `SpellType` | 见第3节 |
| `prefab` | string | 运行时预制体/弹道资源名 |
| `mpCost` | int | 单次施放蓝耗 |
| `shootCount` | int | 单次触发的发射次数 |
| `damage` | float | 基础伤害 |
| `isDPS` / `DPSDamageInterval` | bool / float | 是否为持续伤害（而非单次命中），及伤害结算间隔。**为真时 `damage` 的语义变成「每目标每秒伤害」**，单次伤害 = `damage × DPSDamageInterval`，见 6.3 |
| `isKeepCasting` / `canCancelCasting` | bool | 是否为"持续施法"类型（如激光），及能否中途取消 |
| `speed` / `duration` | float | 弹道飞行速度 / 存续时间 |
| `knockback` / `recoil` | float | 击退敌人力度 / 施法者受到的后坐力 |
| `criticalChance` | float | 额外暴击率加成（百分比） |
| `angle` | float | 散射角度（正数=增加散布，负数=收束） |
| `shootIntervalAddSubRevise` / `coolDownAddSubRevise` | float | 对法杖"射击间隔"/"整体冷却"的加减修正（秒） |
| `coolDownRatio` | float | 对法杖冷却的乘法修正（默认1.0） |
| `mpCostAddSubCorrection` / `mpCostMulDivCorrection` | float | 对蓝耗的加减/乘除修正（百分比） |
| `radius` | float | 效果半径（爆炸/范围类法术） |
| `upSpeed` / `gravity` | float | 上升初速度 / 重力（抛物线弹道用） |
| `summonID` / `summonLimit` | int | 召唤类专用：召唤的单位ID（对应 `UnitConfig`）与同屏数量上限 |
| `haveEffecforMissileSpell` / `haveEffecforSummonSpell` | bool | 该强化符文是否对主动/召唤类法术生效（用于过滤强化范围） |
| `isSplitSpell` | bool | 是否为"分裂"产生的子弹道（避免无限递归分裂） |
| `float1` / `float2` / `float3` / `int1` / `int2` / `int3` | 通用字段 | **含义随 `abilityType` 变化**，是最需要结合具体 Authoring/System 代码解读的字段（见下方示例） |
| `playShootSE` | bool | 是否播放施法音效 |

### 4.1 通用字段 (`float1-3`/`int1-3`) 的多态含义示例

由于篇幅原因，`SpellConfig` 没有为每个能力类型单独定义字段，而是复用 6 个通用槽位，其语义完全由 `abilityType` 决定。以下是从代码中提取到的实际映射证据（节选自 `SlotData.cs` 与 `CanShootSpellUtils.cs`）：

| abilityType | float1 | float2 | float3 | int1 | int2 | int3 |
| --- | --- | --- | --- | --- | --- | --- |
| `EnhanceAttackRatio`/`FireCrystal`/`OverDrive` | 伤害加算百分比(`/100`累加进 addRatio) | — | — | — | — | — |
| `Refraction` | — | 伤害乘算百分比(`/100`累乘进 mulRatio) | — | — | — | — |
| `PowerSavingMode` | — | — | 伤害乘算百分比 | — | — | — |
| `ManaCoin`（魔力金币） | 金币兑换比例 | 每单位伤害加成 | — | — | — | — |
| `LightningChain`（闪电链） | — | — | — | 每跳伤害 | — | — |
| `LifeLine`（生命线） | 每次伤害基数 | 触发倍率/间隔关联值 | — | — | — | — |
| `DeathAdder`（死亡毒蛇） | — | — | — | 触发所需层数上限 | — | — |
| `ManaTendril`（魔力卷须） | 每格强化百分比 | — | — | — | — | — |
| `DimensionTraveller` | — | — | 传送/描述用数值 | — | — | — |
| `OnHitTrigger`（命中触发器） | — | — | — | — | 触发概率分母(`1/int2`) | — |
| `ArcaneExplosion`（奥爆术） | **配置有值但 ECS 从不读取**（文案称「连锁爆炸半径倍率」，仅 Mono 残留轨 `Spell1008ArcaneExplosion.cs` L209 使用） | — | — | 击杀连锁开关，非 0 即在死者位置免费再生成一发（`SpellTakeDamageResultSystem.cs` L282） | — | — |

> **`ArcaneExplosion` 是本节方法论的反面教材，值得单独记一笔**：它的 `float1` 在配置里有值（Lv2 = 0.7、Lv3 = 1.0）、在游戏内机制文案里被明文引用（「生成一次 float1 倍半径的奥爆术」），却在 ECS 实现里完全没有落地——`BuildArcaneExplosion`（`SpellSpawnParamsStorage.cs` L235–243）只改生成位置，不做任何半径运算。仅凭配置数据或游戏文案反推语义会得到错误结论，必须回到代码确认。完整分析见《奥爆术逆向分析.md》4.3。

> 说明：该表并非穷举全部 139 种能力类型（工作量巨大），而是给出方法论证据——**任何一个 `float1-3`/`int1-3` 数值的具体含义，都必须找到对应的 `Spell{ID}xxxAuthoring.cs` 或使用它的 `CanShootSpellUtils`/`SlotData` 代码分支来确认，不能只看数字猜测**。完整的 334 条法术数值见附件《法术数值总表.md》，其中每条记录都列出了非零的 float/int 字段原始值，供按 `abilityType` 交叉查阅对应源码文件（源码文件名规则为 `Spell{id/10}XxxAuthoring.cs`，如 `abilityType=1007`(黑洞) 对应 `Spell1007BlackHoleAuthoring.cs`）。

---

## 5. 法杖（Wand）机制

### 5.1 双层插槽结构

`WandConfig.cs` 显示每把法杖有两组独立插槽：

- **`normalSlots`（普通槽）**：法术按顺序循环释放的主插槽序列。前面放置的 `Enhance` 类符文会对**同一序列中后面**的第一个 `Missile`/`Summon` 类法术生效（见第6节公式），这是"魔杖搭配"策略的核心。
- **`postSlots`（蓄能槽 / "终极槽"）**：独立的第二组插槽，不参与常规循环释放，而是通过**蓄能条**充满后整体触发一次。

蓄能条有 9 种互斥的充能方式（`WandPostSlotTriggerType`），由法杖自身固定其中一种：

```1:13:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\WandPostSlotTriggerType.cs
public enum WandPostSlotTriggerType
{
    KillEnemy, MoveDistance, SpellHit, Stand, CastSpell,
    HighDamage, CriticalHit, TakeDamage, Time, Mix
}
```

对应 `WandConfig` 中的 9 个 `Postslot*ChargeRatio` 字段（击杀怪物/移动距离/法术命中/站立不动/释放法术/造成高额伤害/暴击/受到伤害/时间流逝，每单位事件贡献多少充能）。`Mix` 表示同时存在多种充能来源。

存在专门的"蓄能槽延伸器"符文家族（`PostSlotExtenderMove/Stand/Time/CastSpell`，abilityType 4015-4018）可以把普通槽里获得的充能规则**转移/追加**到蓄能槽上，这是构筑深度的来源之一。

### 5.2 法杖基础属性（`WandConfig` 字段）

| 字段 | 含义 |
| --- | --- |
| `shootInterval` | 单次射击后到下一次可射击的最小间隔 |
| `coolDown` | 循环走完一遍所有普通槽后的额外整体冷却 |
| `angle` | 基础散射角度 |
| `maxMP` / `mpRecovery` | 法力上限 / 每秒回复 |
| `shootCount` | 法杖自身叠加的额外发射次数 |
| `costCorrection` | 全局蓝耗百分比修正（100=不修正） |
| `criticalChance` | 法杖自带暴击率加成 |
| `damageCorrection` | 全局伤害百分比修正（100=不修正），进入第6节公式的 `mulRatio` |
| `specialAbility` | 枚举 `WandAbility`（30种法杖专属被动，如“击退力提升”“友伤降低”“持杖角色可飞行”“四方向齐射”“周期性回满蓝”等，见 `WandAbility.cs`） |
| `normalSlotIsLock`/`postSlotIsLock` | 出厂时哪些格子被锁定（需要花费资源解锁） |

**实例证据**（1号法杖"魔杖"，`WandConfig.json` 第一条记录）：`normalSlots = [null, null, 魔法弹(10011), 魔法弹(10011), 魔法弹(10011)]`，5 格中前两格为空、后三格固定装填 3 发 1 级魔法弹——这正是游戏最初始的新手法杖。

---

## 6. 核心数值公式

### 6.1 通用数值容器 `RatioValue`

游戏所有会被多个来源加成的数值（伤害、暴击、范围、蓄能效率等）都通过统一的 `RatioValue` 类型计算，而不是零散的临时变量相加，这保证了数值可预测、可调试：

```36:36:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\RatioValue.cs
public double ResultDouble => (BaseValue + addBase) * baseAddRatio * addRatio * mulRatio + addExtra;
```

即最终数值 = **(基础值 + 加算基础值) × 基础倍率 × 加算倍率 × 乘算倍率 + 额外加值**。`AddRatio()` 修改 `addRatio`（多个来源相加），`MulRatio()` 修改 `mulRatio`（多个来源相乘），因此系统天然区分"百分比叠加"和"百分比连乘"两种增强方式。

### 6.2 法术伤害的最终公式

以主动法术伤害为例（`CanShootSpellUtils.cs`）：

```7:33:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\CanShootSpellUtils.cs
public static RatioValue GetDamage(float baseDamage, IEnumerable<SpellConfig> enhances, SpellConfig stopTarget)
{
    RatioValue ratioValue = new RatioValue(baseDamage);
    foreach (SpellConfig enhance in enhances)
    {
        if (enhance != stopTarget)
        {
            switch (enhance.abilityType)
            {
            case SpellAbilityType.EnhanceAttackRatio:
            case SpellAbilityType.FireCrystal:
            case SpellAbilityType.OverDrive:
                ratioValue.AddRatio(enhance.float1 / 100f);
                break;
            case SpellAbilityType.Refraction:
                ratioValue.MulRatio(enhance.float2 / 100f);
                break;
            case SpellAbilityType.PowerSavingMode:
                ratioValue.MulRatio(enhance.float3 / 100f);
                break;
            }
            continue;
        }
        return ratioValue;
    }
    return ratioValue;
}
```

再叠加玩家全局加成与法杖自身修正：

```35:40:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\CanShootSpellUtils.cs
public static RatioValue GetDamage_FinalPlayerValue(float baseDamage, SpellConfig[] enhances, SpellConfig stopTarget, Wand wand)
{
    RatioValue damage = GetDamage(baseDamage, enhances, stopTarget);
    damage.AddRatio(PlayerMgr.Inst.ExtraDamageRatio).MulRatio(wand.passiveDamageRatio).MulRatio(wand.WandCfg.damageCorrection / 100f);
    return damage;
}
```

**最终伤害公式**：

```
最终伤害 = 法术基础伤害
         × (1 + Σ强化符文加算百分比 + 玩家全局加算伤害率)
         × Π 强化符文乘算百分比（折射/节能模式等）
         × 法杖遗物乘算伤害率(passiveDamageRatio)
         × 法杖自身伤害修正(damageCorrection / 100)
```

其中"强化符文"只统计**同一段插槽序列**（同一把法杖 `normalSlots` 内、目标法术之前，或同属于 `postSlots`）中位于目标法术**之前**的 Enhance 类符文——这解释了为什么游戏里"符文摆放顺序"至关重要：强化符文必须排在要增强的主动法术左侧才会生效。

同样的模式被复用于闪电链伤害、生命线伤害等多个特殊机制（见 `CanShootSpellUtils.GetLightningChainDamage_FinalPlayerValue` / `GetLifeLineDamage_FinalPlayerValue`），均是"遍历同段 Enhance 列表 → 按 abilityType 分支累加/累乘 → 套用统一 RatioValue 结算"的同一套架构。

### 6.3 `isDPS` 持续伤害模型

上面 6.2 的公式算出的是"这发法术的最终伤害基数"。对 `isDPS = true` 的法术，这个基数**不是单次命中伤害，而是每目标每秒伤害**——中间还夹着一层由 `DPSDamageInterval` 驱动的折算。详细展开见《落雷阵逆向分析.md》第 2.3 节，此处只立通用管线。

**（1）配置 → 组件。** `isDPS` 只决定 `DamageInterval` 是否非 0；为 false 时该字段恒为 0，配置里的 `DPSDamageInterval` 写了也不生效：

```286:286:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\ShootSpellUtils.cs
			DamageInterval = (configCopy.isDPS ? configCopy.DPSDamageInterval : 0f),
```

**（2）组件 → 单次伤害。** `TakeDamageInfo_Dots.NewInfo` 按 `DamageInterval > 0` 分支，非零时把 6.2 算出的 `Damage.Calculate()` 再乘一次间隔：

```153:153:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\TakeDamageInfo_Dots.cs
		info.damage = ((config.DamageInterval > 0f) ? (config.Damage.Calculate() * config.DamageInterval) : config.Damage.Calculate());
```

即 **单次实际伤害 = 每秒伤害 × 结算间隔**。落雷阵（`1018`）Lv3 表值 `damage = 88`、`DPSDamageInterval = 0.25`，一次结算实打 `88 × 0.25 = 22`，一秒四次正好回到 88。

**（3）面板文案。** `isDPS` 唯一的 UI 作用是给伤害数字挂一个"(秒伤)"后缀（链接文本 `14000302`，见 `SlotData.cs` L384–388 与 `SpellConfig.cs` L281–287），不参与任何数值计算。

**（4）蓝耗与 `isDPS` 无关。** `GetSpellManaCost`（`SpellShootData.cs` L614–644）完全不读它，`mpCost` 是施放时一次性扣掉的。这让 DPS 类法术的蓝耗效率完全由"它能打到多少目标 × 多少次"决定，是全表方差最大的一类——同一个落雷阵打 Boss 与打七只小怪，伤害/蓝比相差 4 倍以上。

**（5）ECS 不做取整。** Mono 残留轨的 `MakeDamage` 对 DPS 伤害做了 `Mathf.Ceil`（`Spell1018ThunderAura.cs` L214），上面的 ECS `NewInfo` 是纯浮点乘法。低伤害档位下两条轨会有几点差异，玩家主路径以 ECS 为准。

**（6）专属 Job 可以绕过这层折算。** `Spell1018FallJob.TryAttack` 显式覆写 `damage.damage = config.Damage.Calculate()`（`Spell1018ThunderAuraSystem.cs` L772），跳过 `× DamageInterval` 打满额伤害。读某个 DPS 法术的伤害时，必须确认它的专属 Job 有没有类似覆写。

**（7）派生结论。** 对 `isDPS` 且靠 `duration` 计时的法术，"总伤 = 每秒伤害 × 时长"，于是：

- **时长强化收益严格线性**（每多 1 秒就多 4 次结算，没有飞行段/减速段这类固定开销要摊）；
- **节能模式是双重打折**——它的 `float3` 乘伤害、`float2` 乘时长，对 DPS 法术两个折扣相乘（Lv1 `0.75 × 0.75 = 0.5625`），而蓝耗只打到 0.6，效率比 0.94 属于负收益；对一次性飞弹则只砍伤害，效率比 1.25。

落雷阵 `1018` 是本套文档第一个完整逆向的 `isDPS` 法术。后续 DPS 类法术（高压水流 `1019`、炽烈龙息 `1025`、棱镜核心 `4014` 等）直接引用本节，不再重复推导。

---

## 7. 法术生命周期（ECS System Group 执行顺序）

法术从"生成"到"结算"的整个 ECS 更新顺序（通过各 `XxxSystemGroup.cs` 的 `[UpdateInGroup]`/`[UpdateBefore]`/`[UpdateAfter]` 特性还原）：

```
SimulationSystemGroup（Unity 标准仿真组）
└─ SpellSimulationSystemGroup
   ├─ SpellCreateSystemGroup      [OrderFirst]   —— 法术生成/初始化（按法杖槽位数据创建 Entity）
   ├─ SpacialSpellSystemGroup                    —— 空间/位置相关处理
   ├─ EnhanceSpellSystemGroup     [UpdateAfter SpacialSpellSystemGroup]
   │                                             —— 强化符文对本次施放的加成结算（对应第6节公式）
   └─ SpellEffectSystemGroup      [UpdateAfter EnhanceSpellSystemGroup]
                                                 —— 具体效果表现（各 SpellXXXXEffectSystem/HitTriggerSystem 等）
FixedStepSimulationSystemGroup（定步长物理组）
└─ SpellPhysicsSystemGroup       [UpdateBefore EndFixedStepSimulationEntityCommandBufferSystem]
                                                 —— 弹道移动、碰撞检测
SimulationSystemGroup 末尾
└─ SpellEndSystemGroup           [OrderLast]     —— 法术销毁、命中后处理、掉落结算等收尾
```

这套顺序保证了：先确定"这次施放叠加了哪些强化"，再计算效果数值，最后物理系统按固定步长处理运动与碰撞，全部结束后统一清理。

---

## 8. 法术分级与合成系统

- 每个法术在设计上通常有 **1/2/3 三个等级**（`level` 字段，对应 ID 个位数 1/2/3），高等级在数值上全面强于低等级（伤害、蓝耗、有时候还解锁新的 `int/float` 效果分支，如 10011→10012 新增了 int1/int2 字段生效）。
- `canCompound` 字段控制该法术能否通过合成升级；UI 层 `UICompound.cs` 实现"消耗同名同级材料合成为下一级"的玩法（经典 roguelite 三消升级机制）。
- 稀有度 `dropType`（None/Common/Rare/Epic/Special）同时影响商店出现概率和 UI 显示颜色，与"等级"是两个独立维度（稀有度决定这个法术设计上多强/多稀有，等级决定具体这一件的强化次数）。

---

## 9. 相关系统速览（与法术直接联动的部分）

法术系统不是孤立的，以下周边系统会直接修改法术数值，逆向时必须一并考虑：

- **遗物 (`RelicConfig`，96条)**：每个遗物含 `int1`/`int2` 等字段，但结构是 `{ "value": x, "valueUpgrade": y }`（可通过研究/升级追加数值，而非固定值）。关键发现：遗物带有 `RedRunePoint`/`GreenRunePoint`/`BlueRunePoint` 三个"符文点数"字段，与法术体系里的 `RedRune`/`GreenRune`/`BlueRune`（abilityType 4025-4027）三个法杖核心符文直接对应——这是一套独立的"三色遗物点数"构筑体系，装备的遗物越偏向某一颜色，对应颜色符文的强度越高。代码中 `PlayerMgr.Inst.ItemCtrller.relic_XXX` 命名的字段（如 `relic_PowerfulHarpoonHead`、`relic_FlameHarpoonHead`）显示部分遗物是与**特定法术绑定**的专属强化（鱼叉类法术伤害加成等）。
- **诅咒 (`CurseConfig`，61条)**：Roguelite 双刃增益，例如代码中出现的 `curseCfg_ScatterAdd` 会直接叠加到法杖的散射角度上（`WandConfig.GetWandScatter()`）。
- **套装 (`SetConfig`，11条)** 与 **遗物分组 (`RelicGroupConfig`，5条)**：收集特定遗物组合触发额外套装效果（Build-around 机制）。
- **药水 (`PotionConfig`，34条)** / **研究 (`ResearchConfig`，26条)**：局外成长/局内消耗品，部分数值同样通过 `valueUpgrade` 模式随研究等级提升。

---

## 10. IFix 热更新调查结果

`Magicraft_Data/Managed/IFix.Core.dll`（网易开源 C# 热更新框架）确实存在于安装包中，`HotFix.cs` / `HotFixBundle.cs` 显示了完整的热更新加载逻辑：

```73:106:D:\SteamLibrary\steamapps\common\Magicraft\_reverse_engineering\decompiled\Assembly-CSharp\HotFixBundle.cs
public static IEnumerator InitAsync()
{
    ...
    string path = Path.Combine(Application.persistentDataPath, "hotfix");
    if (Directory.Exists(path)) { ... 加载热更 AssetBundle + IFix "fix" 补丁文件 ... }
    ...
}
```

**关键结论：热更新目录不在安装包内，而是运行时下载到 `Application.persistentDataPath/hotfix/`**（Windows 上对应 `%USERPROFILE%\AppData\LocalLow\bolang\Magicraft\hotfix\`）。经实测检查该目录（游戏正在运行、已有存档的情况下），**`hotfix` 子目录不存在**：

```
C:\Users\GuoFeng\AppData\LocalLow\bolang\Magicraft\ 下仅有：
Logs / MobileCloudSaveData / BackupData1-3 / FinishBuilds.Json /
Player.log / Player-prev.log / SettingData.Json / WorldData0-2.Json /
steam_autocloud.vdf
（无 hotfix 目录）
```

**结论**：当前游戏版本**没有激活任何热更新补丁**，`Assembly-CSharp.dll` 反编译出的代码就是当前实际运行的完整、真实的游戏逻辑，不存在"关键法术逻辑被热更脚本覆盖、静态反编译看不到"的风险。这是符合"能解出来就解，解不出来不强求"（best effort）预期的一个干净结论——因为根本不需要额外解密，静态反编译已经是完整真相。

---

## 11. 完整法术数值表

334 条法术记录的完整数值（ID、双语名称、能力类型、等级、稀有度、伤害、蓝耗、连发数、速度、持续时间、暴击率、半径、冷却修正、以及所有非零的 float/int 扩展字段），已按 `useType`（主动弹幕/召唤/强化/被动）分类整理为独立附件，避免本文档过长：

**→ 详见同目录下 [`法术数值总表.md`](./法术数值总表.md)**

该表由脚本 `_reverse_engineering/tools/build_spell_table.py` 从 `SpellConfig.json` + `TextConfig_Spell.json` 自动生成，任何游戏更新后只需重新运行该脚本（先用 `extract_configs.py` 重新导出最新的 `resources.assets`）即可得到最新数值，保持长期可维护。

---

## 12. 证据与产物清单

按照"保留中间产物、每个结论关联证据"的原则，本次分析在 `_reverse_engineering/` 目录下留存了完整的可追溯产物：

```
_reverse_engineering/
├── 魔法工艺法术系统逆向报告.md   本文档
├── 法术数值总表.md              334 条法术完整数值附表
├── {法术名}逆向分析.md / .html   单法术 / 单符文深度分析（蝴蝶、激光、落雷阵、奥爆术、闪电疾行…）
│                                每篇含数值结构、核心机制代码级拆解、增强符文交互矩阵、证据索引
├── 法术管线流程图.html          可交互流程图：施法管线 / 槽位解析算法 / 帧内 System 时序 / 触发器与派生 / 数值链（点击节点看证据行号）
├── 魔法工艺完整配置表.xlsx      全部配置表按类型分工作表导出
├── 法术能力类型字段语义表.xlsx  139 种 abilityType 的 float1-3 / int1-3 语义穷举
├── decompiled/
│   ├── Assembly-CSharp/          ilspycmd 反编译出的 3403 个 .cs 源文件（含项目文件，可用 Rider/VS 直接打开浏览）
│   └── Assembly-CSharp-firstpass/
├── assets_export/
│   ├── textasset_names.txt       resources.assets 内全部 119 个 TextAsset 名称清单
│   ├── SpellConfig.json          法术数值主表原始 JSON（334条）
│   ├── WandConfig.json           法杖配置原始 JSON（136条）
│   ├── RelicConfig.json / CurseConfig.json / SetConfig.json /
│   │   RelicGroupConfig.json / PotionConfig.json / ResearchConfig.json /
│   │   SpecialObjConfig.json     其余原始 JSON 配置
│   └── TextConfig_*.json         对应的多语言文本表
└── tools/
    ├── extract_configs.py        从 resources.assets 提取任意 TextAsset 的通用脚本
    ├── build_spell_table.py      生成法术数值总表的脚本
    ├── export_configs_xlsx.py    导出多工作表 Excel 配置表（含文本 ID 偏移自动探测）
    └── build_abilitytype_semantics_xlsx.py  生成 abilityType 字段语义表
```

使用的工具链：
- `ilspycmd` 7.2.1（.NET 反编译器，通过 `dotnet tool install -g` 安装，因本机 .NET SDK 为 6.0，选用兼容的 7.2.1 版本而非最新的 10.x）
- `UnityPy` 1.25.2（Python，用于解析 Unity 序列化文件并导出 TextAsset）
- Python 3.12

---

## 13. 局限性与后续建议

1. **`abilityType` 字段语义的置信度分层**：139 种 `abilityType` 的 `float1-3` / `int1-3` 已在 `法术能力类型字段语义表.xlsx` 中穷举，但每行都带置信度标记——有直接代码引用的为高置信度，仅由配置数据规律反推的为低置信度，使用前请先看该列。另有相当数量的字段确认为模板残留（配置里有值但代码从不读取），例如 `FollowOwner(3114)` 的 `float2 / int1 / int2`。
2. **本报告聚焦"设计态"数值**（配置表里的原始数字），未覆盖"运行态"效果（例如粒子特效参数、Spine 动画绑定），如需要可以进一步用 UnityPy 导出 `sharedassets*.assets` 里的 Prefab/贴图资源。
3. **未涉及关卡/房间生成、AI、成就等非法术系统**，如有需要可以用同样的方法论（找到对应的 `XxxConfig.cs` + `Resources/Configs/Xxx.json`）继续扩展分析。
4. 若游戏后续更新触发了热更新（`persistentDataPath/hotfix` 目录出现文件），第10节的"无热更"结论需要重新验证，届时可用 `HotFix.DCSDecrypt`（DES，密钥硬编码为字符串 `"hotfix-key"` 的 UTF8 字节，见 `HotFix.cs`）解密 `version`/`index` 文件，再判断 IFix 补丁（`fix` 文件）是否修改了法术相关逻辑。
