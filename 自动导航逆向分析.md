# 自动导航（Spell3011 / FollowTarget）逆向分析

> 仅玩家法术增强符文「自动导航」（`30111–30113`）。槽位解析与《伤害强化》《范围增强》同一套，本文不复述。
> 公转 / 寻踪 / 自我追踪 / 自由公转只写互斥与选取，不展开各自运动。
> 方法：`SpellConfig.json` + 反编译交叉验证。未标注「推测」的结论均有代码/数据直接证据。
> 第 6 节回旋镖行已按《回旋之刃逆向分析.md》补全：33% 只覆盖 ChaseEnemy，Normal 从第 0 帧弯回。

---

## 1. 它是什么

插在主动/召唤**左侧**的轨道符文。把法术运动类型改成 `ChaseEnemy`，按 `float1` °/米去转朝向。不改伤害、半径、速度、蓝耗、飞行寿命。


| 项 | 值 |
| --- | --- |
| 英文名 | Automatic Navigation |
| 内部名 | `FollowTarget` 跟踪目标 |
| `abilityType` | `3011` / `SpellAbilityType.FollowTarget` |
| 配置 ID | `30111` / `30112` / `30113` |
| 稀有度 / 类型 | 稀有（`dropType=2`），`useType=2`（Enhance），1 槽，0 蓝 |
| 合成 / 售价 | Lv1→2→3；**20 / 60 / 180** 金（×3，比伤害强化贵） |
| 作用标记 | `haveEffecforMissileSpell=true`，`haveEffecforSummonSpell=false`（运行时**不读**这两个字段） |


文案（`7130111–7130113`，三级同一句）：

> 法术以 **float1**° 每米的角速度跟踪目标

底部补充 `7230111`：

> 轨道类强化，不同轨道类强化同时存在时会随机选取。

**机制一句话**：每飞 1 米，朝锁定目标最多转 `float1` 度。转多少跟速度无关，跟走过的路程有关。和公转/寻踪等轨道类型同时存在时，**只生效最靠右的一种**；卡面「随机」和代码不一致。

---

## 2. 数值

### 2.1 配置表（`SpellConfig.json`）


| ID | Lv | float1 °/米 | 售价 | 可合成 |
| --- | --- | --- | --- | --- |
| 30111 | 1 | **12** | 20 | true |
| 30112 | 2 | **22** | 60 | true |
| 30113 | 3 | **38** | 180 | false |


`float2/3`、`int1/2/3` 全 0，不用。等级不是「每级 ×2」：12 → 22 → 38。

### 2.2 字段语义


| 字段 | 语义 | 运行时 |
| --- | --- | --- |
| **float1** | 角速度，单位 °/米 | 同卡多张 **相加**，写入 `ChaseRotateSpeed` / `spellFollowTargetRotateSpeed` |
| float2/3、int1/2/3 | 未使用 | 配置恒 0，无引用 |


没有独立 `Spell3011*System` / Authoring。这张卡本身不挂伤害组件，只改发射包里的运动类型和角速度。

---

## 3. 怎么叠

### 3.1 同卡：加算 °/米，不是取 max

```550:556:decompiled/Assembly-CSharp/SpellShootData.cs
	public float GetChaseEnemyRotateSpeed()
	{
		return (from e in EnhanceList
			select e.GetFinalConfig() into e
			where e.abilityType == SpellAbilityType.FollowTarget
			select e).Sum((SpellConfig e) => e.float1);
	}
```

Mono 两侧同样是 `spellFollowTargetRotateSpeed += float1`（`SpellBase` 约 1254、2182 行）。

```
1×Lv2 = 22°/m，2×Lv1 = 24°/m   （等级不能互替）
1×Lv3 = 38°/m，Lv1+Lv2 = 34°/m
```

合成省槽，但单点效率不是「一张高级 = 两张低级」。手摆可以叠多张。自动填槽把整组轨道卡互斥，**不会**再塞第二张自动导航，也不会和公转/寻踪/自我追踪并排（见 3.2）。

魔能升阶走 `GetFinalConfig()`，抬的是这张卡的 `float1`，叠加规则不变。

### 3.2 不同轨道：右优先，不是随机

运行时轨道集合（`GetSpellMovementType`）：


| abilityType | 卡 | 运动类型 |
| --- | --- | --- |
| `3009` AroundOwner | 公转 | `Rotation` |
| `3010` AroundMouse | 寻踪 | `ChaseMouse` |
| `3011` FollowTarget | 自动导航 | `ChaseEnemy` |
| `3114` FollowOwner | 自我追踪 | `ChaseOwner` |
| `3117` RandomRotationRadiu | 自由公转 | `Rotation` |


```99:117:decompiled/Assembly-CSharp/SpellShootDataExtend.cs
	public static SpellSpecialMovementType GetSpellMovementType(this SpellShootData self, Wand wand)
	{
		for (int num = self.EnhanceList.Length - 1; num >= 0; num--)
		{
			switch (self.EnhanceList[num].GetFinalConfig().abilityType)
			{
			case SpellAbilityType.AroundOwner:
			case SpellAbilityType.RandomRotationRadiu:
				return SpellSpecialMovementType.Rotation;
			case SpellAbilityType.AroundMouse:
				return SpellSpecialMovementType.ChaseMouse;
			case SpellAbilityType.FollowTarget:
				return SpellSpecialMovementType.ChaseEnemy;
			case SpellAbilityType.FollowOwner:
				return SpellSpecialMovementType.ChaseOwner;
			}
		}
		return SpellSpecialMovementType.Normal;
	}
```

从强化列表**右往左**取第一种轨道类型。Mono V2 从左往右写 `currentSpellMovement`，最后一次覆盖，结果同样是最靠右的赢。

`ApplyShootGroupV2EnhanceEffect` 里有个 `ListShuffle` 再取 `[0]`，但那个 `list` **从未 Add**，是死代码。卡面简中/英文写「随机选取」；繁中写「會覆蓋之前的彈道」，和代码一致。

```
[公转] [导航] [弹]     走 ChaseEnemy，角速度用导航
[导航] [公转] [弹]     走 Rotation，导航的 °/米作废
[导航Lv1] [导航Lv2] [弹]   仍是 ChaseEnemy，角速度 12+22=34
```

ECS 写入角速度时按**赢了的类型**取对应求和：`ChaseEnemy` 只加 `FollowTarget`，`ChaseOwner` 只加 `FollowOwner`。输了的那种不进 `ChaseRotateSpeed`。

自动填槽 `RepelSpellGroup`：`FollowOwner / FollowTarget / AroundOwner / AroundMouse` 四张一组，同一段槽位最多放一张。`自由公转` 不在这组里。手摆不受这个限制。

---

## 4. 转向（核心）

### 4.1 公式：°/米，不是 °/秒

Mono：

```1305:1316:decompiled/Assembly-CSharp/SpellBase.cs
	protected virtual void SpellFollowTarget()
	{
		if (SpellFollowHaveTarget)
		{
			Direction = Tool2D.DirMoveTowards(Direction, ToPointDir(spellFollowTargetPpt.transform), CurrentSpeed * spellFollowTargetRotateSpeed * Time.deltaTime);
			rigid.linearVelocity = Direction * CurrentSpeed;
		}
		else
		{
			spellFollowTargetPpt = GetMiniMalAngleTargetablePpt();
		}
	}
```

ECS 通用弹（`SpellMoveJob.Normal_ChaseTarget`）同一套：

```
maxDelta = Speed * ChaseRotateSpeed * DeltaTime
Direction = DirMoveTowards(Direction, 指向目标, maxDelta)
```

`Tool2D.DirMoveTowards` 走 `Mathf.MoveTowardsAngle`（度）；`DTool.DirMoveTowards` 先 `math.radians(maxDelta)` 再转。两边都把 `float1` 当**度**。

`Speed * dt` 是本帧位移（米），乘 `float1` 就是本帧最多转的度。速度变快，每秒转得更多，但每米仍是 12°。转弯半径只由 `float1` 决定：

```
R ≈ 180 / (π × float1) 米
Lv1 ≈ 4.77 m，Lv2 ≈ 2.60 m，Lv3 ≈ 1.51 m
```

本段寿命能转的总角度 ≈ `float1 × 速度 × Duration`。自动导航**不加** `Duration`（`GetSpellDuration` 只给时长强化、省电、寻踪、自由公转加秒）。短寿命弹要转够，得靠叠角速度、加时长或加速度把路程拉长。

### 4.2 锁敌：最小夹角，粘住

不是最近距离。在房间可锁定单位里，取与**当前朝向**夹角最小的那个。

- 发射时：Mono 在 `rotateSpeed > 0` 时立刻 `GetMiniMalAngleTargetablePpt()`；ECS 的 `ChaseTarget` 初始为空，第一帧再 `FindMinAngleTarget`。
- 目标还活着且 `CanBeTarget`：一直跟它，**不会**因为旁边出现夹角更小的而换目标。
- 目标没了 / 不可锁定：当帧按原方向飞，并重新取最小夹角。没有目标就走直线（ECS `Normal_Common`）。
- 默认不穿墙检测。墙后的怪也能锁，然后往墙那边转。
- 玩家弹锁敌对；怪物弹锁玩家。ECS 额外排除 `NotAttack`、`Brittleness`。

`SpellFollowHaveTarget`：引用非空、物体还在、且 `CanBeTarget`。

### 4.3 什么时候不转

`isFlyFinish` / 悬停后速度清零：位移是 0，转角也是 0。悬停锁位置，跟踪停。

`enableFollowTarget == false` 时，通用 `SpellFollowTarget` 不跑（见第 6 节）。

---

## 5. 谁吃得到

和伤害强化一样：只强化**自己右边、同一段**里的主动/召唤；主槽加不到后槽。

```
[导] [弹]           弹跟踪
[弹] [导]           吃不到
[导] [奥爆] [弹]     两张都吃（奥爆自己常关掉通用跟踪，见下）
[导] [召唤]         运行时会写入 ChaseEnemy（表标了召唤无效，但不读）
```

配置 `haveEffecforSummonSpell=false`，自动填槽的 `Filter_SummonOnlyEnhance` 只看 `haveEffecforMissileSpell`。自动导航的导弹标记是 true，所以自动填也会给召唤配。部分召唤物会抄召唤者的 `spellFollowTargetRotateSpeed` / `ChaseRotateSpeed`。

自动填槽：不在 `IgnoreSpells`。流类型避让表里，流星/火球/死亡蝰蛇只避开自我追踪和范围增强，**不避开**自动导航。`SelfRepelSpells` 只有元素晶石。

---

## 6. 谁跳过通用路径

点名，不拆 Job。复刻通用管理器时不要假设「所有法术都走 4.1」。

Init 关掉 `enableFollowTarget`（不走 `SpellBase.SpellFollowTarget`）：

| 法术 | 说明 |
| --- | --- |
| 激光 `1004`、分解射线 `1011`、高压水枪 `1019` | 自己画线/喷流。**注意激光不等于「导航无效」**：它有专属 `ChaseEnemy` 线生成器，见本节末 |
| 奥爆 `1008` | 非坠落关掉；坠落才开 |
| 滚球 `1002`、诡雷 `1012`、巨型泡泡 `1024` | 关掉 |
| 死亡蝰蛇 `1017`、落雷阵 `1018` | 关掉 |
| 龙息 `1025`、超新星 `1027`、宾箭 `1028` | 阶段开关 |
| 审判之刃 `1023`、符文锤 `4013`、激光晶石 `4014` | 自管朝向 |
| 融合召唤 `3115`、召唤1 `2001` | 关掉 |

两处更正：`1018` 本文原写作「雷光环」，那是 `ThunderAura` 的直译，卡面中文名是**落雷阵**（`TextConfig_Spell` `7010181`），已改。另外这张表是 **Mono 侧**的 `enableFollowTarget` 开关，ECS 主路径不读它——落雷阵在 ECS 侧并没有被关掉，而是落到通用分支后因 `Speed = 0` 空转，见本节第四类。

ECS `SpellMoveJob` 在 `ChaseEnemy` 下另有分支（公式系数不同，仍读同一份 `ChaseRotateSpeed`）：

| 法术 | 和通用的差别（一句话） |
| --- | --- |
| 流星 | `max(速度, 50) × 角速度 × dt × 5` |
| 回旋镖 | **只覆盖 ChaseEnemy。** `Boomerang_ChaseTarget`：寿命前 33% 追敌，之后 lerp 回射击者。ChaseMouse 同样 33% 切鼠标→主人；ChaseOwner 前 33% 也朝主人（转向公式不同）；**Normal 无 33%，第 0 帧就开始弯回**。详见《回旋之刃逆向分析.md》3.1–3.3 |
| 审判之刃 / 彼岸刃 / 悬浮鬼火 / 召唤4 | 自管何时调用追敌 |
| 维度旅者 | 生成时 `ChaseRotateSpeed × 0.3` |
| 诡雷 | 生成后 `ChaseRotateSpeed × 0.5` |

反过来还有一类：**法术本来就有自己的追踪，插了导航反而被顶替。**

| 法术 | 插上导航之后 |
| --- | --- |
| 蝴蝶 `1003` | `ChaseEnemy` 的 switch **没有 Butterfly case**，落到 `default → Normal_ChaseTarget`。专属 `Butterfly_Common`（硬编码 `lerp(Direction, 敌向, 5f·dt)`，时间型、与速度无关）完全不再被调用。而 `Spell1003ButterFlySystem` 的查询不看 `movement.Type`，减速照旧跑到 `InitialSpeed × 0.15`——于是 `maxDelta = 2.25 × float1 × dt`，Lv1 约 27 °/s、Lv3 约 85.5 °/s，**低于原生手感**。`StartTraceTargets` 仍会置真但无人读取 |
| 黑洞 `1007` | 同样没有 `BlackHole` case，落到 `Normal_ChaseTarget`。和蝴蝶不同：黑洞**本来就没有**专属追踪，导航是从「匀速直飞」变成「有追敌」，属于纯增量。但 `Speed=1.2`，Lv1 约 **14.4 °/s**、Lv3 约 45.6 °/s，肉眼几乎看不出来。真正把人留在圈里的是 `float2` 拉力，不是这点转向。详见《黑洞逆向分析.md》7.1 |

复刻时不要假设「插导航一定变强」。对速度极低的弹，路程型转向要么是降级（蝴蝶：顶替了更好的原生转向），要么弱到可以忽略（黑洞：补上了追踪但转不动）。

还有第三类：**不走 `SpellMoveJob`，但在自己的 Job 里另实现一套 `ChaseEnemy`。**

| 法术 | 插上导航之后 |
| --- | --- |
| 激光 `1004` | 不走 `SpellMoveJob`（hitscan，一帧成线，`Speed` 首帧被清 0），但 `Spell1004LaserJob` 有专属 `ChaseEnemyLinePosStep`（L1063–1078）：**把原本笔直的射线掰成一条弧**。逐步 `DTool.DirMoveTowardsIgnoreZ(..., ChaseRotateSpeed × orgStepLength)`，锁最小夹角敌人；**无目标就退化成一次性全长直线**（`StraightGoLinePosStep`） |

激光的每步转角上限 = `ChaseRotateSpeed × Speed × 0.05`，段数恒为 20（预算与步长同比放大）：

| 槽位 | `ChaseRotateSpeed` | 单步长 | 每步转角上限 | 20 步累计 |
| --- | --- | --- | --- | --- |
| `[导Lv1] [激光]` | 12 | 0.75 | **9°** | 180° |
| `[导Lv2] [激光]` | 22 | 0.75 | **16.5°** | 330° |
| `[导Lv3] [激光]` | 38 | 0.75 | **28.5°** | 570°（早已收敛） |
| `[导Lv1] [加Lv3] [激光]` | 12 | 1.35 | **16.2°** | 324° |

注意公式里带 `Speed` 因子 → **加速器会放大自动导航在激光上的效果**（这是全表少见的两卡乘性耦合）。Lv3 只需 1–3 步就把方向拧到目标上，视觉上是「急弯后直射」；Lv1 是一条明显长弧。激光另外还有 `ChaseMouse` / `ChaseOwner` / `Rotation` 三套专属线生成器，详见《激光逆向分析.md》§4。

还有第四类：**`Speed = 0`，通用公式整体空转。**

自动导航只做一件事——把 `movement.Type` 写成 `ChaseEnemy`（`SpellShootDataExtend.cs` L110–111）。它不改速度、不加时长（见 4.1 末），所以 `Speed` 一旦是 0，4.1 的公式两端同时归零。

| 法术 | 插上导航之后 |
| --- | --- |
| 落雷阵 `1018` 光环态（`speed = 0`） | `Type` 确实写成了 `ChaseEnemy`，但 `ChaseEnemy` 的 switch 里没有 `ThunderAura` case，落到 `default → Normal_ChaseTarget`。`maxDelta = 0`、`velocity.Linear = 0`——光环一度不转、一米不动。**单插是废符文** |
| 落雷阵 `1018` 坠落态 | **例外，这里有效**：`Spell1018FallJob.CalcFallTargetPosition` 自己实现了一套 `ChaseEnemy`（`Spell1018ThunderAuraSystem.cs` L849–864），把下一跳的落点方向拧向最小夹角敌人 |

```541:545:decompiled/Assembly-CSharp/SpellMoveJob.cs
			float num = (movement.IsFallSpell ? movement.OriginalSpellHorizontalSpeed : movement.Speed);
			float3 source = movement.Direction;
			float3 target = DTool.IgnoreZDir(in targetPosition, in transform.Position);
			movement.Direction = DTool.DirMoveTowardsIgnoreZ(in source, in target, num * movement.ChaseRotateSpeed * DeltaTime);
			velocity.Linear = movement.Direction * movement.Speed;
```

`num = Speed = 0` → `maxDelta = 0`，`Direction` 一度不转；末行 `velocity.Linear = Direction × 0 = 0`，位置也不变。**两处都被 `Speed` 归零**，所以这不是「转得慢」（蝴蝶那种降级），是完全不发生。

**配加速器才活。** `Speed` 非 0 后 `maxDelta = Speed × ChaseRotateSpeed × dt` 恢复正常：


| 槽位 | `Speed` | °/米 | 每秒最多转 |
| --- | --- | --- | --- |
| `[导Lv3] [落雷阵]` | 0 | 38 | **0°** |
| `[加Lv1(+3)] [导Lv1] [落雷阵]` | 3 | 12 | 36 °/s |
| `[加Lv1(+3)] [导Lv3] [落雷阵]` | 3 | 38 | **114 °/s** |


`[加速器Lv1] [自动导航Lv3] [落雷阵]` 把静止光环变成一朵会追人的雷云，转向足够灵活。代价是占两个槽位，而且加速器单插对落雷阵本身是负收益（见《加速器逆向分析.md》3.4）。

坠落态那条例外值得单独记：`CalcFallTargetPosition` 用的是 `DirMoveTowardsIgnoreZ(..., ChaseRotateSpeed * 3f)`，**式子里不乘 `Speed`**，所以 `Speed = 0` 不妨碍它。`[坠落] [反弹] [自动导航] [落雷阵]` = 会自动追着敌人跳的连锁落雷。这也说明「不乘 `Speed`」的写法能让这张卡在静止法术上生效——通用路径的空转是实现选择，不是必然。

**通用限定**：这张卡的效果挂在**路程**上，而路程 = `Speed × 时间`，且它自己既不加速度也不加时长。所以它的强度和载体速度是乘性的：`Speed = 0` 必然空转，速度极低是降级（蝴蝶，见上一类），速度高才划算——和加速器在激光上的耦合（`[导] [加] [激光]`）是同一件事的两面。

持续施法的攻击距离计算里，自动导航**不加**额外秒数；只有公转会把 `float1` 折进部分持续法术的距离。蝴蝶是全表唯一把 `float2` 乘进距离估算的法术（`速度 × 时长 × 0.15`），和导航无关。

---

## 7. 开发注意事项

细节在第 3–6 节。这里只记复刻时会做错的几条。

**运动**

1. **按路程转，不要做成 °/秒。** `maxDelta = 速度 × float1 × dt`。速度为 0（悬停、刚停、或配置本身 `speed = 0`）就不再转。转弯半径只看 `float1`。
2. **锁最小夹角，粘住。** 不是最近、不是每帧重选。没目标就直线飞，下一帧再锁。
3. **总转角看路程。** 自动导航不加寿命。魔法弹 16 m/s × 0.5 s = 8 m，Lv1 最多约 96°。要 U 转得加时长、加速度或叠角速度。

**叠加与互斥**

4. **同卡加算，跨轨道右优先。** `[导航][公转][弹]` 走公转，导航作废。卡面「随机」不要做成洗牌；`ListShuffle` 是死代码。
5. **ECS 只加赢了的那种角速度。** 导航和自我追踪并排时，不要把两张 `float1` 加进同一个速度再只生效一种轨道——那是 Mono 旧路径。
6. **表上的「对召唤无效」运行时不读。** 手插在召唤左侧仍会写入。自动填槽也不拦。

**不要当通用**

7. **激光族、持续喷流、部分阶段法术不走 4.1。** 但「不走通用路径」≠「这张卡没用」：激光在自己的 Job 里重实现了一套 `ChaseEnemy`，把射线掰成弧，且转角公式含 `Speed` 因子（与加速器乘性耦合）。流星会把角速度放大。命中销毁优先，穿透耗尽就回收，跟踪停。
8. **有自己追踪的法术，导航是覆盖而非叠加。** 蝴蝶插导航会丢掉专属分支，只剩通用路程型转向，而它的速度被自身机制压到 15%——结果是变钝。别把「运动类型改写」当成纯增益。
9. **`Speed = 0` 的法术上这张卡是纯空转。** 通用公式里 `maxDelta` 和 `velocity.Linear` 两处都带 `Speed` 因子，而这张卡自己不加速度也不加时长，所以运动类型写成 `ChaseEnemy` 也一点用没有（落雷阵 `1018`，见 §6 第四类）。要让它在静止法术上生效，得像 `Spell1018FallJob` 的坠落分支那样把 `Speed` 因子从公式里去掉。

---

## 8. 算例

魔法弹 Lv1，`speed = 16`，`duration = 0.5`，先不算玩家和法杖。打空，让它活到寿命结束。飞行路程 8 m。


| 槽位 | 运动 | °/米 | 本段最多转 | 说明 |
| --- | --- | --- | --- | --- |
| `[弹]` | Normal | 0 | 0 | 直线 |
| `[导Lv1] [弹]` | ChaseEnemy | **12** | **96°** | 转不满 180° |
| `[导Lv2] [弹]` | ChaseEnemy | **22** | **176°** | 接近掉头 |
| `[导Lv3] [弹]` | ChaseEnemy | **38** | **304°** | 超过一圈 |
| `[导Lv1]×2 [弹]` | ChaseEnemy | **24** | **192°** | 加算；自动填槽不会这样摆 |
| `[时Lv1] [导Lv1] [弹]` | ChaseEnemy | 12 | **480°** | 时长 +2，路程 40 m |
| `[公转] [导Lv1] [弹]` | ChaseEnemy | 12 | 96° | 右优先，导航赢 |
| `[导Lv1] [公转] [弹]` | Rotation | — | — | 公转赢，12°/米不进 Chase |
| `[导Lv1] [弹]` 无目标 | ChaseEnemy | 12 | 0 | 直线，仍消耗 0.5 s |
| `[导Lv1] [悬Lv1] [弹]` | ChaseEnemy | 12 | 96° | 悬停阶段速度 0，不再转 |


`[导][弹]` 命中且无穿透：实体销毁，走不到把角度转完。

表里最后一行的「速度 0 不再转」只是悬停段的临时状态。`speed = 0` 的法术（落雷阵 `1018`）从第一帧起就是这个状态，整段寿命都转不了，见 §6 第四类。

---

## 9. 证据


| 结论 | 文件 |
| --- | --- |
| 12 / 22 / 38，售价 20/60/180 | `SpellConfig.json`（30111–30113） |
| 文案 / 底部轨道说明 | `TextConfig_Spell.json`（703011x / 713011x / 7230111） |
| 同卡加算 | `SpellShootData.GetChaseEnemyRotateSpeed`；`SpellBase` `+= float1` |
| 运动类型右优先 | `SpellShootDataExtend.GetSpellMovementType`；`SpellInitialParameter.finalMovementType` |
| 洗牌死代码 | `SpellBase.ApplyShootGroupV2EnhanceEffect`（`list` 未 Add） |
| 发射包角速度 | `ShootSpellUtils`：`ChaseEnemy` 才写 `GetChaseEnemyRotateSpeed` |
| Mono 转向 | `SpellBase.SpellFollowTarget`；`Tool2D.DirMoveTowards` |
| ECS 转向 / 锁敌 | `SpellMoveJob.Normal_ChaseTarget` / `GetValidChasePosition`；`DTool.DirMoveTowards`（`math.radians`）。注意 `GetValidChaseEnemyPosition` 是蝴蝶专用的另一支 |
| 蝴蝶落到 default | `SpellMoveJob` `ChaseEnemy` switch 的 case 只有 `HoverTorch` / `Meteor` / `JudgementBlade` / `MagicBreaker` / `Boomerang` / `Summon4` / `BiAnLethalBlade` / `HighPressureWasher` / `BlueRune` |
| 最小夹角 | `RoomController.GetMinimalAngleTargetablePpt`；`CurrentRoomEntities.FindMinAngleTarget` |
| 不加 Duration | `SpellShootData.GetSpellDuration` 无 `FollowTarget` 分支 |
| 只写运动类型、不碰速度 | `SpellShootDataExtend.GetSpellMovementType`（L110–111） |
| `Speed = 0` 时公式两端归零 | `SpellMoveJob.Normal_ChaseTarget`（L541–545）：`maxDelta = Speed × ChaseRotateSpeed × dt`、`velocity.Linear = Direction × Speed` |
| 坠落落雷阵的专属 `ChaseEnemy`（不乘 `Speed`） | `Spell1018ThunderAuraSystem.CalcFallTargetPosition`（L849–864）：`DirMoveTowardsIgnoreZ(..., ChaseRotateSpeed × 3f)` |
| 自动填槽组互斥 | `SpellAutoFullTool.RepelSpellGroup` |
| 流星/火球不避开导航 | `SpellAutoFullTool.SpellAvoidEnhances`（只避 `FollowOwner`） |
| 枚举 | `SpellAbilityType.FollowTarget = 3011`；`SpellSpecialMovementType.ChaseEnemy` |


未展开：各法术 Job 里自定义的追敌系数与阶段开关、召唤物抄角速度的逐条、`extraEnhanceIds` 与 `EnhanceList` 顺序不一致时的边角、旧 `ApplyEnhanceSpell` 非 V2 路径。
