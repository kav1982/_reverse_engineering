# 粘液晶石（Spell3004 / MucusCrystal）逆向分析

> 仅玩家法术增强符文「粘液晶石」（`30041–30043`）。槽位解析与《伤害强化》《范围增强》同一套，本文不复述。
> 共享「元素附着」管线（颜色随机 / 命中全生效）写在第 6 节，不逐条展开冰冻/毒/燃烧。
> 方法：`SpellConfig.json` + 反编译交叉验证。未标注「推测」的结论均有代码/数据直接证据。

---

## 1. 它是什么

插在主动/召唤**左侧**的元素附着符文。命中单位后挂一层粘液：减速 + 让目标自己射出的法术变慢。没有独立 `Spell3004*System`，效果写在发射参数的 `SpellElementEffectComponentData` 里，由命中结算调用 `SetMucus`。


| 项 | 值 |
| --- | --- |
| 英文名 | Slime Crystal |
| 内部名 | `MucusCrystal` 粘液水晶 |
| `abilityType` | `3004` / `SpellAbilityType.MucusCrystal` |
| 配置 ID | `30041` / `30042` / `30043` |
| 稀有度 / 类型 | 普通，`useType=2`（Enhance），1 槽，0 蓝 |
| 合成 / 售价 | Lv1→2→3；**6 / 18 / 54** 金（比伤害强化的 9/27/81 便宜一档） |
| 作用标记 | `haveEffecforMissileSpell=true`，`haveEffecforSummonSpell=true`（运行时**不读**这两个字段） |


文案（`7130041–7130043`，三级同一句）：

> 法术命中单位后使其在 **float1** 秒内移动速度×**float2**% 且施放的法术飞行速度×**float3**%

底部补充（`7230041`）是整类元素附着的共享说明，见第 6 节。

---

## 2. 数值

### 2.1 配置表（`SpellConfig.json`）


| ID | Lv | float1 持续秒 | float2 移速×% | float3 飞行速度×% | 可合成 |
| --- | --- | --- | --- | --- | --- |
| 30041 | 1 | **3** | **60** | **70** | true |
| 30042 | 2 | **4** | **45** | **50** | true |
| 30043 | 3 | **5** | **30** | **30** | false |


`int1/2/3` 全 0，不用。等级不是「每级 ×2」：时长微增，两个减速系数各自变狠。

### 2.2 字段语义


| 字段 | 语义 | 运行时 |
| --- | --- | --- |
| **float1** | 附着时长（秒） | 多张取 **max** |
| **float2** | 目标移速系数（文案是「×float2%」，不是「减 float2%」） | 多张 **连乘** `float2/100` |
| **float3** | 目标**自己施放**的法术飞行速度系数 | 多张 **连乘** `float3/100` |


`float2=60` 就是 `×0.6`（慢 40%），不是再乘一次 60%。

---

## 3. 怎么叠（两层，别混）

### 3.1 法术侧：组卡时合成一包元素数据

当前主路径是 `SpellShootData.GetMucusElementData` → `SpellInitialParameter.MucusElementData` → `SpellElementEffectComponentData`：

```430:447:decompiled/Assembly-CSharp/SpellShootData.cs
	public (float moveSpeedRatio, float spellSpeedRatio, float duration) GetMucusElementData()
	{
		float num = 0f;
		float num2 = 1f;
		float num3 = 1f;
		// ...
			if (finalConfig.abilityType == SpellAbilityType.MucusCrystal)
			{
				num = math.max(num, finalConfig.float1);
				num2 *= finalConfig.float2 / 100f;
				num3 *= finalConfig.float3 / 100f;
			}
		return (num2, num3, num);
	}
```

- **时长**：取最大，不累加。
- **两个速度系数**：连乘，无上限、不「只取最高级」。
- 初始必须是 `(1, 1, 0)`。结构体默认值是 `0`，从 0 连乘会变成永久静止。

因此等级**不能**按「2×Lv1 = 1×Lv2」换算：

```
2×Lv1：时长 3，移速 ×0.36，飞行 ×0.49   （更慢，更短）
1×Lv2：时长 4，移速 ×0.45，飞行 ×0.50
3×Lv1：时长 3，移速 ×0.216，飞行 ×0.343
1×Lv3：时长 5，移速 ×0.30，飞行 ×0.30
```

合成省槽，也换「更长、单卡更狠」；多张低级在减速上往往更狠。

旧 Mono `ApplyEnhanceSpell` 里时长写成 `Mathf.Max(spellConfig.float1)`（单参数，等于覆盖成最后一张）。以 ECS / `GetMucusElementData` 为准。

### 3.2 目标侧：命中时 `SetMucus`（覆盖，不是再乘）

```473:498:decompiled/Assembly-CSharp/UnitProperty_Dots.cs
	public void SetMucus(float time, float moveSpeedRatio, float spellSpeedRatio, bool changeColor = true)
	{
		if (IsImmuneMucus || IsInvincible) return;
		// 精英/Boss：时长 *= frozenTimeRatio（常见 Boss 0.1，玩家 0.5，小怪 1）
		if (affect_MucusHitTimer < num)
			affect_MucusHitTimer = num;          // 时长只在「新 > 剩余」时刷新
		affect_MucusMoveSpeedRatio = Clamp(moveSpeedRatio, 0, 1);
		affect_MucusSpellSpeedRatio = Clamp(spellSpeedRatio, 0, 1);  // 直接覆盖
	}
```

后一发**较弱**的粘液会把已经更慢的系数盖回去。时长不会被短的那发砍短。系数钳在 `[0, 1]`，加速不了。

免疫 / 无敌直接 return。`PurifyAbnormalState` 会清掉整层。

---

## 4. 谁吃得到

和伤害强化一样：只强化**自己右边、同一段**里的主动/召唤；主槽加不到后槽。

```
[粘] [弹]           弹带粘液
[弹] [粘]           弹带不到
[粘] [弹] [弹]      两发都带（解析器的 EnhanceList 会一直往后带）
```

自动填槽：`SelfRepelSpells` 含 `MucusCrystal`，**同一根杖不会自动再塞第二张**。手摆可以叠，按 3.1 连乘。

命中结算还有一道门（`UnitPropertyJob`）：

- `immuneDamage` 或 **`damage <= 0`**：只结算击退，**整包元素都不挂**（粘液/毒/冰/烧/虚空一起跳过）。
- 法术实体为空：不挂。
- 条件是 `MucusDuration > 0`，不是看外观颜色。

闪电链、部分召唤物从宿主拷贝 `SpellElementEffectComponentData` / Mono 的 `spellMucus*`，命中仍走 `SetMucus`。

玩家自己也能被挂（`800001`：`immuneMucus=false`，`frozenTimeRatio=0.5`）。「飞行速度×70%」减的是**被命中单位往外射的法术**，不是你这发弹自己的速度。

落地：`MoveSpeed *= affect_MucusMoveSpeedRatio`；单位 `ShootSpell` / 若干怪物射击 Job 再乘 `affect_MucusSpellSpeedRatio`。

---

## 5. 命中后发生什么

```
法术 EnhanceList 合成 MucusDuration / Move / Spell 系数
        ↓ 命中且伤害 > 0
   SetMucus（免疫/无敌无效）
        ↓
   affect_IsMucusHit = true
   身体染色 GameConst.color_BodyMucus（黄 1,1,0）
   每帧倒计时；到点 ClearMucusState，还原颜色
   减速期间挂脚底特效 Prefabs/EF/EF_Deceleration_Dots（请求名 3004_Decelerate）
```

`affect_IsMucusDecelerate` 在倒计时期间为 true，用来驱动脚底特效，不是第二套数值。

---

## 6. 共享元素附着管线

文案 `7230041`：

> 当存在多种元素附着强化时，外观上会随机选择一种颜色，但命中目标时仍然产生所有元素附着强化的效果。

### 6.1 颜色只决定外观（和地面痕迹）

`SpellTools.GetSpellColor` 从 `EnhanceList` 里收集 `ToSpellColor()` 的去重结果，再随机抽一个：


| abilityType | 颜色 |
| --- | --- |
| `MucusCrystal` 3004 | `Mucus` |
| `VenomCrystal` 3005 | `Venom` |
| `Frozen` 3014 | `Frozen` |
| `FireCrystal` 3111 | `Fire` |
| `DeathInfect` 3129 | `Void` |


雷晶石**不在**这张映射里；它用自己的 `float3` 概率把颜色改成 `Thunder`，先于随机抽取。

没有元素色时回落到 `Player`。法杖「随机颜色」/彩虹丝带会先定色，并可能再灌一张对应 Lv1 晶石进效果包（`ApplyElementSpell`），粘液系数会再乘一次。

### 6.2 效果按数据全上，不看颜色

`UnitPropertyJob` 对毒/冰/粘/烧/虚空是**并列 if**，不互斥。弹是火颜色，只要 `MucusDuration > 0` 仍会减速。

### 6.3 地面痕迹跟颜色，不跟效果包

`ColorType == Mucus` 才会 `CreateMucusRequest`（滚球、抛物、高压水枪销毁等）。带了粘液晶石但随机成了毒色：命中仍减速，**不铺地**。

地上粘液是另一套，不要和晶石混用：

| | 晶石命中 | 踩到地上粘液 |
| --- | --- | --- |
| 条件 | `MucusDuration > 0` 且伤害 > 0 | `ColorType==Mucus` 留下的 `MucusTag`；非飞行、非免疫地面 |
| 移速 | 法术包里的连乘系数 | 固定 `Min(0.6, 当前系数)` |
| 飞行速度 | 有 | **无** |
| 时长 | float1（精英/Boss 再乘 `frozenTimeRatio`） | 站上去才有，离开即无 |
| 与晶石共存 | — | 已有 `affect_IsMucusHit` 时**不再**吃地面层 |


史莱姆怪、药水「免疫粘液」走 `immuneMucus` / `ImmuneMucusRegister`，不是这张卡。

---

## 7. 开发注意事项

1. **外观和效果拆开。** 颜色随机一种；命中仍结算 EnhanceList 里全部元素。做成「显示黄色才减速」是错的。
2. **法术侧连乘，目标侧覆盖。** 组卡 `0.6 × 0.45`；命中后弱打强会把系数盖回去。复刻若想「只保留最慢」，要在 `SetMucus` 改成 `Min`，那是改规则，不是原版。
3. **时长两处都是 max，不是叠加。** 法术侧取最大 float1；目标侧只在新时长 > 剩余时刷新。
4. **系数语义是「乘到百分之几」，初始必须是 1。** 从 0 乘会锁死。钳制 `[0, 1]`。
5. **0 伤不挂元素。** 护盾、无敌帧、`damage<=0` 的触发弹不要指望还能上粘液。
6. **「飞行速度」是目标的输出，不是你的弹速。** 玩家中招后自己的法术也会慢（`SpellBase` / `UnitBase.ShootSpell` 乘 `affect_MucusSpellSpeedRatio`）。
7. **精英/Boss 吃 `frozenTimeRatio`。** 和冰冻共用这个字段；时长往往只有 10%。不要给 Boss 套满 5 秒。
8. **别和地上粘液共用一套状态。** 地面只有 60% 移速、不减速法术、不刷新晶石计时；晶石期间地面层被跳过。
9. **地面痕迹看颜色。** 多元素时可能有粘液效果但没有黄印。毒液痕迹伤友军，粘液痕迹不伤人。
10. **自动填槽自排斥，手摆可叠。** 测试连乘必须手插两张。
11. **继承要拷数据，不要只拷颜色。** 闪电链 ECS 带整份 `SpellElementEffectComponentData`；只传 `ColorType` 会丢减速。
12. **售价 6/18/54，时长 3/4/5。** 不要套伤害强化那套 9/27/81 和「每级 ×2」。

---

## 8. 算例

普通小怪（`frozenTimeRatio=1`），先不算彩虹丝带 / 随机色杖。


| 槽位 | 命中后（移速 ×，飞行 ×，秒） |
| --- | --- |
| `[弹]` | 无 |
| `[粘Lv1] [弹]` | ×0.60 / ×0.70 / 3 |
| `[粘Lv2] [弹]` | ×0.45 / ×0.50 / 4 |
| `[粘Lv3] [弹]` | ×0.30 / ×0.30 / 5 |
| `[粘Lv1]×2 [弹]` | ×0.36 / ×0.49 / 3 |
| `[粘Lv1][粘Lv2] [弹]` | ×0.27 / ×0.35 / 4 |
| 先 Lv3 再被 Lv1 打中 | 系数变成 ×0.60 / ×0.70，剩余时长仍按较长的那头 |
| 同上，打在 `frozenTimeRatio=0.1` 的 Boss 上 | 时长 0.5 秒，系数仍是法术包里的值 |


`[粘][毒][弹]`：外观黄或绿各 50%（再被雷晶石概率改色）；命中同时上粘液和毒。

---

## 9. 证据


| 结论 | 文件 |
| --- | --- |
| 3/4/5、60/45/30、70/50/30，售价 6/18/54 | `SpellConfig.json`（30041–30043） |
| 文案与底部共享说明 | `TextConfig_Spell.json`（713004x / 7230041） |
| 法术侧 max + 连乘 | `SpellShootData.GetMucusElementData` |
| 写入发射包 | `ShootSpellUtils` 填 `ElementComponentData`；`ApplyElementSpell` |
| 命中挂状态 | `UnitPropertyJob` → `UnitProperty_Dots.SetMucus` |
| 移速 / 射速落地 | `UnitProperty_Dots.MoveSpeed`；`UnitBase.ShootSpell` |
| 颜色随机、效果全上 | `SpellTools.GetSpellColor` / `ToSpellColor`；`UnitPropertyJob` 并列 if |
| 地面痕迹跟颜色 | `SpellSystem` / `SpellParabolaSystem` 的 `CreateMucusRequest` |
| 地面层 0.6、不叠在晶石上 | `UnitEnvironmentSystem` |
| 脚底特效 | `UnitAttachEffectSystem`（`3004_Decelerate` / `EF_Deceleration_Dots`） |
| 自动填槽自排斥 | `SpellAutoFullTool.SelfRepelSpells` |
| 闪电链继承 | `Spell3007Job` 拷 `ElementEffect`；Mono `SetElementEffect` |
| 枚举 | `SpellAbilityType.MucusCrystal = 3004` |


未展开：各怪物射击 Job 是否都乘了射速系数、彩虹丝带与随机色杖的额外 Lv1 灌入逐条、遗物/诅咒里的粘液免疫来源。
