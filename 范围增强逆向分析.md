# 范围增强（Spell3107 / EnhanceRadiusRatio）逆向分析

> 仅玩家法术增强符文「范围增强」（`31071–31073`）。槽位解析、`RatioValue` 与《伤害强化逆向分析》同一套，本文不复述。
> 方法：`SpellConfig.json` + 反编译交叉验证。

---

## 1. 它是什么

插在主动/召唤**左侧**的纯数值符文。文案「法术影响半径 +float1%」。没有独立 ECS 系统，只被 `GetSpellEffectRadius` 读 `abilityType` 后改一个比率。


| 项 | 值 |
| --- | --- |
| 英文名 | Range Boost |
| `abilityType` | `3107` / `EnhanceRadiusRatio` |
| 配置 ID | `31071` / `31072` / `31073` |
| 稀有度 / 类型 | 普通，`useType=2`（Enhance），1 槽，0 蓝 |
| 合成 / 售价 | Lv1→2→3；9 / 27 / 81 金 |


| ID | Lv | float1 | 可合成 |
| --- | --- | --- | --- |
| 31071 | 1 | **30** | true |
| 31072 | 2 | **60** | true |
| 31073 | 3 | **120** | false |


`float1` 每级 ×2。其余 `float/int` 全 0，不用。

等级只改加数：`1×Lv2 = 2×Lv1 = +60%`，`1×Lv3 = 4×Lv1 = +120%`。合成省槽，不提高单点效率。

---

## 2. 怎么叠

**加算，不是连乘。** 两张 Lv1 是 +60%（×1.60），不是 `1.3 × 1.3 = 1.69`。

```
最终半径 ≈ R × (1 + Σ范围增强float1/100 + 遗物/诅咒半径% + 法杖半径%) × 聚爆乘算
```

写入只有一步：`AddRatio(float1 / 100)`。同类型出现几次加几次，无上限、无「只取最高级」。

```244:261:decompiled/Assembly-CSharp/SpellShootData.cs
	public RatioValue GetSpellEffectRadius()
	{
		RatioValue ratioValue = new RatioValue(Spell.GetFinalConfig().radius);
		// ...
			case SpellAbilityType.EnhanceRadiusRatio:
				ratioValue.AddRatio(finalConfig.float1 / 100f);
			case SpellAbilityType.RadiuRatioDown:
				ratioValue.MulRatio(finalConfig.float1 / 100f);
```

之后拆进 `extraSizeRatio`（加算）和 `finalSizeRatio`（乘算）。Mono：`spellCfg.radius *= radiusRatio * finalRadiusRatio`。ECS：`Radius.AddRatio = extraSizeRatio`。这一层两边一致。

同加算池：遗物/诅咒的 `ExtraRadiusOfInfluence(isSpell: true)`、法杖「全杖影响半径%」。先加总再乘。

乘算层：聚爆（`3113`，×50%）。范围增强把半径做大后，聚爆「少掉的米」也变多，转伤更高——这是聚爆吃加算层，不是范围增强自己转伤。

遗物自己的圈（圣剑、戒指等）走 `isSpell: false`，**不吃**这张卡。

---

## 3. 谁吃得到

和伤害强化一样：只强化**自己右边、同一段**里的主动/召唤；主槽加不到后槽。

```
[范] [奥爆] [弹]     两张都吃
[奥爆] [范] [弹]     只有弹吃
[奥爆] [弹] [范]     都吃不到
```

配置标了 `haveEffecforSummonSpell=false`，但运行时**不读**。手动插在召唤左侧，召唤物的爆炸/拾取圈仍会变大。

自动填槽另有限制：只配给表里 `radius > 0` 的主法术；流星、火球、死亡蝰蛇会被避开。手动摆不受影响。

魔能升阶走 `GetFinalConfig()`，抬的是这张卡的 `float1`，叠加规则不变。

---

## 4. 实际放大什么

它不自己画圈，只提高 `radiusRatio`。谁乘这个倍率，谁就变大：

- 表半径（奥爆术 `R=3`）
- 爆炸 / 坠落炸 / 虚空炸
- 公转半径、部分弹体缩放
- 召唤物拾取、自爆、探测
- 闪电链牵引（从宿主抄倍率，自己不单独结算）

**不是射程。** 不改速度、持续时间、蓝耗。

表 `radius=0` 的法术（魔法弹等）：配置半径仍是 0，但上面那些硬编码圈照样 +30%。自动填槽不会给它们配这张卡，手动摆仍然生效。

---

## 5. 算例

奥爆术 Lv1，`R = 3`。先不算玩家和法杖。


| 槽位 | 半径 |
| --- | --- |
| `[奥爆]` | 3.0 |
| `[范Lv1] [奥爆]` | 3.9 |
| `[范Lv1]×2` 或 `[范Lv2]` | 4.8 |
| `[范Lv3]` 或 `[范Lv1]×4` | 6.6 |
| 范Lv1 + 玩家 +20% | `3 × 1.50 = 4.5`（加算，不是 `3 × 1.30 × 1.20`） |
| 范Lv1 + 聚爆 ×50% | `3 × 1.30 × 0.50 = 1.95` |


---

## 6. 证据

| 结论 | 文件 |
| --- | --- |
| 30 / 60 / 120 | `SpellConfig.json`（31071–31073） |
| 文案 | `TextConfig_Spell.json`（703107x / 713107x） |
| 加算 | `SpellShootData.GetSpellEffectRadius` |
| 玩家/法杖同池 | `GetSpellEffectRadius_FinalPlayerValue` |
| 实伤写入 | `SpellBase`：`radiusRatio` / `spellCfg.radius` |
| 自动填槽 | `SpellAutoFullTool.Filter_EffectRangeEnhance` |
| 枚举 | `SpellAbilityType.EnhanceRadiusRatio = 3107` |

未展开：遗物半径来源逐条、各法术 Job 里额外改半径、魔法弹命中盒是否另有碰撞体。
