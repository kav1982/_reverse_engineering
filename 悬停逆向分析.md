# 悬停（Spell3008 / SpellHover）逆向分析

> 仅玩家法术增强符文「悬停」（`30081–30083`）。槽位解析与《伤害强化》《范围增强》同一套，本文不复述。
> 不展开悬浮鬼火（`HoverTorch = 1006`）、UI 鼠标悬停、减速悬停子弹（`9021x`）。
> 方法：`SpellConfig.json` + 反编译交叉验证。未标注「推测」的结论均有代码/数据直接证据。

---

## 1. 它是什么

插在主动/召唤**左侧**的生命周期符文。法术的**飞行段**走完后，不立刻销毁，而是在原地锁死再停 `float1` 秒。它不改伤害、半径、速度、蓝耗；只往发射包里写一个独立的 `HoverDuration`。


| 项 | 值 |
| --- | --- |
| 英文名 | Hover |
| 内部名 | `SpellHover` 悬浮 |
| `abilityType` | `3008` / `SpellAbilityType.SpellHover` |
| 配置 ID | `30081` / `30082` / `30083` |
| 稀有度 / 类型 | 普通，`useType=2`（Enhance），1 槽，0 蓝 |
| 合成 / 售价 | Lv1→2→3；**8 / 24 / 72** 金（比伤害强化的 9/27/81 便宜 1 金，仍是 ×3） |
| 作用标记 | `haveEffecforMissileSpell=true`，`haveEffecforSummonSpell=false`（运行时**不读**这两个字段） |


文案（`7130081–7130083`，三级同一句）：

> 法术结束时会在原地悬停 **float1** 秒

底部补充 `7230081` 为空。

**机制一句话**：`Duration` 和 `HoverDuration` 是两段计时。飞行结束 → 速度清零 → `HoverTimer` 从 0 加到 `HoverDuration` → 再销毁 / 缩小回收。`OnOver`、分裂发生在**第二段结束**，不是飞行刚停的那一帧。

---

## 2. 数值

### 2.1 配置表（`SpellConfig.json`）


| ID | Lv | float1 悬停秒 | 售价 | 可合成 |
| --- | --- | --- | --- | --- |
| 30081 | 1 | **2** | 8 | true |
| 30082 | 2 | **3** | 24 | true |
| 30083 | 3 | **5** | 72 | false |


`float2/3`、`int1/2/3` 全 0，不用。等级不是「每级 ×2」：2 → 3 → 5。

### 2.2 字段语义


| 字段 | 语义 | 运行时 |
| --- | --- | --- |
| **float1** | 悬停秒数 | 多张 **相加** 写入 `HoverDuration` / `SpellHoverTime` |
| float2/3、int1/2/3 | 未使用 | 配置恒 0，无引用 |


没有独立 `Spell3008*System` / Authoring。这张卡本身不挂伤害组件。`SpellHoverDamageData` 是部分法术预制体自带的驻留伤害，**不是悬停符文加进去的**。

---

## 3. 怎么叠

**加算秒数，不是取 max，也不是连乘。**

```398:411:decompiled/Assembly-CSharp/SpellShootData.cs
	public float GetSpellHoverDuration()
	{
		float num = 0f;
		SlotData[] enhanceList = EnhanceList;
		for (int i = 0; i < enhanceList.Length; i++)
		{
			SpellConfig finalConfig = enhanceList[i].GetFinalConfig();
			if (finalConfig.abilityType == SpellAbilityType.SpellHover)
			{
				num += finalConfig.float1;
			}
		}
		return num;
	}
```

Mono 两侧同样是 `SpellHoverTime += float1`（`SpellBase` 约 1237、2169 行）。

写入发射包：`SpellInitialParameter.SpellHoverDuration` → `ShootSpellUtils` 填 `HoverDuration`，`HoverTimer` 初始为 0。

```
1×Lv2 = 3s，2×Lv1 = 4s   （等级不能互替）
1×Lv3 = 5s，Lv1+Lv2 = 5s （只有这一档碰巧相等）
```

合成省槽，但单点效率不是「一张高级 = 两张低级」。自动填槽**不自排斥**（`SelfRepelSpells` 只有元素晶石），手摆和自动都可以叠多张。

魔能升阶走 `GetFinalConfig()`，抬的是这张卡的 `float1`，叠加规则不变。

---

## 4. 生命周期（核心）

### 4.1 两段计时，不要并进 Duration

`GetSpellDuration()` 只读时长强化（`3103`）、省电、公转/寻踪的额外秒数，**不读悬停**。悬停不进 `Duration`，只进 `HoverDuration`。

通用 ECS 路径在 `SpellSystem.UpdateDuration`：

```275:307:decompiled/Assembly-CSharp/SpellSystem.cs
			if (!spell.Movement.ValueRO.IsFallSpell)
			{
				SpellAbilityType abilityType = spell.Config.ValueRO.AbilityType;
				if (abilityType != Laser && ... && abilityType != HighPressureWasher && ...)
				{
					if (valueRW.DurationTimer < valueRW.Duration.Calculate())
					{
						valueRW.DurationTimer += DeltaTime;
						return;
					}
					spell.Movement.ValueRW.Speed = 0f;
					if (valueRW.HoverTimer < valueRW.HoverDuration)
					{
						valueRW.HoverTimer += DeltaTime;
					}
					else
					{
						// 缩小回收 或 destroySelf = true
					}
				}
			}
			valueRW.DurationTimer += DeltaTime;   // 坠落 / 激光等：只加 Duration，不走上面的悬停分支
```

对应 Mono（`Spell1001Bullet` 等）：`DurationTimer > duration` 后 `isFlyFinish = true`、速度清零，再把 `SpellHoverTimer` 加满，最后 `PoolRecycle`。

```
飞行中          DurationTimer < Duration          正常移动、正常命中
刚结束          Duration 用尽                      Speed = 0，进入悬停
悬停中          HoverTimer < HoverDuration        锁位置，实体还在
悬停结束        HoverTimer >= HoverDuration       销毁 / 缩小 / OnOver / 分裂
```

`SpellMoveJob` 在 `HoverTimer > 0 && HoverDuration > 0` 时把 `Speed` 和 `PhysicsVelocity.Linear` 都写成 0。第一帧 `HoverTimer` 仍是 0，靠 `UpdateDuration` 里那句 `Speed = 0` 刹住；下一帧 MoveJob 才接手锁死。不要只做「HoverTimer>0 才停」，会漏一帧位移。

### 4.2 谁跳过这条通用路径

`UpdateDuration` 在这些情况下**不加 `HoverTimer`**，只继续加 `DurationTimer`：

- `IsFallSpell`（坠落态）
- 激光 / 符文锤 / 激光束 / 彼岸致命刃 / 伞 / 高压水枪 / 戴夫鱼叉

这些法术要么自己读 `HoverDuration`，要么初始化时把悬停**折进 `Duration.Extra`** 再把 `HoverDuration` 清零。复刻通用管理器时，不要假设「所有法术都走 4.1」。

已知折进时长的（点名，不拆 Job）：

| 法术 | 做法 |
| --- | --- |
| 高压水枪 `1019` | `Duration.Extra += HoverDuration`，然后 `HoverDuration = 0`（继续喷，不是停在原地） |
| 诡雷 `1012`（非坠落） | 同上 |
| 巨型泡泡 `1024`（Mono） | `spellCfg.duration += SpellHoverTime` |

持续施法锁定：`SpellGroupShootDurationCalculator` 里，冲刺的按住时长是 `Duration + Σ悬停`；分解射线 / 高压水枪 / 龙息只取 `Duration`，不把悬停算进施法锁。

### 4.3 回收和触发器在第二段之后

Mono `PoolRecycle` 才调 `OnNeedTriggerOnOver`、分裂、结束传送。ECS 则是悬停走完才 `SpellDestroyTag`。

所以：

- `[悬停][弹][结束触发]`：触发弹在悬停 **2/3/5 秒后**才出，不是飞行结束立刻出。
- 命中且穿透耗尽会直接销毁，**走不到悬停**。打空、打墙反弹耗尽寿命，才会停在终点。
- 半命传送仍看 `Duration/2`，和悬停无关。

---

## 5. 悬停阶段还打不打

这张卡**不负责出伤**。它只延长实体存活、锁位置。出不出伤看法术自己：

| 情况 | 悬停阶段 |
| --- | --- |
| 普通弹（`Enter` 命中） | 新走进碰撞盒的单位仍吃 `Enter`；已经重叠的 `Stay` **不会**再结算 |
| `isDPS` / 自带 `DamageInterval`（如黑洞） | 实体还在，DPS 计时器通常继续转 |
| 奥爆 `1008` | 自带 `HoverDamageTimer`，悬停期间约每 **0.4s** 再全范围结算一次 |
| 带 `SpellHoverDamageData` 的预制体 | 独立 Job：`Stay` 重叠满 `Interval`（默认 0.33s）后对半径内单位打一圈；穿透耗尽会销毁 |
| 命中已死的弹 | 没有实体，无悬停 |

不要做成「悬停 = 自动 DPS」。也不要把 `SpellHoverDamageAuthoring` 当成这张卡的运行时组件。

碰撞盒、特效、音效在悬停期间都还在。视觉上应停住，不要播完飞行就淡出。

---

## 6. 谁吃得到

和伤害强化一样：只强化**自己右边、同一段**里的主动/召唤；主槽加不到后槽。

```
[悬] [弹]           弹吃 2s
[弹] [悬]           吃不到
[悬] [奥爆] [弹]     两张都吃
[悬] [召唤]         运行时会写入 HoverDuration（表标了召唤无效，但不读）
```

配置 `haveEffecforSummonSpell=false`，自动填槽的 `Filter_SummonOnlyEnhance` 只看 `haveEffecforMissileSpell`。悬停的导弹标记是 true，所以自动填也会给召唤配。部分召唤物（`Teammate3` / `Teammate51`）会从召唤者法术抄 `SpellHoverTime`。

自动填槽：不在 `IgnoreSpells`、不在 `SelfRepelSpells`、不在 `SpellAvoidEnhances`。奥爆/流星/火球/死亡蝰蛇没有避开悬停。

---

## 7. 开发注意事项

细节在第 3–6 节。这里只记复刻时会做错的几条。

**计时**

1. **不要做成「寿命 +float1」。** `Duration` 管飞行，`HoverDuration` 管结束后停多久。两张 Lv1 是 4 秒，底数从 0 加。等级 2/3/5，售价 8/24/72。
2. **进悬停当帧就要锁位置。** 此时 `HoverTimer` 仍是 0；只抄 `HoverTimer > 0` 会滑一帧。停在当时的坐标，公转/追踪不再跟。

**存活**

3. **命中销毁优先。** 穿透耗尽就回收，走不到悬停。`OnOver` / 分裂挂在悬停结束的回收上，不要在飞行刚停那帧放后槽。
4. **这张卡不出伤。** 只续命、锁位。默认只有新走进碰撞盒的 `Enter`；不要给所有弹加驻留 DPS。碰撞和特效要活过第二段。

**例外（不要当通用）**

5. **坠落和激光族不走 4.1。** 高压水枪 / 诡雷 / 泡泡会把秒数并进 `Duration`（这段更久，不是停在原地）。持续施法锁只有冲刺把悬停算进去。
6. **表上的「对召唤无效」运行时不读。** 手插在召唤左侧仍会写入。若复刻要拦召唤，得自己读字段，那是改规则。

---

## 8. 算例

魔法弹 Lv1，`duration = 0.5`，先不算玩家和法杖。打空，让它活到寿命结束。


| 槽位 | 飞行 | 悬停 | 总存活 | 说明 |
| --- | --- | --- | --- | --- |
| `[弹]` | 0.5 | 0 | 0.5 | 到点销毁 |
| `[悬Lv1] [弹]` | 0.5 | **2** | 2.5 | |
| `[悬Lv2] [弹]` | 0.5 | **3** | 3.5 | |
| `[悬Lv3] [弹]` | 0.5 | **5** | 5.5 | |
| `[悬Lv1]×2 [弹]` | 0.5 | **4** | 4.5 | 加算 |
| `[时Lv1] [弹]` | 2.5 | 0 | 2.5 | 时长强化 `+2` 进 Duration |
| `[时Lv1] [悬Lv1] [弹]` | 2.5 | **2** | 4.5 | 两段分开加 |
| `[悬Lv1] [弹]` 命中且无穿透 | — | — | ≈0 | 走不到悬停 |


`[悬][弹][结束触发·奥爆]`：奥爆在弹悬停结束、回收时才放，不是飞了 0.5s 就放。

---

## 9. 证据


| 结论 | 文件 |
| --- | --- |
| 2 / 3 / 5，售价 8/24/72 | `SpellConfig.json`（30081–30083） |
| 文案 | `TextConfig_Spell.json`（703008x / 713008x） |
| 加算写入 | `SpellShootData.GetSpellHoverDuration`；`SpellBase` `+= float1` |
| 发射包字段 | `SpellInitialParameter.SpellHoverDuration`；`ShootSpellUtils` 填 `HoverDuration` |
| 通用两段生命周期 | `SpellSystem.UpdateDuration`；`Spell1001Bullet.Update` |
| 锁位置 | `SpellMoveJob`（`HoverTimer>0` 时速度清零） |
| 时长强化不进悬停 | `SpellShootData.GetSpellDuration` 无 `SpellHover` 分支 |
| 折进 Duration | `Spell1019HighPressureInitializedSystem`；`Spell1012TrickMineSystem`；`Spell1024GiantBubble` |
| 施法锁 | `SpellGroupShootDurationCalculator`（仅 Dash 加悬停） |
| 奥爆悬停再伤 | `Spell1008Job`（`HoverDamageTimer` / 0.4s） |
| 预制体驻留伤 | `SpellHoverDamageAuthoring` / `SpellHoverDamageJob` |
| 自动填槽不排斥 | `SpellAutoFullTool.SelfRepelSpells` / `IgnoreSpells` |
| 枚举 | `SpellAbilityType.SpellHover = 3008` |


未展开：各法术 Job 里自定义的悬停表现（激光拉长、流星停高、分解射线落地线、龙息淡出、审判之刃坠落伤）、召唤物抄 `SpellHoverTime` 的逐条、对象池与特效回收时序。
