# 闪耀星矢（Spell1026 / ShiningStar）逆向分析

> 范围：仅玩家主动法术「闪耀星矢」（`abilityType = 1026`，配置 ID `10261–10263`）。不含超新星（`Spell1027`）与敌人禁用列表语境。
> 方法：`SpellConfig.json` + 反编译 `Assembly-CSharp` 交叉验证。未标注「推测」的结论均有代码/数据直接证据。
> 配套：总览见《魔法工艺法术系统逆向报告.md》，原始数值见《法术数值总表.md》。精简版见 `闪耀星矢逆向分析.html`。

---

## 1. 身份与定位


| 项 | 值 |
| --- | --- |
| 中文名 | 闪耀星矢 |
| 英文名 | Shining Star Arrow |
| `abilityType` | `1026` / `SpellAbilityType.ShiningStar` |
| 配置 ID | `10261`（Lv1）/ `10262`（Lv2）/ `10263`（Lv3） |
| 文案 ID | 名 `701026x` / 机制 `711026x` / 风味 `7210261` / 短评 `7310261` |
| 稀有度 | 普通（`dropType=1`） |
| `useType` | `0`（主动法术） |
| 槽位 / 蓝耗 | `slotCost=1`；`mpCost` = 27 / 35 / 46 |
| 基础伤害 | `damage` = 78 / 148 / 281 |
| 蓄力标记 | `isKeepCasting=true`，`canCancelCasting=true` |
| 飞行 | `speed=30`，`duration=2`（松手后存活时长），`knockback=12` |
| 预制体 | 三级均 `prefab="10261"`，图标 `1026` |
| 合成 | Lv1→Lv2→Lv3（`canCompound`：Lv1/2=`true`，Lv3=`false`） |
| 售价（金币） | 13 / 39 / 117 |


**机制一句话**：按住蓄力的法术飞弹；蓄力期间暴击率随时间上涨，松手时若暴击率达到阈值则最终伤害乘算，视觉按暴击率分 4 档（紫光为满档）。

文案（`TextConfig_Spell` id `7110261–7110263`）：

> 蓄力施法，蓄力期间每秒暴击率+**float1**%  
> 暴击率超过**float3**%时最终伤害×**float2**%

风味（`7210261`）：

> 需要时间注入魔力，紫色的光芒象征着魔力达到最大，其伤害很难被其他法术超越。

短评（`7310261`）：蓄力爆发的强劲星体。

---



## 2. 数值结构



### 2.1 配置表原始值（`assets_export/SpellConfig.json`）


| ID | Level | damage | mpCost | float1 | float2 | float3 | int1 | duration | speed |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10261 | 1 | **78** | 27 | 25 | **150** | **33** | 10 | 2 | 30 |
| 10262 | 2 | **148** | 35 | 25 | **170** | **66** | 10 | 2 | 30 |
| 10263 | 3 | **281** | 46 | 25 | **200** | **100** | 10 | 2 | 30 |


规律：

- `float1` 三级恒 `25`（文案/Mono/AI 时长计算用「每秒 +25% 暴击」）；
- `float3` 阈值 33 → 66 → 100；`float2` 倍率 150 → 170 → 200；
- `int1=10` 三级恒定，见下节。

### 2.2 通用槽位语义


| 字段 | 语义 | 置信度 | 证据 |
| --- | --- | --- | --- |
| **float1** | 蓄力期间每秒增加的暴击率（百分点）。Mono / 阶段计算器 / AI 蓄力时长均读配置：`Δcrit = float1/100 * t` | **高（Mono）** / **中（ECS）** | `Spell1026TestHoldingSpell.GetCriticalChanceByHoldingTime`；`SpellChargeStageCalculator.Spell1026`；`SpellGroupShootDurationCalculator`。**ECS 主轨写死 `0.25f`，未读 `Float1`**（当前配置恰好等于 25/100） |
| **float2** | 达阈值后的最终伤害乘算系数（百分点，`× float2/100`） | 高 | Mono `StopHolding`；ECS `Spell1026ShiningStarEndChargingSystem` |
| **float3** | 触发伤害乘算所需的暴击率阈值（百分点） | 高 | 同上；队友 AI 也用其推算「蓄到触发」所需时间 |
| **int1** | 配置存在但**闪耀星矢专用逻辑均未读取** | 低（疑似残留/占位） | 文案无 `int1` 占位；`SpellSpawnParamsProcessor` 无 `ShiningStar` 分支消费 `Int1`；仅回旋镖等其它 ability 用 `Int1` 加穿透 |
| damage / mpCost | 基础伤害与蓝耗 | 高 | 配置 + 通用结算 |
| duration | 松手后飞弹存活时间（非最大蓄力时间） | 高 | Mono：`!IsHolding` 后 `DurationTimer > spellCfg.duration` 回收 |
| speed / knockback | 弹速 30、击退 12 | 高 | 配置；ECS 松手后坐额外后坐力另算 |


> 视觉阶段阈值 **硬编码** 为 `0.33 / 0.66 / 1.0`，与 Lv1–3 的 `float3` 数值巧合对齐，但**阶段判定不读 `float3`**。伤害乘算阈值才读 `float3`。

### 2.3 运行时常量（写死在代码，非配置）


| 常量 | 值 | 位置 |
| --- | --- | --- |
| ECS 每秒暴击增量 | `0.25`（= 25%/s） | `ChargingSystem` / `EndChargingSystem` / `DisableColliderSystem` |
| 视觉阶段分界 | crit `>0.33` →2；`>0.66` →3；`≥1` →4 | `SpellChargeStageCalculator`；ECS `Ceil(crit/0.33)` clamp 1–4 |
| 体积缩放底 | 每档 `×1.1^stage`（ECS） | `Spell1026ShiningStarChargingSystem` |
| ECS 松手后坐力 | `2 * CurStage * 法杖后坐力倍率` | 同上（相对射击方向反推） |
| Mono 后坐力 | `shootRecoilPerStage * stage * 倍率` | `Spell1026TestHoldingSpell`（Inspector 字段） |
| 命中特效档数 | `clamp(Ceil(CriticalChance/0.33), 1, 4)` | `SpellTools` Hit 分支 |
| 非玩家最长按住 | `DurationTimer >= 5` 强制停蓄 | Mono `HoldingCondition` |


### 2.4 最终暴击与伤害公式

#### Mono 旧轨（`Spell1026TestHoldingSpell`）

蓄力中：

```
HoldingTime += dt
（松手前不结算伤害倍率）
```

松手 `StopHolding`：

```
overalCriticalChance += float1/100 * HoldingTime

若 overalCriticalChance >= float3/100 且 同阵营为玩家：
  damage = Ceil(damage * float2 / 100)
```

关键代码：

```307:310:decompiled/Assembly-CSharp/Spell1026TestHoldingSpell.cs
	private float GetCriticalChanceByHoldingTime()
	{
		return base.spellCfg.float1 / 100f * HoldingTime;
	}
```

```253:256:decompiled/Assembly-CSharp/Spell1026TestHoldingSpell.cs
		if (base.overalCriticalChance >= base.spellCfg.float3 / 100f && IsSameCamp(UnitType.Player))
		{
			base.spellCfg.damage = Mathf.Ceil(base.spellCfg.damage * base.spellCfg.float2 / 100f);
		}
```

注意：Mono **仅玩家阵营**吃 `float2` 乘算；ECS 无此阵营限制。

#### ECS 主轨

蓄力中（`SpellChargingTag` 启用）：

```
CriticalChance = baseCritical + ChargeTimer * 0.25   // 硬编码，不读 Float1
CurStage = clamp(Ceil(CriticalChance / 0.33), 1, 4)
```

松手后（`SpellChargingTag` 禁用，`EndChargingSystem`）：

```
CriticalChance = baseCritical + ChargeTimer * 0.25
若 CriticalChance >= Float3/100：
  Damage.MulRatio *= Float2/100

若 EnableConvertOverFlowCCToDamage 且 CriticalChance >= 1：
  // 溢出暴击转伤害（构筑/全局开关），与闪耀星矢专属倍率叠加
  MulRatio *= (baseCritical>=1 ? CriticalChance/baseCritical : CriticalChance)
```

```201:216:decompiled/Assembly-CSharp/Spell1026ShiningStarEndChargingSystem.cs
			uncheckedRefRW3.ValueRW.CriticalChance = uncheckedRefRW.ValueRW.baseCritical + uncheckedRefRW5.ValueRW.ChargeTimer * 0.25f;
			if (uncheckedRefRW3.ValueRW.CriticalChance >= uncheckedRefRW3.ValueRW.Float3 / 100f)
			{
				uncheckedRefRW3.ValueRW.Damage.MulRatio *= uncheckedRefRW3.ValueRW.Float2 / 100f;
			}
			if (uncheckedRefRW4.ValueRO.EnableConvertOverFlowCCToDamage && uncheckedRefRW3.ValueRW.CriticalChance >= 1f)
			{
				if (uncheckedRefRW.ValueRW.baseCritical >= 1f)
				{
					uncheckedRefRW3.ValueRW.Damage.MulRatio *= uncheckedRefRW3.ValueRW.CriticalChance / uncheckedRefRW.ValueRW.baseCritical;
				}
				else
				{
					uncheckedRefRW3.ValueRW.Damage.MulRatio *= uncheckedRefRW3.ValueRW.CriticalChance;
				}
			}
```

#### 达到倍率阈值所需蓄力时间（baseCrit=0 时）

```
t = float3 / float1
```


| 等级 | float3 | float1 | 理论最短蓄力 | 对应视觉档（硬编码） |
| --- | --- | --- | --- | --- |
| Lv1 | 33 | 25 | **1.32 s** | 进入 Stage 2（crit>33%） |
| Lv2 | 66 | 25 | **2.64 s** | 进入 Stage 3（crit>66%） |
| Lv3 | 100 | 25 | **4.00 s** | 进入 Stage 4（crit≥100%，紫光） |


有基础暴击时：`t = max(0, (float3/100 - baseCrit) / (float1/100))` —— AI / 时长计算器同式。

---



## 3. 运行时架构（双轨）

游戏同时残留 **MonoBehaviour 旧轨** 与 **DOTS/ECS 主轨**。当前玩家施法主路径走 ECS；分裂弹 / 部分触发仍可能走 Mono。

```
法杖按下射击 → 生成 Spell1026 Entity（带 SpellChargeData / SpellChargingTag）
        ↓
Spell1026ShiningStarDisableColliderSystem（Create 组，Shoot 后）
  · 记录 baseCritical，按当前 ChargeTimer 估初档
  · 碰撞响应暂改为 None（蓄力期不伤人）
        ↓
SpellChargeSystem：ChargeTimer += dt；松手/非按住 → 关 SpellChargingTag
        ↓
Spell1026ShiningStarChargingSystem（SpacialSpellSystemGroup）
  · Initialize：缩放、Charge/Shine 特效、音效
  · 蓄力中：暴击随时间涨、升档 Flash、体积 ×1.1^stage
  · 刚结束蓄力：ReadyToShoot、Trail、Shoot 特效、后坐力、暴击暂置回 base
        ↓
Spell1026ShiningStarEndChargingSystem（同组，After Charging）
  · 恢复碰撞；按 ChargeTimer 重算暴击；套 Float2 倍率 / 溢出暴击转伤
        ↓
飞弹命中 → SpellTools 按 CriticalChance 档位播 Hit_1..Hit_4
坠落触地 → Spell1026FallEffectSystem 播 Fall 粒子
```

**双轨对照**：


| 路径 | 暴击增速 | 伤害倍率触发 | 阵营限制 | 阶段计算 |
| --- | --- | --- | --- | --- |
| ECS 主轨 | 硬编码 `×0.25/s` | `Damage.MulRatio *= Float2/100` | 无 | `Ceil(crit/0.33)` 或 DisableCollider 分段 |
| Mono 旧轨 | 读 `float1` | 直接改 `spellCfg.damage` | **仅玩家** | `SpellChargeStageCalculator.Spell1026`（读 float1） |



### 3.1 相关类型 / 文件


| 文件 | 角色 |
| --- | --- |
| `Spell1026ShiningStarData` | ECS 组件：baseCritical / CurStage / 碰撞备份 / 特效引用 / ReadyToShoot |
| `Spell1026ShiningStarInitializeTag` | 可禁用 tag：首帧初始化 |
| `Spell1026ShiningStarIsChargingTag` | 可禁用 tag：仍处「本体系蓄力视觉」阶段 |
| `Spell1026ShiningStarAuthoring` | Baker：挂 Data + 两 tag（默认启用） |
| `Spell1026ShiningStarDisableColliderSystem` | 生成后关碰撞、记初档 |
| `Spell1026ShiningStarChargingSystem` | 蓄力视觉 / 暴击刷新 / 松手特效与后坐 |
| `Spell1026ShiningStarEndChargingSystem` | 恢复碰撞 + 结算倍率 |
| `Spell1026FallEffectSystem` | 坠落触地粒子 |
| `SpellChargeData` / `SpellChargeSystem` / `SpellChargeEndSystem` | 通用蓄力计时与松手收尾 |
| `SpellChargeStageCalculator` | Mono 阶段 1–4 |
| `Spell1026TestHoldingSpell` | Mono 蓄力飞弹逻辑 |
| `Spell1026Effect` / `Spell1026ChargeEffect` | Mono 特效与 Charge/Shine/Flash |
| `SpellGroupShootDurationCalculator` | AI/UI：蓄到 float3 所需时长 |
| `SpellTools.IsChargingSpell` | `ShiningStar` / `SuperNova` 视为蓄力法术 |


---



## 4. 蓄力与发射逻辑



### 4.1 通用蓄力计时（`SpellChargeSystem`）

对带 `SpellChargingTag` 的实体：`ChargeTimer += DeltaTime`。  
若射击者不是正在 `holdingShoot` 的玩家（且不满足魔导书/星芒特殊释放条件），则 **禁用** `SpellChargingTag` → 进入结束蓄力流程。

### 4.2 生成时：禁碰撞 + 初档（`DisableColliderSystem`）

```187:214:decompiled/Assembly-CSharp/Spell1026ShiningStarDisableColliderSystem.cs
			if (!uncheckedRefRW.ValueRO.RecordColliderType)
			{
				uncheckedRefRW.ValueRW.baseCritical = uncheckedRefRW4.ValueRW.CriticalChance;
				int num = Mathf.FloorToInt(uncheckedRefRW2.ValueRW.ChargeTimer);
				uncheckedRefRW4.ValueRW.CriticalChance = uncheckedRefRW.ValueRW.baseCritical + (float)num * 0.25f;
				if (uncheckedRefRW4.ValueRW.CriticalChance <= 0.33f)
				{
					uncheckedRefRW.ValueRW.CurStage = 1;
				}
				else if (uncheckedRefRW4.ValueRW.CriticalChance <= 0.66f)
				{
					uncheckedRefRW.ValueRW.CurStage = 2;
				}
				else if (uncheckedRefRW4.ValueRW.CriticalChance < 1f)
				{
					uncheckedRefRW.ValueRW.CurStage = 3;
				}
				else
				{
					uncheckedRefRW.ValueRW.CurStage = 4;
				}
				uncheckedRefRW.ValueRW.RecordColliderType = true;
				// ... 备份并 SetCollisionResponse(None) ...
			}
```

要点：

- `baseCritical` 保存生成时已有暴击（符文/法杖等）；
- 初档用 `Floor(ChargeTimer)` 离散秒数（与后续连续 `ChargeTimer*0.25` 略有差别）；
- 蓄力期间双 Compound 子碰撞体 `None`，避免贴身误伤。

### 4.3 蓄力中刷新（`ChargingSystem` query1）

```670:701:decompiled/Assembly-CSharp/Spell1026ShiningStarChargingSystem.cs
			float chargeTimer = uncheckedRefRW4.ValueRW.ChargeTimer;
			uncheckedRefRW6.ValueRW.CriticalChance = uncheckedRefRW2.ValueRW.baseCritical + chargeTimer * 0.25f;
			int num = Mathf.Clamp(Mathf.CeilToInt(uncheckedRefRW6.ValueRW.CriticalChance / 0.33f), 1, 4);
			// ... 升档时追加 Charge_Shine_i / Flash_i，Scale = 1.1^stage ...
			if (num == 4 && (bool)uncheckedRefRW2.ValueRO.ChargePrefab)
			{
				uncheckedRefRW2.ValueRO.ChargePrefab.Value.gameObject.SetActive(value: false);
			}
```

Stage 4 时关闭 `Charge_Charge` 蓄力环粒子——对应风味文案「紫色光芒 = 魔力最大」。

### 4.4 松手瞬间（`ChargingSystem` query2 → `EndChargingSystem`）

1. 关 `Spell1026ShiningStarIsChargingTag`，销毁 `Charge_Charge`；
2. 挂 `Trail_1..CurStage`，播 `ShootStar` / `Shoot_{stage}`；
3. 对射击者施加与档位成正比的后坐力（每射击者每帧最多一次）；
4. **暂时**把 `CriticalChance` 设回 `baseCritical`（随即由 EndCharging 用 ChargeTimer 重算）；
5. EndCharging：恢复碰撞响应；按公式套 `Float2` 与溢出暴击转伤。

`ReadyToShoot` 仅被置 `true`，**工程内无其它读取**——疑似死字段/未完成钩子。

### 4.5 Mono 路径要点

- `IHoldingSpell`：`StartHolding` 关触发器碰撞；`HoldingUpdate` 跟枪口/方向；`HoldingCondition` 看鼠标按住。
- 队友 AI：`OnLaunchFromUnitNotPlayer` 用 `(float3/100 - overalCrit) / (float1/100)` 自动蓄满阈值再射。
- 分裂弹继承 `HoldingTime`。
- Echo 生成时直接 `StopHolding()`（跳过蓄力）。

### 4.6 特殊运动：绕身蓄力星

`SpellChargeEndSystem`：若 ability 为 `ShiningStar` 且运动为 `Rotation`，并带 `SpellFromChargeModeStar`，松手后把 `AroundTarget` 绑到玩家环绕中心 / Owner——属于蓄力星 + 环绕强化的交叉路径。

---



## 5. 视觉与音效



### 5.1 特效名（ECS `SpellSingleton.Effects[1026]`）


| 名称模式 | 时机 |
| --- | --- |
| `Charge_Charge` | 蓄力环（Stage&lt;4） |
| `Charge_Shine_{1..4}` | 各档星芒叠加 |
| `Flash_{stage}` | 升档闪光 |
| `Trail_{1..stage}` | 松手拖尾 |
| `Shoot_{stage}` | 发射爆发（非 Rotation / 非分裂） |
| `Hit_{1..n}` | 命中，n 由当前 CriticalChance 档决定 |
| `3119_Fall_{Color}` | 坠落触地（FallEffectSystem） |


音效：`Flash` / `FinalFlash`（stage4）/ `ShootStar`，经 `DTool.GetSpellSEName(1026, …)`。

### 5.2 Mono 特效

`Spell1026ChargeEffect`：对象池 `Prefabs/Spell/10261/10261_Charge`；`Shine` 下 `Stage1..4` 显隐；满档停 `Charge`。  
`Spell1026Effect`：体型 `Pow(stageSpellSizeRatio, stage) * spellVolumeRatio`（Inspector，对应 ECS 的 1.1）。

### 5.3 档位与文案「紫光」

| Stage | 大致暴击率区间（硬编码） | 表现 |
| --- | --- | --- |
| 1 | ≤33% | 基础蓄力 |
| 2 | (33%, 66%] | Shine×2 |
| 3 | (66%, 100%) | Shine×3 |
| 4 | ≥100% | Shine×4，关蓄力环，FinalFlash —— **紫光满蓄** |


Lv3 的伤害阈值 `float3=100` 与 Stage 4 对齐；Lv1/Lv2 在更低档即可触发伤害倍率。

---



## 6. 数据结构速查

```csharp
// 配置侧（SpellConfig）
// abilityType=1026
// float1=每秒暴击百分点（Mono/文案/AI；ECS 未读）
// float2=达阈值后伤害乘算百分点
// float3=伤害乘算所需暴击百分点
// int1=10（闪耀星矢路径未使用）

struct Spell1026ShiningStarData : IComponentData {
    bool RecordColliderType;
    bool ResetColliderType;
    CollisionResponsePolicy Collider1Type, Collider2Type;
    UnityObjectRef<GameObject> ChargePrefab, ChargeShinePrefab;
    float baseCritical;
    int CurStage;              // 1..4
    bool ResizeSpellEffect;
    bool ReadyToShoot;         // 只写不读
}

struct SpellChargeData : IComponentData {
    float ChargeTimer;
}

// tags: SpellChargingTag, Spell1026ShiningStarInitializeTag,
//       Spell1026ShiningStarIsChargingTag (enableable)

// Mono
// Spell1026TestHoldingSpell : SpellBase, IHoldingSpell, ...
//   float HoldingTime; float shootRecoilPerStage;
// Spell1026Effect / Spell1026ChargeEffect
```

---



## 7. 与构筑相关的行为摘要


| 行为 | 结果 |
| --- | --- |
| 按住蓄力更久 | 暴击率线性上升；越高越易触发 `float2` 倍率与更高视觉档 |
| 堆基础暴击（符文/遗物） | 缩短达到 `float3` 的时间；可能开局即高档 |
| Lv1 短蓄（≈1.32s） | 即可吃 ×150%（相对轻松） |
| Lv3 吃满倍率 | 需蓄到 100% 暴击（≈4s，若无基础暴击），同时进紫光 Stage4 |
| 溢出暴击转伤害开关 | ECS 在 crit≥100% 时额外乘 `CriticalChance`（或相对 base 的比） |
| 改配置 `float1` | **文案/Mono/AI 会变；ECS 主轨仍 0.25/s**，模组时需注意双轨不一致 |
| 改 `int1` | 对闪耀星矢当前逻辑无影响 |
| 环绕类强化 + 蓄力星 | 松手后可能进入绕身飞行（`SpellFromChargeModeStar`） |


---



## 8. 证据索引（关键代码片段）

**枚举**

```29:29:decompiled/Assembly-CSharp/SpellAbilityType.cs
	ShiningStar = 1026,
```

**是否蓄力法术**

```2376:2383:decompiled/Assembly-CSharp/SpellTools.cs
	public static bool IsChargingSpell(this SpellAbilityType abilityType)
	{
		if (abilityType != SpellAbilityType.ShiningStar)
		{
			return abilityType == SpellAbilityType.SuperNova;
		}
		return true;
	}
```

**AI / UI 蓄力时长（读 float1 + float3）**

```25:30:decompiled/Assembly-CSharp/SpellGroupShootDurationCalculator.cs
			num = Mathf.Max(finalConfig.abilityType switch
			{
				SpellAbilityType.ShiningStar => Mathf.Max(0f, (finalConfig.float3 / 100f - ratioValue.Result) / (finalConfig.float1 / 100f)), 
				SpellAbilityType.SuperNova => finalConfig.float2, 
				_ => 0f, 
			}, num);
```

**Mono 阶段（读 float1，阈值硬编码）**

```58:84:decompiled/Assembly-CSharp/SpellChargeStageCalculator.cs
	public static int Spell1026(Param param)
	{
		// ...
		float num2 = num + param.Time * param.ShootData.Spell.GetFinalConfig().float1 / 100f;
		if (num2 > 0.33f)
		{
			if (!(num2 >= 1f))
			{
				if (num2 > 0.66f)
				{
					return 3;
				}
				return 2;
			}
			return 4;
		}
		if (num2 >= 0f)
		{
			return 1;
		}
		return 0;
	}
```

阶段阈值硬编码为 `>0.33` / `>0.66` / `≥1`。

**命中特效档位**

```2526:2535:decompiled/Assembly-CSharp/SpellTools.cs
		case SpellAbilityType.ShiningStar:
		{
			int num = math.clamp(Mathf.CeilToInt(config.CriticalChance / 0.33f), 1, 4);
			for (int i = 1; i <= num; i++)
			{
				FixedString32Bytes effectName = $"Hit_{i}";
				// ...
			}
			return;
		}
```

---



## 9. 待核实 / 推测


| 项 | 说明 |
| --- | --- |
| `int1=10` | 无闪耀星矢引用；可能与超新星等同号占位，或历史穿透残留。改表验证无反馈则视为无效字段 |
| `ReadyToShoot` | 仅赋值，无读取；可能是未完成联调字段 |
| ECS vs Mono `float1` | 当前数值碰巧一致；若热更改 `float1`，ECS 不会跟随——复刻/平衡应以 **ECS 硬编码** 为准，文案以配置为准 |
| Mono 非玩家不吃 float2 | 敌人/召唤物走 Mono 时可能只有暴击上涨无倍率；ECS 敌人若走主轨则仍吃倍率 |


---

*文档生成自配置与反编译交叉分析；复刻实现请以 ECS 主轨为准，并单独处理 float1 双轨差异。*
